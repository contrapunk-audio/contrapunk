# Rust build artifacts
target/

# WASM build output
dist/
ui/src/lib/wasm-pkg/

# SvelteKit static build output
ui/build/

# Node dependencies (any location)
node_modules/

# Tauri generated schemas
**/src-tauri/gen/

# graphify skill output (.claude/skills/graphify)
graphify-out/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store

# Environment
.env

# Logs
*.log

# HN monitoring data
.hn-monitoring/

# AU wrapper build artifacts
au-wrapper/build/

# Claude agent harness — committed bits (CLAUDE.md, settings.json,
# hooks/, skills/, agents/) are shareable; everything else under
# .claude/ is local-only runtime state (settings.local.json,
# scheduled_tasks.lock, worktrees/, etc.) and stays ignored.
.claude/*
!.claude/settings.json
!.claude/hooks/
!.claude/skills/
!.claude/agents/
.claude/.stop-autoloop-state

# Pi project-local packages/prompts. Keep GSD checked in so pi sessions
# have project GSD prompts, but ignore future local pi runtime state.
.pi/*
!.pi/gsd/
!.pi/gsd/**

# Local rigor-gate config (ephemeral, per-checkout)
rigor.yaml

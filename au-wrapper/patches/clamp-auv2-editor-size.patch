diff --git a/src/detail/auv2/auv2_shared.mm b/src/detail/auv2/auv2_shared.mm
index 0d26d7c..0ef3283 100644
--- a/src/detail/auv2/auv2_shared.mm
+++ b/src/detail/auv2/auv2_shared.mm
@@ -12,8 +12,22 @@ bool auv2shared_mm_request_resize(const clap_window_t* win, uint32_t w, uint32_t
   if (!win) return false;
 
   auto* nsv = (NSView*)win;
-  [nsv setFrame:NSMakeRect(0, 0, w, h)];
+  NSSize size = NSMakeSize(w < 1200 ? 1200 : (CGFloat)w, h < 800 ? 800 : (CGFloat)h);
+  [nsv setFrame:NSMakeRect(0, 0, size.width, size.height)];
+  for (NSView *subview in [nsv subviews])
+  {
+    [subview setFrame:[nsv bounds]];
+    [subview setAutoresizingMask:NSViewWidthSizable | NSViewHeightSizable];
+  }
 
-  return false;
+  NSWindow *window = [nsv window];
+  if (window)
+  {
+    [window setContentMinSize:NSMakeSize(1200, 800)];
+    [window setStyleMask:([window styleMask] | NSWindowStyleMaskResizable)];
+    [window setContentSize:size];
+  }
+
+  return true;
 }
 }  // namespace free_audio::auv2_wrapper
diff --git a/src/detail/auv2/wrappedview.asinclude.mm b/src/detail/auv2/wrappedview.asinclude.mm
index 3c362f6..a9e3071 100644
--- a/src/detail/auv2/wrappedview.asinclude.mm
+++ b/src/detail/auv2/wrappedview.asinclude.mm
@@ -34,6 +34,8 @@
 - (void)doIdle;
 - (void)attachCLAPGui;
 - (void)detachCLAPGui;
+- (void)resizeEmbeddedViews;
+- (void)configureHostWindow;
 - (void)dealloc;
 - (void)setFrame:(NSRect)newSize;
 
@@ -104,10 +106,8 @@ void CLAP_WRAPPER_TIMER_CALLBACK(CFRunLoopTimerRef timer, void *info)
   // NSWindow. nih-plug-webview/wry expects the parent NSView to already have
   // a window when set_parent() is called, so create the NSView now and defer
   // CLAP GUI creation/parenting to viewDidMoveToWindow.
-  if (size.width <= 0 || size.height <= 0)
-  {
-    size = NSMakeSize(1200, 800);
-  }
+  if (size.width < 1200) size.width = 1200;
+  if (size.height < 800) size.height = 800;
   self = [super initWithFrame:NSMakeRect(0, 0, size.width, size.height)];
 
   idleTimer = nil;
@@ -123,7 +123,20 @@ void CLAP_WRAPPER_TIMER_CALLBACK(CFRunLoopTimerRef timer, void *info)
 
 - (void)doIdle
 {
-  // auto gui = ui._plugin->_ext._gui;
+  NSView *container = [self superview];
+  if (!container) return;
+
+  NSRect target = [container bounds];
+  if (!NSEqualRects([self frame], target))
+  {
+    [super setFrame:target];
+    if (uiCreated && canary)
+    {
+      auto gui = ui._plugin->_ext._gui;
+      gui->set_size(ui._plugin->_plugin, target.size.width, target.size.height);
+    }
+  }
+  [self resizeEmbeddedViews];
 }
 
 - (void)attachCLAPGui
@@ -140,21 +153,48 @@ void CLAP_WRAPPER_TIMER_CALLBACK(CFRunLoopTimerRef timer, void *info)
   gui->set_parent(ui._plugin->_plugin, &m);
   gui->set_scale(ui._plugin->_plugin, 1.0);
 
-  if (gui->can_resize(ui._plugin->_plugin))
+  [self setAutoresizingMask:NSViewWidthSizable | NSViewHeightSizable];
+  [self configureHostWindow];
+  [self resizeEmbeddedViews];
+
+  auto size = [self frame].size;
+  gui->set_size(ui._plugin->_plugin, size.width, size.height);
+  uiCreated = true;
+}
+
+- (void)resizeEmbeddedViews
+{
+  NSRect bounds = [self bounds];
+  for (NSView *subview in [self subviews])
   {
-    clap_gui_resize_hints_t resize_hints;
-    gui->get_resize_hints(ui._plugin->_plugin, &resize_hints);
-    NSAutoresizingMaskOptions mask = 0;
+    [subview setFrame:bounds];
+    [subview setAutoresizingMask:NSViewWidthSizable | NSViewHeightSizable];
+  }
+}
 
-    if (resize_hints.can_resize_horizontally) mask |= NSViewWidthSizable;
-    if (resize_hints.can_resize_vertically) mask |= NSViewHeightSizable;
+- (void)configureHostWindow
+{
+  NSWindow *window = [self window];
+  if (!window) return;
 
-    [self setAutoresizingMask:mask];
+  NSView *container = [self superview];
+  if (container)
+  {
+    [[container superview] setAutoresizesSubviews:YES];
+    [container setAutoresizesSubviews:YES];
+    [container setAutoresizingMask:NSViewWidthSizable | NSViewHeightSizable];
+    [self setFrame:[container bounds]];
+    [self setAutoresizingMask:NSViewWidthSizable | NSViewHeightSizable];
   }
 
-  auto size = [self frame].size;
-  gui->set_size(ui._plugin->_plugin, size.width, size.height);
-  uiCreated = true;
+  [window setContentMinSize:NSMakeSize(1200, 800)];
+  [window setStyleMask:([window styleMask] | NSWindowStyleMaskResizable)];
+
+  NSSize contentSize = [window contentRectForFrameRect:[window frame]].size;
+  if (contentSize.width < 1200 || contentSize.height < 800)
+  {
+    [window setContentSize:NSMakeSize(contentSize.width < 1200 ? 1200 : contentSize.width, contentSize.height < 800 ? 800 : contentSize.height)];
+  }
 }
 
 - (void)detachCLAPGui
@@ -204,6 +244,7 @@ void CLAP_WRAPPER_TIMER_CALLBACK(CFRunLoopTimerRef timer, void *info)
 - (void)setFrame:(NSRect)newSize
 {
   [super setFrame:newSize];
+  [self resizeEmbeddedViews];
   if (uiCreated && canary)
   {
     auto gui = ui._plugin->_ext._gui;

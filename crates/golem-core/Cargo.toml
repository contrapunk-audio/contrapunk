[package]
name = "golem-core"
version = "0.1.0"
edition = "2021"
description = "Audio-native adaptive drummer engine for Golem"
authors = ["Contrapunk Team"]

[dependencies]
serde = { version = "1.0", features = ["derive"], optional = true }

[features]
default = []
serde = ["dep:serde"]

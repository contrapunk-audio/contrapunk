//! New (rewritten) implementations of the pipeline stages.
//!
//! Empty in Phase 0. Each subsequent phase fills in one stage as a
//! submodule (e.g. `onset.rs`, `pitch.rs`, `octave.rs`, `state_machine.rs`,
//! `emitter.rs`) so the rewrite can be reviewed and A/B tested in isolation.

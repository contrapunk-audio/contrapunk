[package]
name = "contrapunk-transport"
version = "1.1.0"
edition = "2021"
description = "Sample-accurate transport / clock primitive — atomics-only, audio-thread safe"
authors = ["Contrapunk Team"]

[lib]
name = "contrapunk_transport"
path = "src/lib.rs"

[dependencies]

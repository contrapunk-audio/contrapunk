[package]
name = "contrapunk-chord"
version = "1.1.0"
edition = "2021"
description = "Chord recognition + display from MIDI note sets"
authors = ["Contrapunk Team"]

[lib]
name = "contrapunk_chord"
path = "src/lib.rs"

[dependencies]

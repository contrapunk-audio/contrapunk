[workspace]
members = [
    ".",
    "src-tauri",
    "wasm",
    "plugin",
    "xtask",
    "crates/contrapunk-transport",
    "crates/contrapunk-midi",
    "crates/contrapunk-chord",
    "crates/contrapunk-harmony",
    "crates/contrapunk-audio",
    "crates/contrapunk-preset",
]
resolver = "2"

[package]
name = "contrapunk"
version = "1.2.0"
edition = "2021"
description = "Real-time MIDI harmony generation"
authors = ["Contrapunk Team"]

[lib]
name = "contrapunk"
path = "src/lib.rs"

[[bin]]
name = "contrapunk"
path = "src/main.rs"

[dependencies]
contrapunk-transport = { path = "crates/contrapunk-transport" }
contrapunk-midi = { path = "crates/contrapunk-midi" }
contrapunk-chord = { path = "crates/contrapunk-chord" }
contrapunk-harmony = { path = "crates/contrapunk-harmony" }
contrapunk-audio = { path = "crates/contrapunk-audio" }
contrapunk-preset = { path = "crates/contrapunk-preset" }
anyhow = "1.0"
wmidi = "4.0"
rand = "0.10"
ringbuf = "0.4"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
pitch-detection = "0.3"
[features]
default = []
web-midi = [
    "wasm-bindgen",
    "wasm-bindgen-futures",
    "js-sys",
    "web-sys",
    "getrandom",
    "contrapunk-midi/web-midi",
]

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
midir = "0.11"
clap = { version = "4", features = ["derive"] }
cpal = "0.17"
rmp-serde = "1.3"
# CLAP plugin hosting. `clack-host` is the idiomatic safe wrapper over
# the CLAP ABI. Not yet on crates.io — pinned by rev so upstream
# churn can't break our build. See src/plugin_host/clap/mod.rs for
# the full rationale.
clack-host = { git = "https://github.com/prokopyl/clack", rev = "4541f03792635dfdc6be9441a01b786e9b4ccf18" }
clack-extensions = { git = "https://github.com/prokopyl/clack", rev = "4541f03792635dfdc6be9441a01b786e9b4ccf18", features = ["clack-host", "gui", "log", "audio-ports", "note-ports"] }
thiserror = "2.0"

[target.'cfg(target_os = "macos")'.dependencies]
objc2 = "0.6"
objc2-app-kit = { version = "0.3", features = ["NSWindow", "NSView", "NSResponder", "NSApplication"] }
objc2-foundation = { version = "0.3", features = ["NSString", "NSGeometry", "NSObject"] }

[dependencies.wasm-bindgen]
version = "0.2"
optional = true

[dependencies.wasm-bindgen-futures]
version = "0.4"
optional = true

[dependencies.js-sys]
version = "0.3"
optional = true

[dependencies.web-sys]
version = "0.3"
features = ["Window", "Navigator", "MidiAccess", "MidiOptions", "MidiInput", "MidiOutput", "MidiPort", "MidiMessageEvent"]
optional = true

[dependencies.getrandom]
version = "0.2"
features = ["js"]
optional = true

[profile.release]
opt-level = "s"
lto = false
strip = true
panic = "abort"

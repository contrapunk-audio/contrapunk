<!-- Thank you for the PR! -->

# Changes

<!-- Describe your changes here - ideally from your descriptive commit message(s) -->

# Submitter Checklist

As the author of this PR, please check off the items in this checklist:

- [ ] Has [Tests](CONTRIBUTING.md) included if any functionality added or changed
- [ ] `cargo test` passes locally
- [ ] `cargo fmt --all --check` passes
- [ ] `cargo clippy` has no warnings
- [ ] `cargo check -p contrapunk-wasm --target wasm32-unknown-unknown` passes (if touching audio/harmony/WASM code)
- [ ] Follows the commit message standard (`feat`/`fix`/`refactor`/`docs` prefix)
- [ ] Has a kind label (`kind/bug`, `kind/feature`, `kind/cleanup`, `kind/docs`)
- [ ] User-facing changes documented in README or relevant docs
- [ ] Release notes block below updated with any user-facing changes

# Release Notes

```release-note
NONE
```

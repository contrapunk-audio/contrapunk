---
phase: 06.3-style-update
task: UI restructure (tabs removed, 3-column layout, pixel font)
total_tasks: 7 plans + ad-hoc UI work
status: in_progress
last_updated: 2026-01-30
---

<current_state>
Major UI restructure this session: removed the 3-tab system (Play/Craft/Settings), replaced with a single-screen three-column layout. Embedded Press Start 2P pixel font for authentic retro typography. All controls visible at once — no scrolling, no tabs.

The app builds cleanly with `cargo build --features gui`. Not yet tested with `trunk build` (WASM).
</current_state>

<completed_work>

Prior work (before this session):
- Plans 01-06 executed, plan 07 checkpoint rejected
- Full retro pixel theme overhaul (PICO-8 palette, sharp corners, CRT scanlines)
- Card-based layout in all tabs, ornate pixel widgets

This session:
- Removed Tab enum, active_tab field, tab bar from app.rs
- Deleted src/tabs/ directory (play.rs, craft.rs, settings.rs, mod.rs)
- Created src/ui.rs with draw_main_ui -> 3-column layout
- Replaced `mod tabs` with `mod ui` in lib.rs and main.rs
- Embedded Press Start 2P pixel font (assets/fonts/PressStart2P-Regular.ttf, 118KB)
- Configured FontDefinitions: Press Start 2P as primary for Proportional + Monospace
- TextStyle sizes: Small(8), Body(10), Button(10), Heading(14), Monospace(10)
- Removed all redundant `.family(FontFamily::Monospace)` annotations
- Fixed CONTRAPUNK logo hidden behind ornate border (inner_margin on title panel)
- Iterated 2-column -> 3-column to eliminate scrolling
- Removed settings_show_all_outputs field (all 8 outputs always visible)
- Merged MIDI Input + Outputs into single "MIDI I/O" card
- Compact button labels: Save As, New, Del, Exp, Imp

Commits (this session):
- 3520e1d feat(typography): embed Press Start 2P pixel font
- 13df4d4 fix(ui): fix logo hidden behind border and reduce layout to fit screen
- 94f4c52 fix(ui): switch to three-column layout to eliminate scrolling

Quick task tracked: .planning/quick/001-nice-fonts-typography/
</completed_work>

<remaining_work>

- User hasn't confirmed 3-column layout works (may need density tuning)
- User concern: "when I choose a preset or anything that expands, I have to scroll" — may still apply
- WASM build verification (`trunk build`)
- Visual polish: verify ornate widgets render correctly at new font sizes
- Uncommitted changes from prior sessions still in working tree (index.html, colors.rs, session.rs, various .planning files)
- Phase 06.3 formal checkpoint still pending
</remaining_work>

<decisions_made>

- Removed tabs entirely — single-screen side-by-side (user request)
- Three columns: Left=Performance+MIDI, Center=Harmony+Notes, Right=Metronome+Humanization
- Press Start 2P embedded font (OFL, Google Fonts) for both font families
- Card padding 6px, inter-card gaps 2px for maximum density
- Output slot labels shortened to just the number
- Preset buttons abbreviated: Exp, Imp, Del
- No ScrollArea — content must fit without scrolling
</decisions_made>

<blockers>
None active. User may have further layout feedback.
</blockers>

<context>
User is iterating rapidly on UI. Core requirements:
1. Everything on one screen — zero scrolling
2. Retro pixel font everywhere
3. Ornate border not hiding content
4. Three columns if needed to fit

The three-column layout is the latest state. Files changed across multiple sessions are still uncommitted in the working tree. The previous .continue-here.md had context about the retro pixel theme overhaul — all that work is preserved in the current codebase.
</context>

<next_action>
Start with: Run `cargo run --features gui` and verify three-column layout. Check that expanding preset combos/save-as dialogs doesn't cause scrolling. Then verify WASM with `trunk build`. Address any remaining user feedback on layout density.
</next_action>

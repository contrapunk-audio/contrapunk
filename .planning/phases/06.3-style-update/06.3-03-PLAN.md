---
phase: 06.3-style-update
plan: 03
type: execute
wave: 2
depends_on: ["06.3-01", "06.3-02"]
files_modified:
  - src/app.rs
  - src/tabs/mod.rs
  - src/tabs/play.rs
  - src/tabs/craft.rs
  - src/tabs/settings.rs
  - src/lib.rs
autonomous: true

must_haves:
  truths:
    - "Play tab shows key, mode, MIDI device selection, and active preset quick-switch"
    - "Craft tab shows full parameter editor for harmony, humanization, and voice leading"
    - "Settings tab shows app configuration"
    - "Piano keyboard and active notes are always visible across all tabs"
    - "Existing SidePanel controls are migrated into tab content"
  artifacts:
    - path: "src/tabs/mod.rs"
      provides: "Tab enum and tab bar rendering function"
      min_lines: 20
    - path: "src/tabs/play.rs"
      provides: "Play tab UI with simplified controls"
      min_lines: 40
    - path: "src/tabs/craft.rs"
      provides: "Craft tab with full parameter editor and preset management"
      min_lines: 60
    - path: "src/tabs/settings.rs"
      provides: "Settings tab with app configuration"
      min_lines: 20
  key_links:
    - from: "src/app.rs"
      to: "src/tabs/mod.rs"
      via: "Tab content routing in update()"
      pattern: "match.*active_tab"
    - from: "src/tabs/play.rs"
      to: "src/preset/mod.rs"
      via: "Preset quick-switch selector"
      pattern: "preset|PresetManager"
---

<objective>
Refactor app.rs layout from SidePanel + CentralPanel into tabbed navigation with Play/Craft/Settings tabs, keeping piano always visible.

Purpose: Reorganize the UI into focused tabs per the context decisions — Play for performance, Craft for deep editing, Settings for config.
Output: Three tab content modules, refactored app.rs with tab routing, piano in BottomPanel always visible.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.3-style-update/06.3-CONTEXT.md
@.planning/phases/06.3-style-update/06.3-01-SUMMARY.md
@.planning/phases/06.3-style-update/06.3-02-SUMMARY.md
@src/app.rs
@src/piano.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create tab modules and extract UI content from app.rs</name>
  <files>src/tabs/mod.rs, src/tabs/play.rs, src/tabs/craft.rs, src/tabs/settings.rs, src/lib.rs</files>
  <action>
1. Create `src/tabs/mod.rs`:
   - Re-export Tab enum (move from app.rs if created there, or define here)
   - `pub mod play; pub mod craft; pub mod settings;`

2. Create `src/tabs/play.rs` — Play tab rendering function:
   - Takes `&mut ContrapunkApp` (or relevant state refs) and `&mut egui::Ui`
   - Shows: Key selector (ComboBox), Mode selector (ComboBox), MIDI input device selector, output port selectors, Start/Stop button
   - Shows: Active preset name + quick-switch dropdown (using PresetManager)
   - Shows: Active notes display (input + harmony note names, chord detection)
   - This is the EXISTING SidePanel content, reorganized into a cleaner layout

3. Create `src/tabs/craft.rs` — Craft tab rendering function:
   - Full harmony parameter controls (key, mode, octave mode)
   - Full humanization controls (all HumanizeConfig sliders — timing jitter, velocity variation, swing, etc.)
   - Full voice leading controls (enable/disable, style selection)
   - Preset management section: current preset name, Save As button, Create New button, preset parameter display
   - This combines ALL parameter controls in one editing workspace

4. Create `src/tabs/settings.rs` — Settings tab rendering function:
   - MIDI device configuration (input/output selection, refresh button)
   - App info (version, build type native/WASM)
   - Any future configuration options

5. Add `pub mod tabs;` to src/lib.rs (feature-gated `#[cfg(feature = "gui")]`).
  </action>
  <verify>
    `cargo check --features gui` compiles.
    All four tab files exist under src/tabs/.
  </verify>
  <done>Tab content modules created with Play (performance), Craft (editing), Settings (config) separation.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor app.rs layout to use tabs with always-visible piano</name>
  <files>src/app.rs</files>
  <action>
1. Add PresetManager field to ContrapunkApp struct. Initialize in `new()` with `PresetManager::new()`.

2. Refactor the `update()` method layout order (egui requires panels in this order):
   a. `TopBottomPanel::top("tab_bar")` — styled tab bar with Play/Craft/Settings selectable values. Use steampunk colors from theme.
   b. `TopBottomPanel::bottom("piano_panel")` — piano keyboard widget (always visible). Move piano rendering here from its current location. Include active notes display and chord name below/above piano.
   c. `CentralPanel::default()` — match on `active_tab` and call the appropriate tab render function (play::draw, craft::draw, settings::draw).

3. REMOVE the existing SidePanel. All its content is now in tab modules.

4. Pass necessary state to tab drawing functions. Options:
   - Pass `&mut ContrapunkApp` directly if tabs are methods on ContrapunkApp
   - Or pass individual state references if tabs are free functions
   - Prefer methods on ContrapunkApp for simplicity: `self.draw_play_tab(ui)`, etc., which delegate to tabs::play::draw(...)

5. When a preset is selected (in Play tab quick-switch or Craft tab), apply it:
   - Set self.state.key, self.state.mode, self.state.octave_mode from preset
   - Set voice_leading_enabled, voice_leading_style from preset
   - Set humanize_config from preset
   - Signal the router thread of config changes (same mechanism as existing key/mode changes)

6. Ensure WASM build also compiles — tab rendering is platform-independent. MIDI device sections in Play/Settings tabs should be `#[cfg(not(target_arch = "wasm32"))]` gated where they use native MIDI APIs, with WASM alternatives where applicable.
  </action>
  <verify>
    `cargo check --features gui` compiles.
    `cargo check --target wasm32-unknown-unknown --features wasm` compiles.
    app.rs no longer has SidePanel. Tab routing with match statement exists.
    Piano in BottomPanel always visible.
  </verify>
  <done>Layout fully refactored: tab bar at top, piano at bottom (always visible), tab content in center. Preset apply wired to state changes.</done>
</task>

</tasks>

<verification>
- `cargo check --features gui` and WASM check both succeed
- SidePanel removed from app.rs
- Piano keyboard renders in BottomPanel (visible on all tabs)
- Tab switching changes CentralPanel content
- Preset selection applies all bundled settings
</verification>

<success_criteria>
Complete layout refactor from SidePanel to tabbed navigation. All existing controls accessible. Piano always visible. Preset application wired to state.
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-style-update/06.3-03-SUMMARY.md`
</output>

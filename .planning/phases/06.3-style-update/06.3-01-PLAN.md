---
phase: 06.3-style-update
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/theme/mod.rs
  - src/theme/colors.rs
  - src/lib.rs
  - src/app.rs
autonomous: true

must_haves:
  truths:
    - "App applies a dark steampunk color scheme on startup"
    - "Tab bar renders with Play, Craft, Settings tabs"
    - "Tab switching changes central panel content"
  artifacts:
    - path: "src/theme/mod.rs"
      provides: "ContrapunkTheme struct with apply_theme() function"
      min_lines: 30
    - path: "src/theme/colors.rs"
      provides: "Named color constants (gold, copper, amber, dark backgrounds)"
      min_lines: 20
  key_links:
    - from: "src/app.rs"
      to: "src/theme/mod.rs"
      via: "apply_theme() call in update()"
      pattern: "apply_theme|set_visuals"
---

<objective>
Set up the steampunk theme system, add serde dependencies, and implement tabbed navigation skeleton.

Purpose: Foundation for all visual changes — theme colors applied globally, tab enum routing ready for content migration.
Output: Theme module with color palette, ContrapunkTheme applied at startup, Tab enum with Play/Craft/Settings, serde + serde_json in Cargo.toml.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.3-style-update/06.3-CONTEXT.md
@.planning/phases/06.3-style-update/06.3-RESEARCH.md
@src/app.rs
@src/lib.rs
@Cargo.toml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add serde dependencies and create theme module with color palette</name>
  <files>Cargo.toml, src/theme/mod.rs, src/theme/colors.rs, src/lib.rs</files>
  <action>
1. Add to Cargo.toml [dependencies]:
   - `serde = { version = "1.0", features = ["derive"] }`
   - `serde_json = "1.0"`
   - Add `"persistence"` to the eframe features: `eframe = { version = "0.33", optional = true, features = ["persistence"] }`

2. Create `src/theme/colors.rs` with named Color32 constants for the steampunk palette:
   - Background colors: DARK_BG (15,12,8), PANEL_BG (20,16,12), WIDGET_BG (25,20,15), WIDGET_INACTIVE (45,35,25)
   - Accent colors: GOLD (255,215,140), COPPER (180,120,50), AMBER (220,170,60), COPPER_HOVER (120,85,40)
   - Border colors: BORDER (160,110,40), CORNER_ACCENT (200,150,60)
   - Text colors: TEXT_PRIMARY (255,215,140), TEXT_SECONDARY (180,160,130), TEXT_DIM (120,100,75)
   - Special: NOTE_ACTIVE (100,220,100), HARMONY_NOTE (100,150,255)

3. Create `src/theme/mod.rs`:
   - `pub mod colors;`
   - `pub struct ContrapunkTheme;`
   - `impl ContrapunkTheme { pub fn apply(ctx: &egui::Context) }` — constructs `Visuals::dark()`, overrides all widget states (noninteractive, inactive, hovered, active), panel_fill, window_fill, extreme_bg_color, override_text_color, selection, window_rounding using colors from colors.rs.

4. Add `pub mod theme;` to src/lib.rs (feature-gated with `#[cfg(feature = "gui")]` like existing GUI modules).
  </action>
  <verify>
    `cargo check --features gui` compiles without errors.
    `src/theme/mod.rs` and `src/theme/colors.rs` exist.
  </verify>
  <done>Theme module exists with color palette and apply() function. serde + serde_json in Cargo.toml. eframe has persistence feature.</done>
</task>

<task type="auto">
  <name>Task 2: Add Tab enum and integrate theme + tab bar into app.rs</name>
  <files>src/app.rs</files>
  <action>
1. Add a `Tab` enum to app.rs (or in theme/mod.rs and import):
   ```rust
   #[derive(Clone, Copy, PartialEq)]
   enum Tab { Play, Craft, Settings }
   ```

2. Add `active_tab: Tab` field to `ContrapunkApp` struct, default to `Tab::Play`.

3. In the `update()` method of `ContrapunkApp`:
   - Call `ContrapunkTheme::apply(ctx)` ONCE at startup (use a `theme_applied: bool` field, set to true after first apply). Do NOT call every frame.
   - Add a `TopBottomPanel::top("tab_bar")` BEFORE the existing SidePanel. Render three `ui.selectable_value()` tabs horizontally. Style the tab bar with the steampunk colors.
   - Keep the existing SidePanel and CentralPanel content as-is for now (layout refactor is Plan 03). The tab bar is visible but doesn't change content yet — just switches the `active_tab` value.

4. Ensure both native and WASM builds compile. The Tab enum and theme apply are not platform-specific.
  </action>
  <verify>
    `cargo check --features gui` compiles.
    `cargo check --target wasm32-unknown-unknown --features wasm` compiles (if wasm target installed).
    Tab bar renders at top of window (visual check deferred to Plan 07).
  </verify>
  <done>Tab bar with Play/Craft/Settings renders at top. Theme colors applied on startup. active_tab state tracked.</done>
</task>

</tasks>

<verification>
- `cargo check --features gui` succeeds
- `src/theme/mod.rs` contains `apply` function using steampunk colors
- `src/theme/colors.rs` has at least 10 named color constants
- `src/app.rs` contains Tab enum and tab bar rendering
- Cargo.toml has serde, serde_json, and eframe persistence feature
</verification>

<success_criteria>
Dark steampunk theme system is created and applied. Tab navigation skeleton exists. serde dependencies added for preset serialization (Plan 02). No visual breakage in existing layout.
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-style-update/06.3-01-SUMMARY.md`
</output>

---
phase: 06.3-style-update
plan: 05
type: execute
wave: 3
depends_on: ["06.3-02", "06.3-03", "06.3-04"]
files_modified:
  - src/tabs/craft.rs
  - src/tabs/play.rs
  - src/app.rs
autonomous: true

must_haves:
  truths:
    - "User can select a preset from the Play tab and it immediately applies all settings"
    - "Craft tab shows preset editor with Save As and Create New options"
    - "Custom presets persist across app restarts (native and WASM)"
    - "Built-in presets cannot be modified or deleted"
    - "Custom widgets (ornate sliders/toggles) used in Craft tab controls"
  artifacts:
    - path: "src/tabs/craft.rs"
      provides: "Preset management UI with save/create/delete"
      contains: "save_custom_presets"
    - path: "src/app.rs"
      provides: "eframe save() and Storage loading for presets"
      contains: "fn save"
  key_links:
    - from: "src/app.rs"
      to: "src/preset/storage.rs"
      via: "save() and new() for persistence"
      pattern: "save_custom_presets|load_custom_presets"
    - from: "src/tabs/craft.rs"
      to: "src/theme/widgets.rs"
      via: "ornate_slider, ornate_toggle usage"
      pattern: "ornate_slider|ornate_toggle"
---

<objective>
Wire preset persistence (save/load), integrate custom widgets into tab UIs, and build the preset management UI in the Craft tab.

Purpose: Complete the preset user experience — select presets, tweak parameters with themed controls, save custom presets, persist across restarts.
Output: Working preset selection, custom preset save/load, ornate widgets in use, eframe persistence wired.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06.3-style-update/06.3-02-SUMMARY.md
@.planning/phases/06.3-style-update/06.3-03-SUMMARY.md
@.planning/phases/06.3-style-update/06.3-04-SUMMARY.md
@src/app.rs
@src/tabs/craft.rs
@src/tabs/play.rs
@src/preset/mod.rs
@src/preset/storage.rs
@src/theme/widgets.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wire eframe persistence and preset apply/save logic</name>
  <files>src/app.rs</files>
  <action>
1. Implement `fn save(&mut self, storage: &mut dyn eframe::Storage)` on ContrapunkApp:
   - Call `storage::save_custom_presets(storage, self.preset_manager.custom_presets())`

2. In `ContrapunkApp::new()` (or wherever the app is constructed from CreationContext):
   - If `cc.storage` is Some, call `storage::load_custom_presets(storage)` and pass to PresetManager via `set_custom_presets()`

3. Add a method `apply_preset(&mut self, preset: &StylePreset)`:
   - Sets self.state.key = preset.key
   - Sets self.state.mode = preset.harmony_mode
   - Sets self.state.octave_mode = preset.octave_mode
   - Sets voice_leading_enabled and voice_leading_style from preset
   - Sets humanize_config from preset
   - Signals router thread of changes (reuse existing config-change mechanism)

4. Ensure `eframe::NativeOptions` or `eframe::WebOptions` enables persistence (the feature flag was added in Plan 01).
  </action>
  <verify>
    `cargo check --features gui` compiles.
    app.rs has `fn save()` implementation.
    app.rs has `apply_preset()` method.
  </verify>
  <done>Preset persistence wired via eframe Storage. apply_preset changes all app state from a preset. Custom presets load on startup.</done>
</task>

<task type="auto">
  <name>Task 2: Build preset management UI in Craft tab and integrate custom widgets</name>
  <files>src/tabs/craft.rs, src/tabs/play.rs</files>
  <action>
1. In `src/tabs/play.rs`:
   - Add preset quick-switch: ComboBox showing all presets (built-in + custom) with persona name + genre subtitle
   - On selection change, call apply_preset()
   - Show active preset character persona name prominently

2. In `src/tabs/craft.rs`:
   - Replace default egui sliders with `ornate_slider()` for humanization controls (timing jitter, velocity variation, swing amount, etc.)
   - Replace default egui checkboxes with `ornate_toggle()` for boolean controls (voice leading enabled, humanization enabled, etc.)
   - Use `section_header()` to separate sections: "Harmony", "Voice Leading", "Humanization", "Octave"

   Preset management section in Craft tab:
   - "Current Preset" display showing active preset name + persona
   - "Save As..." button: opens inline text input for name + persona, saves current settings as new custom preset via PresetManager::add_custom()
   - "Create New" button: creates preset from current settings with default name
   - For custom presets: "Delete" button (only for custom, not built-in)
   - "Export JSON" button: calls export_preset_json(), shows in a text area for copy
   - "Import JSON" button: text area input, parses and adds as custom preset

3. All preset mutations go through PresetManager methods. Built-in presets are never modified.
  </action>
  <verify>
    `cargo check --features gui` compiles.
    Craft tab has ornate_slider and ornate_toggle calls.
    Craft tab has Save As, Create New, Delete, Export, Import functionality.
    Play tab has preset quick-switch ComboBox.
  </verify>
  <done>Preset UI complete — quick-switch in Play tab, full editor + save/create/delete/export/import in Craft tab. Custom widgets (ornate slider/toggle) used for themed controls.</done>
</task>

</tasks>

<verification>
- `cargo check --features gui` and WASM check both succeed
- Presets persist: app saves on exit, loads on start
- Built-in presets always present, cannot be deleted
- Custom presets can be created, saved, deleted, exported, imported
- Ornate widgets render in Craft tab
</verification>

<success_criteria>
Full preset user experience working: select, apply, customize, save, persist, export/import. Themed custom widgets integrated into parameter controls.
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-style-update/06.3-05-SUMMARY.md`
</output>

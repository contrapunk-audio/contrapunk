---
phase: 06.3-style-update
plan: 02
subsystem: preset
tags: [preset, serde, style, persona]
depends_on: []
provides: [StylePreset, PresetManager, builtins, storage]
affects: [06.3-03, 06.3-04, 06.3-05]
tech-stack:
  added: [serde, serde_json]
  patterns: [preset-manager, json-storage]
key-files:
  created:
    - src/preset/mod.rs
    - src/preset/builtins.rs
    - src/preset/storage.rs
  modified:
    - src/harmony/config.rs
    - src/harmony/voice_leading/styles.rs
    - src/humanize/config.rs
    - src/main.rs
    - src/theme/mod.rs
    - Cargo.toml
decisions:
  - "serde rename_all snake_case for clean JSON enum serialization"
  - "HumanizeConfig metronome_output_port serialized as Option (nullable JSON)"
  - "is_builtin field skipped in serde (runtime-only marker)"
metrics:
  duration: "4 min"
  completed: "2026-01-30"
---

# Phase 6.3 Plan 02: Preset Data Model and Built-in Library Summary

**One-liner:** StylePreset struct bundling harmony/voice-leading/humanize settings with 11 character-persona built-in presets and JSON storage

## What Was Built

### Task 1: StylePreset struct and serde derives
- Added `Serialize, Deserialize` derives to `Key`, `HarmonyMode`, `OctaveMode`, `VoiceLeadingStyle`, and `HumanizeConfig`
- Created `src/preset/mod.rs` with `StylePreset` struct (name, persona, genre, harmony_mode, key, voice_leading, octave_mode, humanize_config)
- Created `PresetManager` with builtin/custom separation, active selection, add/remove custom

### Task 2: Built-in presets and storage
- 11 built-in presets with character personas: The Alchemist (Jazz), The Architect (Classical), The Bard (Pop), The Clockmaker (Baroque), The Monk (Minimal), The Phantom (Lo-fi), The Seer (Ambient), The Preacher (Gospel), The Forge (Metal), The Voyager (Prog), The Spirit (Soul)
- Storage module: save/load custom presets via eframe Storage, export/import as JSON

## Commits

| Hash | Description |
|------|-------------|
| 490163e | feat(06.3-02): add StylePreset struct and serde derives to existing types |
| 6a6f740 | feat(06.3-02): create 11 built-in presets with character personas and storage module |

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 3 - Blocking] Fixed theme corner_radius field name for egui 0.33**
- **Found during:** Task 1 (cargo check)
- **Issue:** `src/theme/mod.rs` (from parallel 06.3-01 plan) used `rounding` field which is now `corner_radius` in egui 0.33, and `window_rounding` -> `window_corner_radius`
- **Fix:** Replaced all `.rounding` with `.corner_radius` and `window_rounding` with `window_corner_radius`
- **Files modified:** src/theme/mod.rs

## Verification

- `cargo check --features gui` succeeds
- `cargo check --target wasm32-unknown-unknown --features wasm` succeeds
- All existing enums have serde derives
- `builtins::all()` returns 11 presets
- PresetManager separates built-in and custom presets

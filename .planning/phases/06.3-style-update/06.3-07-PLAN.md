---
phase: 06.3-style-update
plan: 07
type: execute
wave: 4
depends_on: ["06.3-05", "06.3-06"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "GUI has steampunk dark theme with gold/copper/amber colors"
    - "Tabbed navigation works (Play/Craft/Settings)"
    - "Piano keyboard visible on all tabs"
    - "Presets selectable and apply distinct musical settings"
    - "Custom presets can be saved and persist across restart"
    - "Animations visible (gears, frame, music-reactive)"
    - "Both native and WASM builds work"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete Style Update phase.

Purpose: Verify that the steampunk theme, tabbed layout, preset system, custom widgets, and animations all work correctly together in both native and WASM builds.
Output: Approval or issues list for gap closure.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Style Update: steampunk theme, tabbed navigation, musical style presets, custom widgets, animations, preset persistence</what-built>
  <how-to-verify>
1. Run native: `cargo run --features gui`
   - Verify dark steampunk theme (gold/copper/amber colors, dark backgrounds)
   - Verify tab bar at top with Play/Craft/Settings tabs
   - Verify piano keyboard visible at bottom on all tabs
   - Switch to Play tab: select different presets, verify settings change (key, mode should update)
   - Switch to Craft tab: verify ornate sliders for humanization, voice leading controls, preset save/create/delete
   - Switch to Settings tab: verify MIDI device configuration
   - Verify animated gears in background, decorative frame around window
   - Play some MIDI notes: verify music-reactive animations respond
   - Create a custom preset: Save As with custom name, restart app, verify it persists
   - Delete a custom preset, verify built-in presets cannot be deleted

2. Run WASM (if available): `trunk serve` or visit deployment
   - Verify same theme and layout
   - Verify preset selection works
   - Verify animations render

3. Check CPU: with no MIDI input, CPU should not spike (30fps idle animation, not 60fps)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Human approval that all Style Update features work correctly.
</verification>

<success_criteria>
All phase success criteria verified by human:
1. GUI has steampunk dark theme
2. Dark-only theme (no light toggle — per user decision)
3. Musical style presets selectable (11+ built-in)
4. Each preset configures harmony, voice leading, humanization, octave as bundle
5. Users can customize and save own presets
6. Style presets provide audibly distinct character
</success_criteria>

<output>
After completion, create `.planning/phases/06.3-style-update/06.3-07-SUMMARY.md`
</output>

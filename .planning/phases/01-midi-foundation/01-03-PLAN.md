---
phase: 01-midi-foundation
plan: 03
type: execute
wave: 3
depends_on: ["01-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User confirms MIDI pass-through works with real hardware"
    - "Notes played on input device are heard on output device"
    - "Application remains stable during continuous playing"
  artifacts: []
  key_links: []
---

<objective>
Verify MIDI pass-through works end-to-end with real hardware.

Purpose: Confirm the MIDI foundation is solid before building harmony features on top.
Output: User-verified working MIDI pass-through.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-midi-foundation/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete MIDI pass-through system:
- Port enumeration and selection via CLI
- MIDI input connection with callback
- Multiple output connections via OutputRouter
- Message forwarding from input to first output
  </what-built>
  <how-to-verify>
1. Connect a MIDI input device (keyboard, controller, etc.)
2. Connect a MIDI output device or virtual instrument
3. Run `cargo run` from project directory
4. Select your input device from the list
5. Select at least 2 output devices (your instrument + another)
6. Play notes on the input device
7. Verify:
   - Debug output shows received MIDI messages
   - Notes are heard on the first output device
   - Application remains responsive
   - No crashes or stuck notes

If no hardware available:
- macOS: Use "IAC Driver" virtual MIDI ports (enable in Audio MIDI Setup)
- Test with a DAW or MIDI monitor app to verify messages route correctly
  </how-to-verify>
  <resume-signal>Type "approved" if MIDI pass-through works, or describe any issues encountered</resume-signal>
</task>

</tasks>

<verification>
User confirms:
1. MIDI input device detected and selectable
2. MIDI output devices detected and selectable
3. Notes pass through from input to output
4. Application stable during use
</verification>

<success_criteria>
- User has verified MIDI pass-through with real or virtual devices
- Phase 1 success criteria met:
  1. User can select a MIDI input device from a list of available ports
  2. User can select 2-8 MIDI output ports from available ports
  3. User can play a note on input device and hear it on the first output port
  4. Application runs without GUI (CLI mode) for testing MIDI flow
</success_criteria>

<output>
After completion, create `.planning/phases/01-midi-foundation/01-03-SUMMARY.md`
</output>

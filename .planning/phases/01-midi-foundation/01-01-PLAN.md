---
phase: 01-midi-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - src/main.rs
  - src/midi/mod.rs
  - src/midi/ports.rs
autonomous: true

must_haves:
  truths:
    - "Running `cargo build` compiles without errors"
    - "Running the binary lists available MIDI input ports"
    - "Running the binary lists available MIDI output ports"
    - "User can select input port by number"
    - "User can select multiple output ports by number"
  artifacts:
    - path: "Cargo.toml"
      provides: "Project configuration with midir dependency"
      contains: "midir"
    - path: "src/main.rs"
      provides: "Entry point with CLI port selection"
      min_lines: 30
    - path: "src/midi/mod.rs"
      provides: "MIDI module re-exports"
      contains: "pub mod ports"
    - path: "src/midi/ports.rs"
      provides: "Port enumeration and selection functions"
      exports: ["list_input_ports", "list_output_ports", "select_input_port", "select_output_ports"]
  key_links:
    - from: "src/main.rs"
      to: "src/midi/ports.rs"
      via: "use midi::ports"
      pattern: "use.*midi::ports"
    - from: "src/midi/ports.rs"
      to: "midir"
      via: "midir::MidiInput and MidiOutput"
      pattern: "midir::(MidiInput|MidiOutput)"
---

<objective>
Set up Rust project structure and implement MIDI port enumeration with CLI selection.

Purpose: Establish the foundation for MIDI I/O - users must be able to see available devices before connecting to them.
Output: Compiling Rust project that lists MIDI ports and accepts user selection.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-midi-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Rust project with dependencies</name>
  <files>Cargo.toml, src/main.rs, src/midi/mod.rs</files>
  <action>
Create Cargo.toml with:
- Package name: contrapunk
- Edition: 2021
- Dependencies: midir = "0.10", anyhow = "1.0"

Create src/main.rs with:
- Basic main function that prints "Contrapunk MIDI Router"
- Placeholder for port selection flow
- Use anyhow::Result for error handling

Create src/midi/mod.rs with:
- `pub mod ports;` re-export

Verify the project compiles with `cargo build`.
  </action>
  <verify>`cargo build` succeeds with no errors</verify>
  <done>Project compiles and prints startup message when run</done>
</task>

<task type="auto">
  <name>Task 2: Implement port enumeration and selection</name>
  <files>src/midi/ports.rs, src/main.rs</files>
  <action>
Create src/midi/ports.rs with:

1. `list_input_ports()` function:
   - Create MidiInput with client name "contrapunk-in"
   - Get ports via `ports()` method
   - Return Vec of (index, port_name) tuples
   - Handle error if no ports available

2. `list_output_ports()` function:
   - Create MidiOutput with client name "contrapunk-out"
   - Get ports via `ports()` method
   - Return Vec of (index, port_name) tuples
   - Handle error if no ports available

3. `select_input_port()` function:
   - Display numbered list of input ports
   - Prompt user for selection via stdin
   - Validate selection is in range
   - Return selected port index

4. `select_output_ports(min: usize, max: usize)` function:
   - Display numbered list of output ports
   - Prompt user to select multiple ports (comma-separated)
   - Validate selection count is within min/max (2-8 for this phase)
   - Return Vec of selected port indices

Update src/main.rs to:
- Call list_input_ports() and display
- Call select_input_port() and store selection
- Call list_output_ports() and display
- Call select_output_ports(2, 8) and store selections
- Print confirmation of selections

Use the patterns from 01-RESEARCH.md - specifically the port selection with validation pattern.
  </action>
  <verify>`cargo run` lists ports, accepts input/output selections, prints confirmation</verify>
  <done>User can see MIDI ports, select 1 input and 2-8 outputs, selections are confirmed</done>
</task>

</tasks>

<verification>
1. `cargo build` compiles without warnings
2. `cargo run` shows "Contrapunk MIDI Router" header
3. Running binary lists available MIDI input ports (may be empty if no devices)
4. Running binary lists available MIDI output ports
5. User can type a number to select input port
6. User can type comma-separated numbers to select output ports
7. Program confirms selections before continuing
</verification>

<success_criteria>
- Cargo.toml exists with midir and anyhow dependencies
- `cargo build` succeeds
- `cargo run` displays port lists and accepts selections
- Port selection validates input (rejects invalid numbers)
- Multiple output port selection works (2-8 ports)
- MIDI-01 partially satisfied (selection mechanism exists)
- MIDI-02 partially satisfied (multi-output selection exists)
</success_criteria>

<output>
After completion, create `.planning/phases/01-midi-foundation/01-01-SUMMARY.md`
</output>

---
phase: 01-midi-foundation
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/midi/input.rs
  - src/midi/output.rs
  - src/midi/mod.rs
  - src/router.rs
  - src/main.rs
autonomous: true

must_haves:
  truths:
    - "MIDI input callback receives messages from connected device"
    - "Messages forward from input to first output port"
    - "Application runs continuously until user presses Enter"
    - "Connection errors are handled gracefully"
  artifacts:
    - path: "src/midi/input.rs"
      provides: "MIDI input connection with callback"
      exports: ["connect_input"]
      min_lines: 30
    - path: "src/midi/output.rs"
      provides: "Multiple MIDI output connections"
      exports: ["OutputRouter"]
      min_lines: 40
    - path: "src/router.rs"
      provides: "Message routing from input to outputs"
      exports: ["run_router"]
      min_lines: 30
  key_links:
    - from: "src/midi/input.rs"
      to: "std::sync::mpsc"
      via: "channel sender in callback"
      pattern: "mpsc::Sender"
    - from: "src/router.rs"
      to: "src/midi/output.rs"
      via: "OutputRouter::send_to_first"
      pattern: "send_to_first"
    - from: "src/main.rs"
      to: "src/router.rs"
      via: "run_router call"
      pattern: "router::run_router"
---

<objective>
Implement MIDI input/output connections and pass-through routing.

Purpose: Complete the MIDI data flow - notes played on input device should appear on output port.
Output: Working MIDI pass-through that forwards all messages from input to first output.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-midi-foundation/01-RESEARCH.md
@.planning/phases/01-midi-foundation/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement MIDI input and output connections</name>
  <files>src/midi/input.rs, src/midi/output.rs, src/midi/mod.rs</files>
  <action>
Create src/midi/input.rs with:

1. `connect_input(port_index: usize, tx: mpsc::Sender<Vec<u8>>)` function:
   - Create MidiInput with client name "contrapunk-in"
   - Set ignore to Ignore::None (receive all message types)
   - Get port at specified index
   - Connect with callback that:
     - Clones message bytes to Vec (message is borrowed slice)
     - Sends via channel: `tx.send(message.to_vec())`
     - Prints timestamp and message for debugging
   - Return MidiInputConnection (must stay alive)
   - Use `move` closure to capture tx

2. Handle the lifetime correctly - the MidiInputConnection must be stored and kept alive.

Create src/midi/output.rs with:

1. `OutputRouter` struct:
   - `connections: Vec<MidiOutputConnection>`
   - Track which ports are connected

2. `OutputRouter::new(port_indices: &[usize])` constructor:
   - For each port index, create MidiOutput and connect
   - Use unique client names per port: "contrapunk-out-{idx}"
   - Store all connections
   - Return error if any connection fails

3. `OutputRouter::send_to_first(&mut self, message: &[u8])` method:
   - Send to connections[0] if exists
   - Return error if no connections or send fails

4. `OutputRouter::send_to_all(&mut self, message: &[u8])` method:
   - Send to all connections
   - Collect and return any errors

Update src/midi/mod.rs:
- Add `pub mod input;`
- Add `pub mod output;`

Use patterns from 01-RESEARCH.md - specifically Pattern 1 (callback-to-channel) and Pattern 3 (multiple outputs).
  </action>
  <verify>`cargo build` succeeds; input and output modules compile without errors</verify>
  <done>MIDI input connection with forwarding callback and OutputRouter struct exist</done>
</task>

<task type="auto">
  <name>Task 2: Implement router and integrate into main</name>
  <files>src/router.rs, src/main.rs</files>
  <action>
Create src/router.rs with:

1. `run_router(input_port: usize, output_ports: &[usize])` function:
   - Create mpsc channel for message forwarding
   - Connect input using midi::input::connect_input
   - Create OutputRouter with output ports
   - Print "MIDI routing active. Press Enter to stop."
   - Main loop:
     - Receive message from channel
     - Forward to OutputRouter::send_to_first
     - Print message info for debugging
   - Use select! or try_recv with stdin check for graceful exit
   - Keep MidiInputConnection alive (store in variable with underscore prefix: _conn_in)

2. Handle graceful shutdown:
   - Check for Enter key press using separate thread or non-blocking stdin
   - Or simpler: just use blocking recv() and Ctrl+C to exit

Update src/main.rs to:
- After port selection, call router::run_router(input_idx, &output_indices)
- Handle any errors with proper messages
- Ensure clean exit message

IMPORTANT: The MidiInputConnection returned from connect_input MUST be stored in run_router and kept alive for the duration. If dropped, MIDI stops working. Use `let _conn_in = connect_input(...)` pattern.
  </action>
  <verify>`cargo run` starts routing, shows debug output when MIDI notes received (if device available)</verify>
  <done>Complete MIDI pass-through works - notes from input appear on first output</done>
</task>

</tasks>

<verification>
1. `cargo build` compiles all modules without errors
2. `cargo run` completes port selection then enters routing mode
3. "MIDI routing active" message appears
4. If MIDI device available: playing notes shows debug output
5. If MIDI device available: notes appear on output device
6. Program handles missing devices gracefully (error message, not crash)
7. Program can be stopped with Enter key or Ctrl+C
</verification>

<success_criteria>
- All MIDI modules compile and link
- Router loop runs and processes messages
- Messages forward from input callback to output connection
- Connection lifetime handled correctly (no dropped connections)
- Error handling covers: no ports available, invalid port index, connection failure
- MIDI-01 fully satisfied (input device selection and connection)
- MIDI-02 fully satisfied (multi-output port selection and connection)
- MIDI-03 fully satisfied (pass-through to first output)
- MIDI-04 infrastructure ready (OutputRouter.send_to_all exists for future use)
</success_criteria>

<output>
After completion, create `.planning/phases/01-midi-foundation/01-02-SUMMARY.md`
</output>

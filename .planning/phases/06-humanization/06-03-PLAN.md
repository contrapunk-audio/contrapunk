---
phase: 06-humanization
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - src/app.rs
autonomous: true

must_haves:
  truths:
    - "User can toggle humanization on/off via GUI"
    - "User can adjust timing jitter range with a slider"
    - "User can adjust velocity variation with a slider"
    - "User can adjust swing amount with a slider"
    - "User can adjust BPM with a slider"
    - "User can toggle metronome click on/off"
    - "GUI sliders write to shared HumanizeConfig which router reads"
  artifacts:
    - path: "src/app.rs"
      provides: "Humanization control panel in GUI"
      contains: "humaniz"
  key_links:
    - from: "src/app.rs"
      to: "src/router.rs"
      via: "GUI writes HumanizeConfig to GUIRouterState, router reads it each loop"
---

<objective>
Add GUI controls for all humanization parameters so users can enable, disable, and tune jitter, velocity variation, swing, BPM, and metronome from the interface.

Purpose: Makes humanization user-controllable. Without GUI sliders, humanization is always at defaults (disabled).
Output: Humanization section in the GUI with sliders and toggles.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-humanization/06-RESEARCH.md
@.planning/phases/06-humanization/06-02-SUMMARY.md
@src/app.rs
@src/humanize/config.rs
@src/router.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add humanization controls to GUI</name>
  <files>src/app.rs</files>
  <action>
**Add to ContrapunkApp struct:**
- `humanize_config: HumanizeConfig` — local copy of humanization config for GUI editing

**In the GUI `update()` method, add a "Humanization" section** (collapsing header or separator section) below the existing controls:

1. **Master toggle:** `ui.checkbox(&mut self.humanize_config.enabled, "Enable Humanization");`

2. **Show sliders only when enabled** (use `if self.humanize_config.enabled { ... }`):

3. **Beat Clock section:**
   - BPM slider: `ui.add(egui::Slider::new(&mut self.humanize_config.bpm, 40.0..=240.0).text("BPM"));`
   - Time signature display: show `beats_per_bar/beat_unit` (read-only for now, or simple dropdown for common values: 4/4, 3/4, 6/8)
   - Metronome toggle: `ui.checkbox(&mut self.humanize_config.metronome_enabled, "Metronome Click");`

4. **Timing Jitter section:**
   - Enable toggle: `ui.checkbox(&mut self.humanize_config.jitter_enabled, "Timing Jitter");`
   - Min/Max sliders (only if jitter_enabled):
     - `ui.add(egui::Slider::new(&mut self.humanize_config.jitter_min_ms, 0..=50).text("Min ms"));`
     - `ui.add(egui::Slider::new(&mut self.humanize_config.jitter_max_ms, 0..=50).text("Max ms"));`
   - Ensure min <= max (clamp min to max if needed)

5. **Velocity Variation section:**
   - Enable toggle: `ui.checkbox(&mut self.humanize_config.velocity_enabled, "Velocity Variation");`
   - Range slider: `ui.add(egui::Slider::new(&mut self.humanize_config.velocity_variation, 0..=30).text("±"));`

6. **Duration Variation section:**
   - Enable toggle: `ui.checkbox(&mut self.humanize_config.duration_enabled, "Duration Variation");`
   - Range slider: `ui.add(egui::Slider::new(&mut self.humanize_config.duration_variation_ms, 0..=100).text("ms"));`

7. **Swing section:**
   - Enable toggle: `ui.checkbox(&mut self.humanize_config.swing_enabled, "Swing");`
   - Amount slider: `ui.add(egui::Slider::new(&mut self.humanize_config.swing_amount, 0.0..=1.0).text("Amount"));`

**Sync config to router shared state:**
- After drawing the UI, if `is_running` and shared state exists, write `self.humanize_config.clone()` to `state.lock().unwrap().humanize_config`.
- This happens each frame, which is fine — it's just copying a small struct.

**WASM mode:**
- For WASM (`#[cfg(target_arch = "wasm32")]`), the humanize config is stored on `ContrapunkApp` directly. The WASM frame-based loop in app.rs should apply humanization similarly to the native router. If WASM integration is complex, add the GUI controls but skip WASM humanization wiring (document as known limitation). The GUI sliders should still render on WASM.

**Initialize:**
- In `ContrapunkApp::new()` or `Default`, initialize `humanize_config: HumanizeConfig::default()`.
  </action>
  <verify>Run `cargo check --features gui`. Must compile. Run `cargo run --features gui` briefly to confirm the humanization section appears in the GUI (visual check).</verify>
  <done>GUI shows humanization controls: master toggle, BPM slider, metronome toggle, jitter sliders, velocity slider, duration slider, swing slider. Config syncs to router shared state each frame.</done>
</task>

<task type="auto">
  <name>Task 2: WASM time compatibility and build verification</name>
  <files>src/humanize/beat_clock.rs</files>
  <action>
**Verify WASM compatibility:**
- BeatClock already uses `f64` milliseconds (not `std::time::Instant`), so it should work on WASM.
- In `src/app.rs` WASM path, if a frame-based humanization loop exists, call `humanizer.tick(now_ms)` where `now_ms` comes from `web_sys::window().unwrap().performance().unwrap().now()` (already returns f64 ms).
- If the WASM path doesn't have a humanization integration point yet, add a TODO comment noting that WASM humanization is a known limitation — the GUI controls render but humanization effects require native router integration.

**Build verification:**
- Run `cargo check --target wasm32-unknown-unknown` to verify humanize module compiles for WASM (no Instant usage, no thread-only APIs).
- Fix any WASM compilation issues (e.g., if `rand::thread_rng()` doesn't work on WASM, gate it with `#[cfg(not(target_arch = "wasm32"))]` and provide a WASM-safe alternative using `rand::rngs::OsRng` or `js_sys::Math::random()`).

**Native build verification:**
- Run `cargo build --features gui` to confirm full native build succeeds.
  </action>
  <verify>Run `cargo check --target wasm32-unknown-unknown` and `cargo build --features gui`. Both must succeed.</verify>
  <done>Humanize module compiles on both native and WASM targets. BeatClock uses f64 ms timestamps. Any WASM-incompatible APIs are gated with cfg.</done>
</task>

</tasks>

<verification>
1. `cargo build --features gui` succeeds
2. `cargo check --target wasm32-unknown-unknown` succeeds (or has only non-blocking warnings)
3. GUI shows humanization section with all sliders and toggles
4. HumanizeConfig syncs from GUI to router shared state
5. All six humanization parameters are adjustable: jitter, velocity, duration, swing, BPM, metronome
</verification>

<success_criteria>
- User can see and interact with humanization controls in the GUI
- All HUM-01 through HUM-06 parameters have corresponding GUI controls
- Config flows from GUI to router via shared state
- WASM compilation is not broken by humanize module
- Native build works with full humanization pipeline
</success_criteria>

<output>
After completion, create `.planning/phases/06-humanization/06-03-SUMMARY.md`
</output>

---
phase: 04-server-mode
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "Server starts and listens on configured port"
    - "Client connects and sends MIDI to server"
    - "Server returns harmonized MIDI to client"
    - "Client routes harmonized output to local MIDI devices"
    - "Multiple clients can connect simultaneously"
  artifacts: []
  key_links: []
---

<objective>
End-to-end verification of server mode with real MIDI hardware.

Purpose: Confirms the full server/client flow works with actual MIDI devices - not just compilation.
Output: Verified working server mode.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-server-mode/04-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build and smoke test</name>
  <files></files>
  <action>
1. Run `cargo build` (CLI mode, no GUI feature) - must succeed
2. Run `cargo build --features gui` - must succeed
3. Start server in background: `cargo run -- --server --port 9900`
4. Verify server prints listening message
5. In a separate process, attempt TCP connection to 127.0.0.1:9900 (can use a simple test with netcat or a quick Rust test)
6. Verify server logs the connection
7. Kill server process
  </action>
  <verify>
Both builds succeed. Server accepts TCP connection on port 9900.
  </verify>
  <done>
Server binary builds and accepts connections. Smoke test passes.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete server/client mode for network MIDI harmony processing</what-built>
  <how-to-verify>
1. Open terminal 1: `cargo run -- --server --port 9900`
   - Should print "Contrapunk server listening on port 9900"
2. Open terminal 2: `cargo run -- --client 127.0.0.1:9900`
   - Select MIDI input device (e.g., Akai MPK Mini)
   - Select MIDI output ports (e.g., IAC Driver buses)
   - Select key and mode
3. Play notes on MIDI input device
   - Should hear harmonized output on the output ports
   - Server terminal should show client connection log
4. Change configuration during play (if supported)
5. Disconnect client (press Enter) - verify clean shutdown, no stuck notes
6. Optionally: connect a second client to verify concurrent sessions
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- Server starts and accepts connections
- Client connects, sends MIDI, receives harmonized output
- MIDI output is routed to local devices correctly
- Clean disconnect with no stuck notes
</verification>

<success_criteria>
Full round-trip: local MIDI input -> network -> server harmony processing -> network -> local MIDI output. Multiple concurrent clients supported.
</success_criteria>

<output>
After completion, create `.planning/phases/04-server-mode/04-04-SUMMARY.md`
</output>

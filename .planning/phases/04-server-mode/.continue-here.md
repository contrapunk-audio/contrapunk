---
phase: 04-server-mode
task: verification-debugging
total_tasks: 4 plans (all complete per metadata, but output routing unverified)
status: debugging
last_updated: 2026-01-29
---

<current_state>
Phase 4 execution is complete (all 4 plans committed). However, during hardware
verification the user reported they can only see [IN] messages from the client —
no harmonized output is arriving at MIDI output ports.

Debug logging has been added to src/main.rs (client) and src/server/session.rs
(server) but has NOT been tested yet. These changes are uncommitted.

The phase metadata (SUMMARY, STATE, ROADMAP) has been committed as complete,
but the actual end-to-end flow needs debugging.
</current_state>

<completed_work>
- Plan 04-01: Wire protocol and server config types (committed)
- Plan 04-02: Session handler and server accept loop (committed)
- Plan 04-03: CLI integration with clap, --server/--client/--port flags (committed)
- Plan 04-04: Smoke test passed, timeout bug fixed (committed as b6f27ec)
- Phase metadata committed as complete (de5d33a)
</completed_work>

<remaining_work>
- Debug why client receives no harmonized MIDI output from server
- Debug logging added to session.rs and main.rs (uncommitted) — needs testing
- Once bug is found and fixed, commit the fix
- Re-verify with hardware: server -> client -> MIDI output working
</remaining_work>

<decisions_made>
- Read timeouts (WouldBlock/TimedOut) treated as non-fatal in both server and client loops
- clap Args struct unconditional; only run_client is cfg-gated to CLI build
- AtomicUsize voice counter for lock-free round-robin output routing
</decisions_made>

<blockers>
- Client-server MIDI round-trip not producing audible output
- Root cause unknown — could be:
  1. Server not receiving MIDI bytes correctly
  2. Server HarmonyEngine not producing output (PassThrough mode should echo)
  3. Server not writing responses back to TCP stream
  4. Client reader thread not receiving responses
  5. Client output routing not sending to MIDI ports
- Debug logging added but not yet tested by user
</blockers>

<context>
The debug logging added to session.rs and main.rs will reveal where the chain
breaks. Key log lines to watch:

Server terminal:
  [server] client X received N MIDI bytes  — confirms server gets data

Client terminal:
  [client] received N bytes from server: [...]  — confirms server responds
  [client] routing N bytes to output N  — confirms client routes to MIDI

If server receives but client doesn't see responses, the issue is in TCP write
or client read thread. If client receives but doesn't route, it's in the
output_router. If server doesn't receive at all, it's in client send or
protocol framing.

The uncommitted changes in src/main.rs and src/server/session.rs are ONLY
debug eprintln! additions — no logic changes.
</context>

<next_action>
1. Build and test with debug logging: cargo build && run server + client
2. Play a MIDI note and check both terminal outputs
3. Identify where the chain breaks from the log output
4. Fix the root cause
5. Remove debug logging (or keep as trace-level)
6. Commit fix: fix(04-04): fix client-server MIDI output routing
7. Re-verify with hardware
</next_action>

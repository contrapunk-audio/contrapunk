---
phase: 06.7-extended-scale-modes-barry-harris
plan: 03
type: execute
wave: 2
depends_on: ["06.7-01"]
files_modified:
  - src/ui.rs
autonomous: true

must_haves:
  truths:
    - "Scale dropdown groups modes by family with section headers"
    - "All ~28 scale modes are selectable from the dropdown"
    - "Barry Harris harmony mode appears in harmony mode dropdown"
  artifacts:
    - path: "src/ui.rs"
      provides: "Grouped scale dropdown using ScaleFamily"
      contains: "ScaleFamily"
  key_links:
    - from: "src/ui.rs"
      to: "src/harmony/config.rs"
      via: "ScaleFamily::all() and ScaleMode::all_in_family()"
      pattern: "all_in_family"
---

<objective>
Update the GUI to show scale modes grouped by family (Church, Harmonic Minor, Melodic Minor, Exotic, Barry Harris) in the scale dropdown, and add Barry Harris to the harmony mode dropdown.

Purpose: With ~28 scale modes, a flat list is unusable. Grouping by family makes selection intuitive. Barry Harris harmony mode must also be selectable.

Output: Updated ui.rs with grouped scale ComboBox and BarryHarris in harmony mode list.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.7-extended-scale-modes-barry-harris/06.7-01-SUMMARY.md
@src/ui.rs
@src/harmony/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement grouped scale dropdown and add BarryHarris to harmony mode list</name>
  <files>src/ui.rs</files>
  <action>
1. Find the existing scale mode ComboBox (currently iterates `ScaleMode::all()` flat). Replace with a grouped dropdown:
   - Use egui ComboBox with a custom popup body
   - For each `ScaleFamily::all()`, render a non-selectable label/header (e.g., bold or separator + family name)
   - Under each header, list `ScaleMode::all_in_family(family)` as selectable items
   - When a scale mode is selected, call `engine.set_scale_mode(mode)` as before
   - The ComboBox preview text should show the currently selected mode's Display name

2. The harmony mode dropdown already uses `HarmonyMode::all()` which will now include BarryHarris (added in plan 02). No code change needed for harmony mode dropdown IF `HarmonyMode::all()` is used. Verify this is the case. If the dropdown hard-codes modes, update it.

3. Style the family headers using the existing steampunk theme (gold/copper colors from the theme module). Use `ui.separator()` between families and `ui.label(RichText::new(family.to_string()).strong())` for headers.

4. Test: `cargo check` and `cargo build --target wasm32-unknown-unknown` (if WASM target is set up) to verify both native and WASM compile.
  </action>
  <verify>Run `cargo check`. Run `cargo build` to verify full compilation. Visually: run the app and confirm scale dropdown shows grouped families with all ~28 modes, and Barry Harris appears in harmony mode dropdown.</verify>
  <done>Scale dropdown shows 5 family groups with headers, all ~28 modes are selectable, Barry Harris harmony mode is in the harmony dropdown. Both native and WASM compile.</done>
</task>

</tasks>

<verification>
- `cargo check` passes
- `cargo build` succeeds
- Scale dropdown has 5 grouped sections
- All scale modes are present and selectable
- BarryHarris appears in harmony mode dropdown
</verification>

<success_criteria>
- Scale modes are grouped by family in the UI dropdown
- User can select any of the ~28 scale modes
- Barry Harris harmony mode is available in the harmony mode dropdown
- No regressions in existing UI functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06.7-extended-scale-modes-barry-harris/06.7-03-SUMMARY.md`
</output>

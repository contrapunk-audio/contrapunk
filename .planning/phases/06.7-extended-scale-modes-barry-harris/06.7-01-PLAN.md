---
phase: 06.7-extended-scale-modes-barry-harris
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/harmony/config.rs
  - src/harmony/scale.rs
autonomous: true

must_haves:
  truths:
    - "All 7 modes of harmonic minor are defined with correct intervals"
    - "All 7 modes of melodic minor are defined with correct intervals"
    - "5 exotic scales are defined with correct intervals"
    - "2 Barry Harris 8-note scales are defined with correct intervals"
    - "Scale struct supports both 7-note and 8-note scales"
    - "Existing tests pass with generalized scale system"
  artifacts:
    - path: "src/harmony/config.rs"
      provides: "ScaleMode enum with ~28 variants, ScaleFamily enum"
      contains: "ScaleFamily"
    - path: "src/harmony/scale.rs"
      provides: "Scale with Vec<u8> offsets supporting variable-length scales"
      contains: "Vec<u8>"
  key_links:
    - from: "src/harmony/config.rs"
      to: "src/harmony/scale.rs"
      via: "ScaleMode::intervals() returns Vec<u8>"
      pattern: "intervals.*Vec<u8>"
---

<objective>
Generalize the Scale system from fixed 7-note to variable-length scales, and add all new ScaleMode variants (28 total) with ScaleFamily grouping.

Purpose: This is the foundation for all extended scales. The current `[u8; 7]` offsets array and hardcoded `% 7` / `/ 7` arithmetic in Scale must be generalized to support Barry Harris 8-note scales alongside existing 7-note scales.

Output: Updated config.rs with ~28 ScaleMode variants + ScaleFamily enum, updated scale.rs with Vec<u8> offsets and scale_len-based arithmetic.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.7-extended-scale-modes-barry-harris/06.7-RESEARCH.md
@src/harmony/config.rs
@src/harmony/scale.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend ScaleMode enum and add ScaleFamily</name>
  <files>src/harmony/config.rs</files>
  <action>
1. Add a `ScaleFamily` enum with variants: `Church`, `HarmonicMinor`, `MelodicMinor`, `Exotic`, `BarryHarris`. Derive Clone, Copy, Debug, PartialEq, Eq, Serialize, Deserialize. Add Display impl and an `all()` method.

2. Expand `ScaleMode` enum from 9 to ~28 variants. Keep existing 9 variants unchanged (serde compatibility). Add:
   - Harmonic minor modes (5 new, HarmonicMinor already exists): LocrianNat6, IonianAug, DorianSharp4, PhrygianDominant, LydianSharp2, SuperLocrianDim
   - Melodic minor modes (6 new, MelodicMinor already exists): DorianFlat2, LydianAug, LydianDominant, MixolydianFlat6, LocrianNat2, SuperLocrian
   - Exotic (5 new): DoubleHarmonic, HungarianMinor, Enigmatic, NeapolitanMinor, NeapolitanMajor
   - Barry Harris (2 new): BHMajor6thDim, BHMinor6thDim

3. Change `intervals()` return type from `[u8; 7]` to `Vec<u8>`. Add all interval data from RESEARCH.md. Barry Harris scales return 8 elements, all others return 7.

4. Add `family(&self) -> ScaleFamily` method to ScaleMode that returns the family for each variant.

5. Update `all()` to return all variants. Add `all_in_family(family: ScaleFamily) -> Vec<ScaleMode>` method.

6. Update `Display` impl for all new variants with human-readable names (e.g., "Phrygian Dominant", "BH Major 6th Dim").

7. Update `Default` impl to remain Ionian.

IMPORTANT: Keep serde rename_all = "snake_case". New variant names must be valid snake_case serde names.
  </action>
  <verify>Run `cargo check` to verify config.rs compiles. Run `cargo test -p contrapunk -- config` if any config tests exist.</verify>
  <done>ScaleMode has ~28 variants, ScaleFamily enum exists, intervals() returns Vec&lt;u8&gt; with correct data for all variants including 8-note Barry Harris scales.</done>
</task>

<task type="auto">
  <name>Task 2: Generalize Scale struct for variable-length scales</name>
  <files>src/harmony/scale.rs</files>
  <action>
1. Change `offsets: [u8; 7]` to `offsets: Vec<u8>` in the Scale struct.

2. Add `scale_len()` helper method returning `self.offsets.len()`.

3. Fix ALL hardcoded 7-degree assumptions (identified in RESEARCH.md):
   - Line ~107: `total_degrees / 7` -> `total_degrees / scale_len as i8`
   - Line ~112: `((total_degrees % 7) + 7) % 7` -> `((total_degrees % len) + len) % len` where `len = scale_len as i8`
   - Line ~117: octave_shift * 12 stays the same (chromatic octave is always 12)
   - `degree_of()`: no change needed (already uses iter().position())
   - `snap_to_scale()`: no change needed

4. Update `Scale::new()` to call `mode.intervals()` which now returns Vec<u8>.

5. Update test `test_scale_new_with_each_mode`: remove the `assert_eq!(count, 7)` assertion — Barry Harris modes have 8 degrees. Change to `assert!(count == 7 || count == 8)` or check against `mode.intervals().len()`.

6. Add new tests:
   - `test_barry_harris_major_6th_dim_has_8_degrees`: Create scale with BHMajor6thDim, verify 8 notes in one octave (C through B), verify degree_of returns Some for all 8 scale tones.
   - `test_barry_harris_transpose_diatonic`: Transpose in 8-note scale, verify octave wrapping works (degree 7 + 1 = degree 0 one octave up).
   - `test_phrygian_dominant_intervals`: Verify C Phrygian Dominant has Db, E (not Eb), G, Ab, Bb.
   - `test_existing_modes_unchanged`: Verify C Ionian still gives C D E F G A B.

7. Run full test suite to ensure no regressions.
  </action>
  <verify>Run `cargo test` — all existing tests pass, new tests pass. Run `cargo check` with no warnings.</verify>
  <done>Scale struct uses Vec&lt;u8&gt;, all arithmetic uses scale_len() instead of hardcoded 7, Barry Harris 8-note scales work with transpose_diatonic and degree_of, all existing tests pass.</done>
</task>

</tasks>

<verification>
- `cargo test` passes with zero failures
- `cargo check` produces no errors
- ScaleMode::all().len() >= 28
- ScaleMode::BHMajor6thDim.intervals().len() == 8
- ScaleMode::Ionian.intervals().len() == 7
- Scale::new(0, ScaleMode::BHMajor6thDim).degree_of(Note::C4) == Some(0)
</verification>

<success_criteria>
- All 28 scale mode variants compile and have correct interval data
- ScaleFamily enum groups modes correctly
- Scale struct handles 7-note and 8-note scales with correct octave arithmetic
- All pre-existing harmony engine tests pass without modification (except the count==7 assertion)
</success_criteria>

<output>
After completion, create `.planning/phases/06.7-extended-scale-modes-barry-harris/06.7-01-SUMMARY.md`
</output>

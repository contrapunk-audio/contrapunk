---
phase: 10.3-internal-midi-looper
plan: 03
type: execute
wave: 3
depends_on:
  - 10.3-02
files_modified:
  - ui/src/lib/adapter/types.ts
  - ui/src/lib/adapter/tauri.ts
  - ui/src/lib/adapter/wasm.ts
  - ui/src/lib/adapter/plugin.ts
  - ui/src/lib/stores/looper.svelte.ts
  - ui/src/lib/stores/transport.svelte.ts
  - ui/src/lib/components/TransportBar.svelte
autonomous: false
requirements:
  - LOOP-09
  - LOOP-10

must_haves:
  truths:
    - "Only Tauri advertises and renders MIDI looper controls"
    - "The UI exposes press, clear, and truthful backend status without implementing musical timing"
    - "Reload starts Empty because no loop state is persisted"
    - "Automated checks pass before the manual MPK and guitar performance checkpoint"
    - "Daisy work remains blocked until the user explicitly accepts the performance session"
  artifacts:
    - path: "ui/src/lib/stores/looper.svelte.ts"
      provides: "Capability-gated Tauri looper state and actions"
    - path: "ui/src/lib/components/TransportBar.svelte"
      provides: "Minimal accessible Loop/Clear/status controls"
  key_links:
    - from: "ui/src/lib/stores/looper.svelte.ts"
      to: "src-tauri/src/commands/looper.rs"
      via: "adapter press, clear, and status calls"
    - from: "ui/src/lib/components/TransportBar.svelte"
      to: "ui/src/lib/stores/transport.svelte.ts"
      via: "beat updates refresh truthful loop status"
---

<objective>
Expose the validated backend through the minimum desktop controls, run the complete automated gate, and stop for the user's MPK-plus-guitar performance acceptance before any Daisy work resumes.
</objective>

<context>
@.planning/phases/10.3-internal-midi-looper/10.3-CONTEXT.md
@.planning/phases/10.3-internal-midi-looper/10.3-02-SUMMARY.md
@ui/src/lib/adapter/types.ts
@ui/src/lib/adapter/tauri.ts
@ui/src/lib/adapter/wasm.ts
@ui/src/lib/adapter/plugin.ts
@ui/src/lib/stores/transport.svelte.ts
@ui/src/lib/components/TransportBar.svelte
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add an honest surface capability and adapter contract</name>
  <action>Add `midiLooper`, status types, and press/clear/status methods to the adapter contract. Tauri sets the capability true and invokes the backend. WASM/plugin set it false and return no fake local behavior. Keep methods present for interface completeness but controls hidden when unsupported.</action>
  <verify><automated>npm --prefix ui run check</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Add the minimal looper store and controls</name>
  <action>Add one Svelte store that reads backend status and delegates press/clear. Refresh after actions and on transport beat updates; do not poll faster or reproduce scheduler timing in JavaScript. Add Loop, Clear, and compact state/length text to `TransportBar` only when `midiLooper` is true. Use real buttons, focus-visible behavior, ARIA labels/live status, disabled pending state, and reduced-motion-safe styling. A separate Clear button is the desktop equivalent of the future hardware long press.</action>
  <verify><automated>npm --prefix ui run check</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Run the automated release gate</name>
  <action>Run the focused core, harmony, Tauri, workspace, UI, catalog, formatting, and diff checks. Do not regenerate WASM because Phase 10.3 intentionally adds no WASM binding; only the shared TypeScript capability is false there. Record pre-existing Clippy failures separately rather than widening this phase into unrelated cleanup.</action>
  <verify><automated>cargo test -p contrapunk-transport --lib && cargo test -p contrapunk-companion --lib && cargo test -p contrapunk-harmony --lib && cargo test -p contrapunk-tauri --lib && cargo check --workspace --message-format=short && node --test ui/src/lib/arrangement/catalog.test.mjs && npm --prefix ui run check && cargo fmt --all -- --check && git diff --check</automated></verify>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: User performs the desktop looper acceptance session</name>
  <action>Provide a tested desktop build and the exact seven-step script from `10.3-CONTEXT.md`: stopped count-in recording with MPK, downbeat close, live layering including same pitch, key/arrangement reharmonization, clean monophonic guitar take, stop/resume/clear with sustain, and final stuck-note/drift check.</action>
  <verify><manual>User confirms no stuck notes, duplicate attacks, missed/duplicated loop events, Phrase Context contamination, recursive capture, or timing drift.</manual></verify>
</task>

</tasks>

<verification>
- Tauri controls are visible and functional; WASM/plugin controls are absent.
- Status always comes from Rust and displays Empty, Armed, Recording, Closing, Playing, or Stopped truthfully.
- Reload/restart begins Empty.
- Automated gate passes with pre-existing warnings/failures explicitly separated.
- User acceptance is recorded before Phase 10.3 is marked complete.
</verification>

<output>
After user approval, create `10.3-03-SUMMARY.md`, mark LOOP-01 through LOOP-10 and Phase 10.3 complete, and unblock separate Daisy firmware/hardware-UX planning. If the user finds a performance defect, keep Phase 10.3 active and fix only the failing contract before retesting.
</output>

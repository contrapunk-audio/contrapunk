---
phase: 06.10.1-ui-modernization
plan: 06
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - ui/src/lib/components/HumanizePanel.svelte
  - ui/src/lib/components/GeneratorPanel.svelte
  - ui/src/lib/components/ActiveNotes.svelte
  - ui/src/routes/+page.svelte
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can enable/disable humanization and adjust all parameters (jitter, velocity, duration, swing)"
    - "User can adjust BPM and toggle metronome"
    - "User can see active input and harmony notes in real-time"
    - "User can configure note generator mode and parameters"
    - "Humanization and generator panels integrate into right column of layout"
    - "Active notes display shows note names with color coding"
  artifacts:
    - path: "ui/src/lib/components/HumanizePanel.svelte"
      provides: "Humanization controls (jitter, velocity, duration, swing, BPM, metronome)"
      contains: "humanize"
    - path: "ui/src/lib/components/GeneratorPanel.svelte"
      provides: "Note generator mode and configuration"
      contains: "generator"
    - path: "ui/src/lib/components/ActiveNotes.svelte"
      provides: "Real-time note display with color coding"
      contains: "notes"
  key_links:
    - from: "ui/src/lib/components/HumanizePanel.svelte"
      to: "ui/src/lib/adapter/index.ts"
      via: "adapter humanize calls"
      pattern: "adapter"
    - from: "ui/src/lib/components/ActiveNotes.svelte"
      to: "ui/src/lib/stores/engine.svelte.ts"
      via: "engine store note state"
      pattern: "engine"
---

<objective>
Build the humanization, note generator, and active notes display panels for the right column.

Purpose: Complete the three-column layout by implementing the right column panels: humanization controls (timing/velocity/swing), note generator configuration, and real-time active notes display. These panels cover all remaining control surfaces from the existing egui UI.

Output: Working humanization panel with all parameter sliders, note generator panel with mode selection, and live active notes display — all integrated into the right column.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.10.1-ui-modernization/06.10.1-CONTEXT.md
@.planning/phases/06.10.1-ui-modernization/06.10.1-RESEARCH.md
@.planning/phases/06.10.1-ui-modernization/06.10.1-03-SUMMARY.md
@.planning/phases/06.10.1-ui-modernization/06.10.1-04-SUMMARY.md
@src/ui.rs
@src/humanize/config.rs
@src/generator/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build humanization and BPM control panel</name>
  <files>
    ui/src/lib/components/HumanizePanel.svelte
  </files>
  <action>
Create the humanization controls panel with HLD styling:

**HumanizePanel.svelte:**

**Humanize Section:**
- Header: "HUMANIZE" in --color-accent-gold pixel font
- Master enable/disable toggle (pixel-style checkbox/button)
- When enabled, show parameter sliders:

**Parameter sliders (all use HLD-styled range inputs):**
- Timing Jitter: 0-30ms range, step 1. Label "JITTER" + value display
- Velocity Variation: 0-20 range, step 1. Label "VELOCITY" + value
- Duration Variation: 0-50ms range, step 1. Label "DURATION" + value
- Swing Amount: 0-100% range, step 1. Label "SWING" + value

**Slider styling (HLD aesthetic):**
- Replace default range input with custom CSS:
  - Track: thin line in --color-border, active portion in --color-accent-cyan
  - Thumb: small square (not round) in --color-accent-magenta with glow
  - Labels in pixel font at --font-size-xs
  - Value readout next to slider in --color-accent-cyan
- Health-bar style: filled portion glows like HLD health bars

**Beat Clock Section:**
- Header: "TEMPO" in --color-accent-gold
- BPM control: number input or slider, range 40-240, current value prominently displayed
- Metronome toggle: on/off with "METRO" label
- Time signature display (if applicable)
- Beat position indicator: simple pulsing dot or bar that follows the beat (visual metronome)

Each parameter change calls adapter.setHumanizeConfig() with the updated values.
Store humanize state in a local reactive state (part of engine store or separate).
  </action>
  <verify>
    <automated>cd /Users/vibhavbobade/go/src/github.com/waveywaves/contrapunk/ui && npm run build 2>&1 | tail -5</automated>
    <manual>Verify all humanize sliders render, BPM control works, metronome toggle present</manual>
  </verify>
  <done>Humanization panel renders with enable toggle, 4 parameter sliders (jitter, velocity, duration, swing), BPM control, and metronome toggle. HLD health-bar slider styling applied.</done>
</task>

<task type="auto">
  <name>Task 2: Build generator panel, active notes display, and wire right column</name>
  <files>
    ui/src/lib/components/GeneratorPanel.svelte
    ui/src/lib/components/ActiveNotes.svelte
    ui/src/routes/+page.svelte
  </files>
  <action>
Create the note generator panel and active notes display, integrate into layout.

**GeneratorPanel.svelte:**
- Header: "GENERATOR" in --color-accent-gold pixel font
- Enable/disable toggle for the note generator
- Mode selector: grid of buttons for generator modes (Held Notes, Chord, Arpeggio Up, Arpeggio Down, Arpeggio UpDown, Scale Runner, Random Diatonic)
- When Chord mode selected: chord type selector grid (Major, Minor, 7th, Maj7, Min7, Dim, Aug, Sus4, Sus2 — from existing ChordType enum)
- When Arpeggio mode selected: show direction buttons (Up, Down, UpDown)
- Selected notes display: list of currently selected generator notes
- Note selection: either via clicking piano keys (handled in Piano.svelte via store) or via note name buttons
- Styling: pixel-btn grids, --color-accent-teal for generator highlights

**ActiveNotes.svelte:**
- Header: "NOTES" in --color-accent-gold pixel font
- Two sections:
  1. "INPUT" — list of input note names (e.g., "C4 E4 G4") in --color-piano-input green
  2. "HARMONY" — list of harmony note names in --color-piano-harmony orange
- Notes sorted by pitch (low to high)
- If no notes active, show "(none)" in --color-text-dim
- Chord name display: show detected chord prominently (e.g., "Cmaj7")
- If modal interchange active, show borrowed-from mode: "from Aeolian" in --color-piano-borrowed amber
- Compact layout — just colored text lists, no heavy UI chrome
- Updates reactively from engine store (inputNotes, harmonyNotes, chordName, lastBorrowedFrom)

**Layout integration (+page.svelte):**
- Import GeneratorPanel, HumanizePanel, ActiveNotes
- Right column layout (top to bottom):
  1. ActiveNotes (real-time note display)
  2. HumanizePanel (humanization controls)
  3. GeneratorPanel (note generator)
- Each panel in a pixel-card container
- Right column scrolls if content overflows

Ensure all three columns (left: MIDI/Presets, center: Controls, right: Notes/Humanize/Generator) are fully populated. The main page should now show the complete feature set.
  </action>
  <verify>
    <automated>cd /Users/vibhavbobade/go/src/github.com/waveywaves/contrapunk/ui && npm run build 2>&1 | tail -5</automated>
    <manual>Verify all three columns populated, active notes display updates, generator modes selectable</manual>
  </verify>
  <done>Generator panel with mode selection, active notes display with color-coded notes and chord name, all integrated into right column. Three-column layout fully populated with all existing functionality.</done>
</task>

</tasks>

<verification>
1. `cd ui && npm run build` succeeds
2. Humanize panel has 4 sliders + BPM + metronome toggle
3. Generator panel shows mode grid with chord/arpeggio sub-options
4. Active notes display shows INPUT and HARMONY sections
5. Right column contains all three panels
6. All three columns of the layout are populated
</verification>

<success_criteria>
- Humanization controls cover all parameters (jitter, velocity, duration, swing, BPM, metronome)
- Note generator panel has mode selection and configuration
- Active notes display shows color-coded input/harmony notes with chord detection
- Right column fully integrated into the Ableton-inspired layout
- All existing egui UI functionality now has a Svelte equivalent
</success_criteria>

<output>
After completion, create `.planning/phases/06.10.1-ui-modernization/06.10.1-06-SUMMARY.md`
</output>

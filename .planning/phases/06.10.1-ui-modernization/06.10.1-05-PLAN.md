---
phase: 06.10.1-ui-modernization
plan: 05
type: execute
wave: 3
depends_on: [03, 04]
files_modified:
  - ui/src/lib/components/MidiDevices.svelte
  - ui/src/lib/components/PresetManager.svelte
  - ui/src/routes/+page.svelte
autonomous: true
requirements: []

must_haves:
  truths:
    - "User can select MIDI input device from a dropdown of available ports"
    - "User can select up to 8 MIDI output devices from available ports"
    - "User can see preset list and load a preset (builtin + custom)"
    - "User can save current configuration as a custom preset"
    - "User can delete custom presets"
    - "MIDI device and preset components integrate into the left column of the layout"
  artifacts:
    - path: "ui/src/lib/components/MidiDevices.svelte"
      provides: "MIDI device selection (input + output)"
      contains: "midi"
    - path: "ui/src/lib/components/PresetManager.svelte"
      provides: "Preset list, load, save, delete"
      contains: "preset"
  key_links:
    - from: "ui/src/lib/components/MidiDevices.svelte"
      to: "ui/src/lib/stores/midi.svelte.ts"
      via: "import midi store"
      pattern: "midi"
    - from: "ui/src/lib/components/PresetManager.svelte"
      to: "ui/src/lib/adapter/index.ts"
      via: "adapter preset calls"
      pattern: "adapter"
---

<objective>
Build the MIDI device selection and preset management panels for the left column.

Purpose: Implement the two panels that occupy the left column of the Ableton-inspired layout: MIDI device management (input/output selection) and preset management (load/save/delete). These cover the essential I/O and configuration persistence features.

Output: Working MIDI device selection with input dropdown and multi-output selector. Working preset manager with list, load, save, and delete operations.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.10.1-ui-modernization/06.10.1-CONTEXT.md
@.planning/phases/06.10.1-ui-modernization/06.10.1-RESEARCH.md
@.planning/phases/06.10.1-ui-modernization/06.10.1-03-SUMMARY.md
@.planning/phases/06.10.1-ui-modernization/06.10.1-04-SUMMARY.md
@src/ui.rs
@src/preset/builtins.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build MIDI device selection panel</name>
  <files>
    ui/src/lib/components/MidiDevices.svelte
  </files>
  <action>
Create the MIDI device selection component with HLD styling:

**MidiDevices.svelte:**

**Input Device Section:**
- Header: "INPUT" in --color-accent-gold pixel font
- Dropdown/select showing available input ports from `midi.inputs` store
- Options include: "Select..." (null), plus all available ports by name
- Also include virtual inputs: "Note Generator" and "Computer Keyboard" (sentinel values, matching existing INPUT_NOTE_GENERATOR and INPUT_COMPUTER_KEYBOARD from app.rs)
- Selected input stored in `midi.selectedInput`
- "Refresh" button (small pixel icon or text) that calls `midi.refresh()`

**Output Device Section:**
- Header: "OUTPUTS" in --color-accent-gold pixel font
- 8 output slots (matching existing MAX_OUTPUT_SLOTS = 8)
- Each slot: a dropdown showing available output ports from `midi.outputs` store
- Slots are labeled "Voice 1" through "Voice 8" (Voice 1 is melody passthrough, rest are harmony)
- Each slot can be "None" or an output port
- Selected outputs stored in `midi.selectedOutputs` array
- Slots use compact layout to fit in left column

**Computer keyboard input:**
- When "Computer Keyboard" is selected as input, show a small info text: "Z-M: C3-B3, Q-U: C4-C5" (the QWERTY piano mapping)

**Styling:**
- Use pixel-card class for section containers
- Dropdowns styled with --color-widget-bg background, --color-border border, --color-text-primary text
- Active/selected items highlighted with --color-accent-cyan
- Compact layout — minimal spacing, dense information (Ableton style)
- No smooth transitions on hover/selection
  </action>
  <verify>
    <automated>cd /Users/vibhavbobade/go/src/github.com/waveywaves/contrapunk/ui && npm run build 2>&1 | tail -5</automated>
    <manual>Verify input dropdown lists devices, 8 output slots render, virtual inputs appear</manual>
  </verify>
  <done>MIDI device selection panel renders with input dropdown, 8 output slots, virtual input options, and refresh button. HLD styling applied.</done>
</task>

<task type="auto">
  <name>Task 2: Build preset manager panel and wire into layout</name>
  <files>
    ui/src/lib/components/PresetManager.svelte
    ui/src/routes/+page.svelte
  </files>
  <action>
Create the preset management component and integrate both panels into the layout.

**PresetManager.svelte:**

**Preset List:**
- Header: "PRESETS" in --color-accent-gold pixel font
- List of presets from adapter.listPresets() — show name, builtin badge
- Active preset highlighted with --color-accent-magenta background
- Click to load: calls adapter.loadPreset(name), then syncs engine store from backend
- Builtin presets (11+) have a subtle badge or different border color
- Custom presets show a small delete icon (pixel "X")

**The 11+ builtin presets from the existing system:**
Classical Elegance, Jazz Explorer, Pop Machine, Ambient Dreamer, Blues Wanderer, Baroque Inventor, Celtic Mystic, Modal Jazz Master, Funk Alchemist, Gamelan Echo, Minimalist. Each has a character persona — show a 1-line description or just the name for compactness.

**Save Controls:**
- "Save Preset" button that opens a small inline form (name input + confirm)
- Name input: pixel-font, --color-widget-bg background
- Saves current engine state as a new custom preset

**Delete:**
- Only available for custom presets (not builtins)
- Clicking delete icon removes the preset after confirmation (simple confirm dialog or inline "Sure?" toggle)

**Layout integration (+page.svelte):**
- Import MidiDevices and PresetManager
- Place MidiDevices at top of left column
- Place PresetManager below MidiDevices in left column
- Both use pixel-card containers with consistent spacing
- Left column should scroll if content overflows (but aim for compact fit)

**Styling:**
- Preset list items: small text (--font-size-xs), compact padding (4px)
- Active preset: magenta glow border
- Builtin badge: small "BUILT-IN" text in --color-text-dim
- Save/Delete buttons: pixel-btn style
- Entire panel fits in ~200px width left column
  </action>
  <verify>
    <automated>cd /Users/vibhavbobade/go/src/github.com/waveywaves/contrapunk/ui && npm run build 2>&1 | tail -5</automated>
    <manual>Verify preset list renders with builtin presets, save/delete buttons work, left column shows both panels</manual>
  </verify>
  <done>Preset manager renders with preset list, save, and delete. MIDI devices and presets integrated into left column. Layout complete for left panel.</done>
</task>

</tasks>

<verification>
1. `cd ui && npm run build` succeeds
2. MIDI input dropdown shows virtual inputs (Note Generator, Computer Keyboard)
3. 8 output slots render with "None" default
4. Preset list shows builtin presets
5. Save preset form appears on button click
6. Left column contains both MIDI and Preset panels
</verification>

<success_criteria>
- MIDI device selection works (input dropdown + 8 output slots + virtual inputs)
- Preset management works (list, load, save, delete)
- Both panels integrated into left column of Ableton layout
- HLD pixel art styling consistent
</success_criteria>

<output>
After completion, create `.planning/phases/06.10.1-ui-modernization/06.10.1-05-SUMMARY.md`
</output>

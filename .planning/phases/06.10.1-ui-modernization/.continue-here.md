---
phase: 06.10.1-ui-modernization
task: 9 (Task 2 — human verification)
total_tasks: 9 plans (18 tasks total)
status: in_progress
last_updated: 2026-02-25T01:18:52.872Z
---

<current_state>
Phase 06.10.1 (UI Modernization) is code-complete. All 9 plans executed. Plan 09 Task 1 (remove egui) done. Plan 09 Task 2 (human verification) in progress.

The dropdown styling issue from last session has been FIXED — created a custom PixelSelect.svelte component replacing native `<select>` elements in MidiDevices.svelte. The dropdowns now match the HLD neon-on-dark pixel aesthetic.

However, during investigation of "nothing is wired up" feedback, we discovered the app has NO initialization logic — the adapter layer exists but nothing calls it on startup. The WASM package is a stub (dev fallback). The key missing pieces are:

1. **No `adapter.init()` call** — nobody initializes the WASM/Tauri adapter on page load
2. **No automatic MIDI device refresh** — the midi store's `refresh()` isn't called on mount
3. **Start button partially broken** — `engine.toggle()` only handles stop; start requires device indices but the StatusBar doesn't pass them
4. **Stub WASM** — `ui/src/lib/wasm-pkg/index.js` is a dev stub that logs warnings and does nothing real. Need to run `wasm-pack build` to get real WASM, OR wire up the stub to actually work for dev
</current_state>

<completed_work>

- Plans 01-08: All complete (Tauri backend, HLD design system, stores, WASM bridge, components, particles, build pipeline)
- Plan 09 Task 1: Remove egui/eframe/CLI/Trunk (-6,056 lines) - Done
- Plan 09 Task 2: Human verification - IN PROGRESS
  - Fixed dropdown styling: Created PixelSelect.svelte custom component (button+positioned div pattern)
  - Updated MidiDevices.svelte to use PixelSelect instead of native `<select>`
  - Diagnosed wiring gap: adapter init, MIDI refresh, and start/stop flow not connected

Total commits: ~25
</completed_work>

<remaining_work>

1. **Wire up app initialization** — Add `adapter.init()` call on page/layout mount, followed by `engine.syncFromBackend()` and `midi.refresh()`
2. **Fix Start/Stop flow** — StatusBar's Start button needs to call `engine.start(midi.selectedInput, midi.selectedOutputs)` with actual device indices from the midi store
3. **Build real WASM** (optional for dev) — Run `wasm-pack build wasm/ --target web --out-dir ui/src/lib/wasm-pkg` to replace the stub, OR enhance the stub to make dev mode functional
4. **Complete human verification** — Once wired up, verify all controls, piano, particles, HLD aesthetic at localhost:5173
5. **After approval** — Finalize 06.10.1-09-SUMMARY.md and update STATE.md/ROADMAP.md to mark phase complete
</remaining_work>

<decisions_made>

- Created PixelSelect.svelte as reusable custom dropdown component (button + absolutely-positioned div) because native `<select>` option popups are OS-rendered and ignore CSS
- PixelSelect uses cyan glow on open/focus, magenta for active selection, deep indigo panel background
- Supports `small` prop for compact output slot dropdowns
- Uses focusout + relatedTarget check for click-outside-to-close behavior
</decisions_made>

<blockers>
- App initialization not wired: adapter.init(), engine.syncFromBackend(), midi.refresh() need to be called on mount
- Start/Stop button needs midi store integration for device indices
</blockers>

<context>
The architecture is sound — adapter pattern (Tauri vs WASM), Svelte 5 rune stores, all components built. The gap is purely in the bootstrap/initialization layer. The page loads, components render, but nothing talks to the backend because nobody kicks off the adapter. This is a ~30 line fix in +layout.svelte or +page.svelte to:
1. Call adapter.init()
2. Call engine.syncFromBackend()
3. Call midi.refresh()
4. Wire StatusBar start button to engine.start() with midi store state

The stub WASM is fine for dev — the stub Engine class has all the right methods, they just don't do real harmony processing. But they're enough to test the UI wiring.

Files modified this session:
- ui/src/lib/components/PixelSelect.svelte (NEW - custom dropdown component)
- ui/src/lib/components/MidiDevices.svelte (MODIFIED - uses PixelSelect instead of native select)
</context>

<next_action>
1. Add initialization logic in +page.svelte or +layout.svelte: call adapter.init(), engine.syncFromBackend(), midi.refresh() in an onMount/$effect
2. Fix StatusBar Start button to call engine.start(midi.selectedInput, midi.selectedOutputs)
3. Test at localhost:5173 — controls should now be reactive even with stub WASM
4. Ask user to verify the UI
</next_action>

---
phase: 08-guitar-input-integration
task: debug-tauri-start-routing
total_tasks: 6
status: blocked
last_updated: 2026-04-01T17:53:19.399Z
---

<current_state>
Guitar input wiring Tasks 1-5 complete. Task 6 (end-to-end test) blocked because the Tauri app shows "Initializing engine..." and never starts routing. The backend's start_routing is never called — no [tauri-router] logs appear in terminal. The issue is in the UI layer, not the Rust backend.
</current_state>

<completed_work>
## Massive Session — Everything Built

### ML Training (5 rounds)
- Round 1: Raw baseline (Pure CNN 96.2%)
- Round 2: Onset alignment (+0.0%)
- Round 3: Quality cleanup (97.3% — best)
- Round 4: Goertzel harmonics (96.4%)
- Round 5: Data augmentation (96.4%)
- All model weights saved (.pt, .joblib, .onnx, .npy)

### DSP Guitar Input Pipeline (src/audio/guitar_input.rs)
- 93+ tests, full MPE output
- 9 accuracy improvements (state machine, plausibility, multi-frame voting, octave correction, spectral flux, note-off gating, input gain, configurable window)
- Vibrato + slide + legato + bend detection
- Calibrated velocity (attack peak, power curve)
- CC74 brightness from spectral centroid
- Pressure envelope with hold
- 14-bit pitch bend, per-string channels 2-7, NoteOff velocity
- Message ordering (PB before NoteOn)
- MidiEvent::to_midi_bytes() for MIDI channel output

### Guitar Input Wiring (Tauri backend)
- Task 1: MidiEvent::to_midi_bytes() — DONE
- Task 2: GuitarBridge module (cpal → DSP → mpsc) — DONE
- Task 3: Routing thread integration — DONE
- Task 4: Tauri commands (set_guitar_device, set_guitar_config, list_audio_devices) — DONE
- Task 5: UI adapter wiring — DONE

### WASM
- WasmGuitarInput compiled to WASM (full Rust DSP in browser)
- guitarCapture.ts wired to WASM process_block
- Browser guitar input limited by getUserMedia (can't access Audient iD14 DI properly, RMS stays at noise floor 0.002)

### Diary (deployed to contrapunk.fly.dev)
- 8+ pages: landing (animated demo), ML overview, Rounds 1-5, The Pivot
- 15+ components: audio playback, spectrograms, SVG charts, model diagrams
- Interactive dials, per-string tuner, technique toggles

### UI Guitar Input Panel
- GuitarInputPanel with device/channel selector, dials (+/- buttons), technique toggles
- GuitarInputStore with localStorage persistence
- Guitar Audio as virtual input in MidiDevices dropdown
- MIDI virtual input persistence (Guitar Audio, Computer Keyboard, Note Generator)
- Tauri adapter detection fixed (__TAURI_INTERNALS__ for v2)

### Research
- DSP deep dive (MPM > YIN, inharmonicity B, Abesser features)
- MPE + MIDI Guitar 3 analysis (5 dimensions of guitar expression)
- Modulator architecture phase plan
- Guitar input integration spec + implementation plan
</completed_work>

<remaining_work>
- DEBUG: Tauri app "Initializing engine..." — start_routing never called
- Browser guitar input: getUserMedia can't access Audient iD14 DI (noise floor only)
- WASM audio bridge needs AudioWorklet for proper pro audio interface access
- Modulator architecture (envelope shaping between DSP and MIDI output)
- Diary DSP chapter
- 3-pass tuner in UI (currently single-pass noise floor + per-string)
- Deploy latest diary updates
</remaining_work>

<decisions_made>
- Pure DSP primary, ML refinement layer (98.5% DSP vs 97.3% ML)
- MPE output: per-string ch 2-7, master ch 1, PB before NoteOn
- Compile GuitarInput to WASM (not TypeScript port) for browser
- Legato detected by speed (>50 cents/frame), not distance
- "Contrapunk is an improvisation companion"
- Both Tauri + WASM from start
- localStorage persistence for all guitar settings
</decisions_made>

<blockers>
- Tauri app: "Initializing engine..." stuck — start_routing Tauri command is never invoked by the UI. Backend logs (eprintln) never appear. The UI's engine.start() calls adapter.startRouting() but it seems to hang. Possible causes:
  1. Adapter factory might still be choosing WasmAdapter in the Tauri window
  2. The WasmAdapter's startRouting for guitar might be hanging on WASM init
  3. The Tauri invoke call might have a parameter mismatch
  DEBUG: Open Tauri devtools (right-click → Inspect → Console) to see JS errors
- Browser: getUserMedia returns only 2 channels and can't access Audient iD14's DI input (RMS 0.002 = noise only)
</blockers>

<context>
The guitar_input_demo example works perfectly with cpal — full DSP pipeline, tuner, calibration, live detection. The issue is ONLY in how the Tauri app's UI connects to the backend. The Rust backend code (GuitarBridge, routing thread integration, Tauri commands) compiled cleanly and should work once startRouting is actually called.

The most likely fix: add console.log to engine.start() and adapter.startRouting() to trace where it hangs, then fix the parameter mismatch or adapter detection issue.
</context>

<next_action>
Start with: Open Tauri devtools console to see JS errors. Add console.log to ui/src/lib/stores/engine.svelte.ts start() method and ui/src/lib/adapter/tauri.ts startRouting() to trace where the call hangs. The fix is likely a Tauri invoke parameter mismatch or the adapter factory still choosing WasmAdapter.
</next_action>

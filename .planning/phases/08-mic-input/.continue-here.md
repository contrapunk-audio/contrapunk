---
phase: 07-mic-input
task: 0
total_tasks: 8
status: planned
last_updated: 2026-02-06
---

<current_state>
Phase 7 (Mic Input) is fully PLANNED but NOT YET EXECUTED.

All 8 plans have been:
- Created via gsd-planner
- Verified by gsd-plan-checker (passed after 1 revision)
- Committed to git

The phase is ready for execution. No code has been written yet.
</current_state>

<completed_work>

## This Session
- Validated .planning/ environment exists
- Researched Phase 7 domain (audio capture, pitch detection, audio-to-MIDI)
- Created 07-RESEARCH.md with standard stack (cpal, pitch-detection, rtrb)
- Created 7 initial plans (07-01 through 07-07)
- Plan checker found 5 blockers, 1 warning
- Revision added 07-05b (WASM integration split), fixed all issues
- Final verification passed with 8 plans in 5 waves

## Plans Created

| Plan | Wave | Objective |
|------|------|-----------|
| 07-01 | 1 | Audio module foundation: config types, pitch detection types, note tracker |
| 07-02 | 2 | Native audio capture (cpal + rtrb lock-free ring buffer) |
| 07-03 | 2 | Pitch detector (YIN algorithm, freq-to-MIDI conversion) |
| 07-04 | 2 | WASM audio capture (getUserMedia + AnalyserNode) |
| 07-05 | 3 | Native app integration: MicState, dropdown, harmony wiring, persistence |
| 07-05b | 3 | WASM app integration: WebAudioCapture wiring, permissions |
| 07-06 | 4 | UI: Mic Settings, tuner display, level meter, piano coloring (cyan) |
| 07-07 | 5 | Human verification checkpoint |

</completed_work>

<remaining_work>

## Phase 7 Execution
All 8 plans need to be executed:
- Plan 01: Audio types foundation
- Plan 02: Native capture
- Plan 03: Pitch detection
- Plan 04: WASM capture
- Plan 05: Native integration + persistence
- Plan 05b: WASM integration
- Plan 06: UI controls
- Plan 07: Human verification

## After Phase 7
- Phase 8 (Vocoder) uses the raw audio buffer created in this phase
- Phases 6, 6.1, 6.4, 6.5 are incomplete but lower priority

</remaining_work>

<decisions_made>

## User Decisions (from CONTEXT.md - LOCKED)
- Monophonic pitch detection only (single pitch at a time)
- Voice range C2-C6 optimized for vocals
- Quantize to nearest chromatic note (no pitch bend)
- Smooth vibrato handling (hysteresis prevents flickering)
- Unified IN dropdown (mic alongside MIDI devices)
- Tuner-style display (note + octave + cents deviation)
- Persist all mic settings across sessions
- Mic profiles feature (like presets)
- Default 30-40ms latency, user-adjustable

## Claude Discretion (from CONTEXT.md)
- YIN algorithm via pitch-detection crate (best accuracy/speed for voice)
- cpal for native audio capture (de facto Rust audio library)
- rtrb for lock-free ring buffer (audio callback to main thread)
- Web Audio API getUserMedia + AnalyserNode for WASM
- Cyan color (100, 220, 255) for mic-detected notes on piano

## Planning Decisions
- Split Plan 05 into 05 (native) and 05b (WASM) to reduce complexity
- Added persistence task to Plan 05 for settings save/load
- Uses existing midi_defaults.rs pattern for persistence

</decisions_made>

<blockers>
None. Phase is fully planned and verified. Ready for execution.
</blockers>

<context>
Phase 7 adds microphone input to Contrapunk, enabling:
1. Voice/melody → MIDI conversion via pitch detection
2. Raw audio passthrough buffer for Phase 8 (Vocoder)

Key technical approach:
- Native: cpal audio thread → rtrb ring buffer → main thread pitch detection
- WASM: getUserMedia → AnalyserNode → frame-based polling in update()
- Both: YIN algorithm with clarity threshold (0.7) and hysteresis (40 cents)

The architecture follows existing patterns:
- Similar to router thread communication (Arc<Mutex<GUIRouterState>>)
- Persistence via MidiDefaults (like Phase 6.6)
- Virtual input sentinel (like Note Generator usize::MAX)
</context>

<next_action>
Run: `/gsd:execute-phase 7`

This will:
1. Execute all 8 plans across 5 waves
2. Wave 1: Foundation types (parallel: none)
3. Wave 2: Capture + detection (parallel: 02, 03, 04)
4. Wave 3: Integration (parallel: 05, 05b)
5. Wave 4: UI controls
6. Wave 5: Human verification checkpoint

Remember to `/clear` first for fresh context window.
</next_action>

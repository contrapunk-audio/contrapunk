---
phase: 10.1-guitar-reliability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/contrapunk-audio/Cargo.toml
  - crates/contrapunk-audio/src/guitar_input.rs
  - crates/contrapunk-audio/tests/guitar_corpus_acceptance.rs
  - crates/contrapunk-audio/tests/guitar_corpus_holdout.txt
  - src-tauri/src/guitar_bridge.rs
  - src-tauri/src/commands/guitar.rs
  - src-tauri/src/state.rs
  - ui/src/lib/components/GuitarInputPanel.svelte
  - ui/src/lib/adapter/wasm.ts
  - ui/src/lib/audio/guitarCapture.ts
autonomous: true
requirements:
  - GTR-REL-01
  - GTR-REL-02
  - GTR-REL-03
  - GTR-REL-04
  - GTR-REL-05
  - GTR-REL-06

must_haves:
  truths:
    - "The shipping GuitarInput is evaluated against every labeled corpus WAV with an immutable holdout list"
    - "Both development and holdout meet the locked corpus-specific gates"
    - "A single isolated recording cannot leave a note active or emit duplicate raw bend messages"
    - "Structural config replacement rebuilds detector buffers safely"
    - "The Tauri cpal callback does not allocate, block, lock GuitarInput, or execute detector DSP"
    - "Calibration terminology does not promise measurements the persisted profile does not apply"
  artifacts:
    - path: "crates/contrapunk-audio/tests/guitar_corpus_acceptance.rs"
      provides: "Deterministic corpus evaluator and automated gates"
    - path: "crates/contrapunk-audio/tests/guitar_corpus_holdout.txt"
      provides: "Fixed untouched holdout partition"
    - path: "src-tauri/src/guitar_bridge.rs"
      provides: "Bounded callback-to-worker guitar processing"
  key_links:
    - from: "crates/contrapunk-audio/tests/guitar_corpus_acceptance.rs"
      to: "crates/contrapunk-audio/src/guitar_input.rs"
      via: "actual GuitarInput processing"
    - from: "src-tauri/src/guitar_bridge.rs"
      to: "crates/contrapunk-audio/src/guitar_input.rs"
      via: "worker-owned detector"
---

<objective>
Make the existing clean-monophonic guitar input measurable, correct, corpus-reliable, real-time safe on Tauri, and truthfully calibrated for the v1.3.0 release.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10.1-guitar-reliability/10.1-CONTEXT.md
@.planning/phases/10.1-guitar-reliability/10.1-RESEARCH.md
@crates/contrapunk-audio/src/guitar_input.rs
@src-tauri/src/guitar_bridge.rs
@src-tauri/src/commands/guitar.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add permanent corpus evaluator</name>
  <action>Evaluate all 138 checked-in WAV files through the real GuitarInput. Parse expected string/fret from filenames. Add an explicit fixed holdout list and report exact first note, eventual correct note, octave errors, retriggers, matching NoteOffs, latency percentiles, processing ratio, determinism, and per-string/fret partitions.</action>
  <verify><automated>cargo test -p contrapunk-audio --test guitar_corpus_acceptance -- --nocapture</automated></verify>
</task>

<task type="auto">
  <name>Task 2: Lock automated acceptance gates</name>
  <action>Assert >=95% exact first notes, <5% files with more than one NoteOn, 100% matching NoteOff cleanup, deterministic repeated event streams, no stuck notes/crashes, and total processing faster than corpus duration. Report development and holdout independently; gates apply to each partition.</action>
  <verify><automated>cargo test -p contrapunk-audio --test guitar_corpus_acceptance -- --nocapture</automated></verify>
</task>

<task type="auto">
  <name>Task 3: Fix confirmed correctness defects</name>
  <action>Fix first-note candidate selection, read previous slide cents before pushing current cents, compute vibrato frame duration from hop_size, emit one canonical bend representation, center stale bends during note cleanup, and replace unsafe structural config mutation with a rebuilding API. Add focused unit tests.</action>
  <verify><automated>cargo test -p contrapunk-audio --lib</automated></verify>
</task>

<task type="auto">
  <name>Task 4: Tune isolated-note detection</name>
  <action>Tune attack consensus, harmonic/subharmonic candidate scoring, octave correction, low-string handling, decay/retrigger suppression, and thresholds against development only. Run holdout after choices are fixed. Do not encode labels or file-specific exceptions.</action>
  <verify><automated>cargo test -p contrapunk-audio --test guitar_corpus_acceptance -- --nocapture</automated></verify>
</task>

<task type="auto">
  <name>Task 5: Move Tauri DSP off the cpal callback</name>
  <action>Use bounded ring buffers. The callback deinterleaves into the sample producer without heap allocation and returns. A named worker owns GuitarInput, applies safe config/profile changes, emits MIDI and signal updates, handles overflow with cleanup, and stops when the bridge is dropped.</action>
  <verify><automated>cargo check -p contrapunk-tauri && cargo test -p contrapunk-tauri commands::engine --lib</automated></verify>
</task>

<task type="auto">
  <name>Task 6: Make calibration truthful and unified</name>
  <action>Route persisted calibration through one authoritative application path and remove contradictory config behavior. Rename the visible workflow to Tune Guitar unless it measures and persists all DSP values claimed by Calibration. Wire browser guitar config rather than accepting it as a no-op.</action>
  <verify><automated>npm --prefix ui run check && cargo check -p contrapunk-tauri</automated></verify>
</task>

</tasks>

<verification>
- All targeted commands pass.
- `git diff --check` passes.
- No unrelated Elixir/Golem files are staged or modified by this plan.
- Product documentation and UI retain the clean-monophonic claim boundary.
</verification>

<output>
After completion, create `.planning/phases/10.1-guitar-reliability/10.1-01-SUMMARY.md` and update STATE/ROADMAP/REQUIREMENTS statuses.
</output>

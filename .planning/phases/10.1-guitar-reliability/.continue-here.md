---
phase: post-10.1-release-validation
task: 3
total_tasks: 4
status: paused
last_updated: 2026-07-19T08:06:59.197Z
---

<current_state>

Phase 10.1 automated acceptance is complete at commit `e7f51264`. Post-phase work is paused during optimized hardware validation and before starting a separately isolated, functioning native UI prototype.

The uncommitted 10 ms attack candidate passed the entire 108-file development gate: 108/108 exact, zero retriggers, clean deterministic releases, p50 16.0 ms, p95 26.7 ms. The sealed holdout has not been rerun.

The optimized Tauri app successfully opened the Audient iD14 on backend channel 1 (user-facing Input 2), but the user did not complete the played-note sequence before pause. No `[guitar-midi]` sustain trace was captured, so hardware latency/retrigger acceptance remains open. The managed release process was stopped.

A static concept at `/tmp/contrapunk-clean-guitar-design.html` established the approved direction: dense but calm, native system typography, monochrome structure, and color only when it directly represents sound. The user then clarified that the next deliverable must be a separate **functioning native prototype covering every Contrapunk feature**, not only Guitar Input and not a replacement of the current production UI.

</current_state>

<completed_work>

- Task 1: Frozen Phase 10.1 corpus acceptance — done in `e7f51264`.
- Task 2: Tested the development-only 10 ms attack candidate — 108/108 exact, zero retriggers, p95 26.7 ms.
- Task 3: In progress:
  - synchronized guitar device and configuration at the exact routing-start boundary;
  - corrected user-facing channel display to 1-based Input 2;
  - added stop/sync/restart behavior for live device/channel changes;
  - reset stale note, confidence, velocity, signal and clarity state on restart;
  - versioned persisted DSP settings so stale 5 ms analysis values are ignored;
  - fixed the intended profile at 1,024 samples/about 21 ms and removed misleading analysis/noise-gate controls;
  - separated raw PITCH from actual routed MIDI OUT status;
  - clarified Sustain level versus Release tail;
  - adopted legible native sans-serif typography with monospace technical readouts;
  - added debug-only guitar NoteOn/NoteOff logs and a sustained-beating no-retrigger regression;
  - removed the duplicate Arrangement Map and unused GuitarInputPanel selectors;
  - passed `cargo test -p contrapunk-audio --lib` (226 tests), `npm --prefix ui run check` (0 errors), the development corpus gate, and focused `git diff --check`.
- Design direction: user approved the monochrome density of the separate HTML concept.

</completed_work>

<remaining_work>

- Finish Task 3 with optimized real-hardware testing:
  - short note;
  - 3–5 second sustain;
  - repeated plucks;
  - bend and slide;
  - compare PITCH with MIDI OUT;
  - capture `[guitar-midi]`, `[guitar_bridge]`, and any CC123 logs;
  - confirm no worker-queue overflow cleanup during ordinary playing.
- Freeze or reject the 10 ms candidate from development plus hardware evidence. Do not rerun the sealed holdout before explicit authorization and final candidate freeze.
- Independently review and commit only the focused post-Phase-10.1 guitar/UI slice. Never broadly stage the dirty tree.
- Task 4: create a separate functioning native prototype for **all** Contrapunk features. Do not change/replace the production UI.
- After prototype/reliability work, complete standalone/browser/Logic/REAPER, sample-rate, block-size, device-loss, routing, installer, signing and notarization acceptance.

</remaining_work>

<decisions_made>

- The next UI is a separate functional native prototype, not an in-place production restyle.
- The prototype must include all Contrapunk features, not a Guitar-only workspace.
- Monochrome defines structure, hierarchy and idle state. Color is reserved for live input, emitted MIDI, harmony voices or other actual sound.
- Keep the piano roll. Add a continuous Guitar trace whose pitch position reveals bends/slides/vibrato, whose visual weight reflects dynamics, and whose opacity can reflect detection confidence. Keep discrete emitted MIDI visible for comparison.
- Use optimized Rust for guitar performance testing; debug DSP overflow invalidates latency and sustain observations.
- Keep analysis fixed at 1,024 samples/about 21 ms. The current 10 ms value is attack confirmation, not the analysis window.
- Keep holdout sealed until complete freeze.
- Preserve Elixir/Golem work but do not include it in v1.3.0 or the prototype without explicit re-scoping.

</decisions_made>

<blockers>

- Real-hardware sustain and latency acceptance still requires the user to play through Audient iD14 Input 2 in an optimized build.
- External MIDI routing most recently reported zero connections.
- The working tree contains extensive unrelated and mixed-scope changes. `.pi-subagents/` must never be staged; generated WASM, Golem, Elixir, AU and other dirty work must not leak into focused commits.
- Existing 250 ms fixtures do not certify long sustain, broad hardware generalization or polyphonic guitar.
- Final release still requires user-operated DAW, installer, signing and notarization checks.

</blockers>

<context>

Start the prototype with product architecture, not component styling. Inventory the real capabilities already exposed by `ContrapunkAdapter` and group them into a quiet native information hierarchy. Reuse existing backend behavior; do not duplicate harmony or routing logic. The HTML concept is only a visual seed and remains at `/tmp/contrapunk-clean-guitar-design.html`.

The current production tree is highly dirty. Full details and an exact categorized file list are in `.planning/HANDOFF.json`. Inspect `git status`, use explicit paths/hunks, and inspect `git diff --cached` before every commit.

The native prototype should remain observable: input source, raw sound, converted MIDI, generated voices and final output must be distinguishable without decorative dashboards. Silence should look monochrome and calm; sound may introduce role-specific color.

</context>

<next_action>

Start with: inventory every existing Contrapunk feature/capability and propose the smallest separate native prototype boundary and top-level navigation. Confirm that architecture with the user before implementation. Do not modify the current production UI. After that, implement the native shell using existing adapters/backend paths, carrying forward the approved monochrome/sound-color rules and piano-roll expression concept.

</next_action>

---
phase: 06.2-voice-leading
plan: 03
type: execute
wave: 3
depends_on: ["06.2-02"]
files_modified:
  - src/gui/app.rs
  - src/wasm_app.rs
autonomous: true

must_haves:
  truths:
    - "User can toggle voice leading on/off via GUI"
    - "User can select voice leading style from GUI dropdown"
    - "Voice leading controls appear in both native and WASM builds"
    - "Works with all existing harmony modes"
  artifacts:
    - path: "src/gui/app.rs"
      provides: "Voice leading toggle and style selector in GUI"
      contains: "voice_leading"
    - path: "src/wasm_app.rs"
      provides: "Voice leading controls in WASM GUI"
      contains: "voice_leading"
  key_links:
    - from: "src/gui/app.rs"
      to: "src/harmony/engine.rs"
      via: "set_voice_leading_enabled, set_voice_leading_style"
      pattern: "set_voice_leading"
    - from: "src/wasm_app.rs"
      to: "src/harmony/engine.rs"
      via: "set_voice_leading_enabled, set_voice_leading_style"
      pattern: "set_voice_leading"
---

<objective>
Add voice leading controls to the GUI (native and WASM) so users can toggle voice leading and select counterpoint styles.

Purpose: Makes voice leading accessible to users via the existing GUI interface.
Output: Voice leading toggle checkbox and style dropdown in both native and WASM builds.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06.2-voice-leading/06.2-02-SUMMARY.md
@src/gui/app.rs
@src/wasm_app.rs
@src/harmony/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add voice leading controls to native GUI</name>
  <files>src/gui/app.rs</files>
  <action>
Add voice leading controls in the GUI, following the existing pattern used for OctaveMode and HarmonyMode selectors:

1. Add `voice_leading_enabled: bool` and `voice_leading_style: VoiceLeadingStyle` fields to the app state (or wherever OctaveMode state is stored -- follow existing pattern).
2. In the settings/controls panel, add a new section "Voice Leading" (using CollapsingHeader like the humanization controls from Phase 6.1):
   - Checkbox: "Enable Voice Leading" bound to `voice_leading_enabled`.
   - ComboBox/dropdown: "Style" with VoiceLeadingStyle::all() options, using `.description()` for display text. Only shown when voice leading is enabled.
3. When checkbox changes: call `engine.set_voice_leading_enabled(value)` on the shared HarmonyEngine state (follow existing pattern for how OctaveMode changes are communicated to the engine -- likely through shared state mutex or message channel).
4. When style changes: call `engine.set_voice_leading_style(style)`.
5. Import VoiceLeadingStyle from harmony module.

Follow the EXACT same state-sharing pattern used for octave mode and humanization controls. Do NOT introduce a new pattern.
  </action>
  <verify>
`cargo build --features gui` compiles. Run the app and verify voice leading checkbox and style dropdown appear in the GUI.
  </verify>
  <done>
Voice leading toggle and style selector visible in native GUI. Changes propagate to HarmonyEngine.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add voice leading controls to WASM GUI</name>
  <files>src/wasm_app.rs</files>
  <action>
Mirror the native GUI controls in the WASM app:

1. Add `voice_leading_enabled: bool` and `voice_leading_style: VoiceLeadingStyle` fields to ContrapunkApp (or wherever the WASM app stores its state).
2. In the WASM GUI panel, add "Voice Leading" section matching native GUI:
   - Checkbox for enable/disable.
   - Style dropdown (ComboBox) shown when enabled.
3. Wire to the HarmonyEngine instance (in WASM, the engine is owned directly by ContrapunkApp, not behind Arc<Mutex>).
4. Call `self.engine.set_voice_leading_enabled()` and `self.engine.set_voice_leading_style()` when controls change.

Follow the existing WASM GUI patterns exactly (how octave mode and humanization controls are rendered in wasm_app.rs).
  </action>
  <verify>
`trunk build` compiles the WASM build. `cargo build --features gui` still compiles. Both native and WASM have matching voice leading controls.
  </verify>
  <done>
Voice leading controls appear in WASM build matching native GUI. Both builds compile. Controls wire to HarmonyEngine.
  </done>
</task>

</tasks>

<verification>
- `cargo build --features gui` compiles (native)
- `trunk build` compiles (WASM)
- Voice leading checkbox and style dropdown visible in both GUIs
- Controls properly wire to HarmonyEngine methods
</verification>

<success_criteria>
- Voice leading toggle visible and functional in native and WASM GUI
- Style selector shows all 4 styles (Palestrina, Bach Chorale, Jazz, Free)
- Controls follow existing GUI patterns (CollapsingHeader, ComboBox)
- Both builds compile cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/06.2-voice-leading/06.2-03-SUMMARY.md`
</output>

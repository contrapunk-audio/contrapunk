---
phase: "06.8"
plan: "01"
title: "Fix WASM Build and Improve CI Caching"
subsystem: "ci-deployment"
tags: [github-actions, wasm, trunk, ci, caching]
dependency-graph:
  requires: ["05.1"]
  provides: ["wasm-deploy-fix", "ci-pipeline"]
  affects: ["all-future-phases"]
tech-stack:
  added: ["Swatinem/rust-cache@v2"]
  patterns: ["conditional-install-on-cache-miss", "parallel-ci-jobs"]
key-files:
  created: [".github/workflows/ci.yml"]
  modified: [".github/workflows/deploy.yml", "src/theme/colors.rs", "src/piano.rs", "src/server/session.rs", "index.html"]
  deleted: ["src/tabs/craft.rs", "src/tabs/mod.rs", "src/tabs/play.rs", "src/tabs/settings.rs"]
decisions:
  - id: "cache-strategy"
    description: "Use Swatinem/rust-cache@v2 instead of manual actions/cache for Rust deps"
  - id: "trunk-cache"
    description: "Cache Trunk binary with version-keyed key (trunk-OS-v0.21)"
  - id: "ci-jobs"
    description: "Three parallel CI jobs: check, test, wasm-check"
metrics:
  duration: "3 min"
  completed: "2026-02-02"
---

# Phase 06.8 Plan 01: Fix WASM Build and Improve CI Caching Summary

**One-liner:** Committed all pending source changes fixing WASM deploy, replaced manual cargo cache with Swatinem/rust-cache, added Trunk binary caching, and created 3-job CI pipeline.

## What Was Done

### Task 1: Commit pending source changes (f84ebbd)
Committed 8 files that were modified/deleted in working tree but never committed, causing WASM deploy failures. This includes the PICO_INDIGO color constant, piano/session updates, index.html changes, and removal of obsolete tab files.

### Task 2: Improve deploy workflow caching (3f0f3ae)
Replaced manual actions/cache block with Swatinem/rust-cache@v2 (shared-key: wasm). Added Trunk binary cache with version-keyed key so cargo install trunk is skipped on cache hit.

### Task 3: Add CI workflow (6fb97bf)
Created .github/workflows/ci.yml with three parallel jobs:
- **check**: cargo check for fast compile validation
- **test**: cargo test for unit tests
- **wasm-check**: cargo check --target wasm32-unknown-unknown for WASM compile verification

All jobs use Swatinem/rust-cache@v2 with appropriate shared keys.

### Task 4: Local verification
- `trunk build --release` succeeds (dist/ contains index.html and .wasm)
- `cargo test` passes (0 failures)

## Commits

| Task | Commit | Message |
|------|--------|---------|
| 1 | f84ebbd | fix(06.8-01): commit pending source changes to fix WASM build |
| 2 | 3f0f3ae | chore(06.8-01): improve deploy workflow caching |
| 3 | 6fb97bf | feat(06.8-01): add CI workflow for PRs and main |

## Deviations from Plan

None - plan executed exactly as written.

## Decisions Made

1. **Swatinem/rust-cache over manual cache** - More robust, handles cache invalidation automatically
2. **Trunk version-keyed cache** - trunk-OS-v0.21 key allows easy cache bust on Trunk upgrades
3. **Three parallel CI jobs** - check/test/wasm-check run independently for faster feedback

## Verification Results

- [x] `trunk build --release` succeeds locally
- [x] `cargo test` passes
- [x] `.github/workflows/deploy.yml` uses rust-cache and cached Trunk
- [x] `.github/workflows/ci.yml` exists with check, test, wasm-check jobs
- [x] No PICO_INDIGO or missing file errors in WASM build

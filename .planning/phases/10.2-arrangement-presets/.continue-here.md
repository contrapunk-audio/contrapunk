---
phase: 10.2-arrangement-presets
wave: 2
preset: 27-quartal-colossus
status: active
last_updated: 2026-07-20T09:46:00Z
loop_id: 1
---

<current_state>

Preset 27 Quartal Colossus has three independent cited reports and a parent synthesis committed in:

- `0425296 docs(presets): research quartal colossus`
- `.planning/phases/10.2-arrangement-presets/research/27-quartal-colossus/`

Eight baseline presets are operational: 2, 4, 7, 8, 12, 14, 23, and 25. Remaining order: 27, 36, 43, 48.

</current_state>

<approved_scope>

The approved preset is a static shared-HarmonyEngine mapping:

- Dorian;
- DiatonicFourths;
- four total voices;
- `voicePosition: 3`, so the exact source is the bottom note and three chained voices lie above;
- voice leading off, octave mode None, interchange off, Companion off;
- transport optional and preserved.

For Dorian degrees 1, 2, 5, and 6, output relative vectors are exactly `[0,5,10,15]`. Degrees flat-3, 4, and flat-7 are disclosed diatonic fourth-derived variants containing one augmented fourth. Copy must not call every stack identical or all-perfect-fourth.

The performer supplies rhythm, accents, register, repetition, dynamics, phrase/section development, resolution, and silence. The preset does not generate patterns, bass/pedal roles, comping decisions, chromatic resolution, adaptive form, ensemble interaction, or artist identity.

</approved_scope>

<next_action>

1. Add `QUARTAL_COLOSSUS_PRESET` to the catalog with the exact synthesized config and honest copy.
2. Add the smallest HarmonyEngine regression covering all seven Dorian vectors, exact-source-once/source-lowest placement, matching NoteOff sets, chromatic fallback outside corpus, and empty active state.
3. Add catalog assertions and the approved-ID expectation.
4. Run:
   - focused harmony test;
   - `cargo test -p contrapunk-harmony --lib`;
   - catalog/persistence Node tests;
   - `npm --prefix ui run check`.
5. Commit implementation and checkpoint state separately.
6. Only then begin preset 36 research.

</next_action>

<git_safety>

The tree contains unrelated Golem, Elixir, AU wrapper, recon, docs, and `.pi-subagents/` work. Stage only explicit preset, harmony-test, and Phase 10.2 paths. Never stage `.pi-subagents/`.

All subagents and research must use `openai-codex/gpt-5.6-sol`; never use another provider. Concurrent Codex startup showed the prior credential race; sequential retries with the same model succeeded.

</git_safety>

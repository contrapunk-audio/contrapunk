---
phase: 10.2-arrangement-presets
wave: 2
preset: 36-pixel-trio
status: active
last_updated: 2026-07-20T10:48:00Z
loop_id: 1
---

<current_state>

Preset 36 Pixel Trio has three independent cited reports and a parent synthesis committed in:

- `d31d84d docs(presets): research pixel trio`
- `.planning/phases/10.2-arrangement-presets/research/36-pixel-trio/`

Decision: `keep_locked`. Nine baseline presets remain operational.

</current_state>

<decision>

The NES has five unequal hardware sources; “trio” is a bounded three-pitched-role abstraction. Pixel Trio’s defining roles are live lead, independently timed low support, and independently timed counter/support. Current HarmonyEngine outputs share every source onset and release. Diatonic blocks, contrary pitch motion, Strict Counterpoint, or a fixed delayed echo cannot turn synchronous shadows into independent roles.

A generic three-pitch vertical exercise may be useful, but it is not Pixel Trio. Do not weaken the name/result to force the operational count.

Unlocking requires reusable `pattern_lane` and `stable_lane_groups` capabilities with independent role timing/rests, register bounds, density states, transport scheduling, and exact ownership cleanup. Do not add a preset-specific scheduler.

</decision>

<next_action>

1. Replace the preset 36 pending draft with a research-complete blocked record.
2. Use target copy from `synthesis.md`; explain that current synchronous harmony cannot supply independent low-support/counterline timing.
3. Declare `pattern_lane` and `stable_lane_groups` requirements and keep Apply disabled while the preset remains visible.
4. Add catalog assertions for blocked status, missing capabilities, honest copy, and preserved performance environment.
5. Run catalog/persistence Node tests and `npm --prefix ui run check`.
6. Commit the locked catalog record and checkpoint state.
7. Begin preset 43 research; operational count remains 9/12.

</next_action>

<git_safety>

The tree contains unrelated Golem, Elixir, AU wrapper, recon, docs, and `.pi-subagents/` work. Stage only explicit Phase 10.2 paths.

All subagents and research must use `openai-codex/gpt-5.6-sol`.

</git_safety>

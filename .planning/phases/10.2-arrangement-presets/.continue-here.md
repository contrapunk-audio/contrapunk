---
context: phase
phase: 10.2-arrangement-presets
plan: 3
task: 6
task_name: preset-13-axis-mirror-research
total_tasks: 10
status: paused
last_updated: 2026-07-21T10:54:53.741Z
loop_id: 1
---

# Phase 10.2 Handoff — Axis Mirror Research Next

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Research provider drift | Earlier concurrent launches failed on transport or missing Codex credentials. | advisory | Discover executable agents first, require `openai-codex/gpt-5.6-sol`, validate artifacts, retry sequentially if needed, and stop after three failures. |
| Realtime symptom patching | Unoptimized development DSP fell behind realtime, overflowed the guitar worker queue, and emitted CC123 resets. | advisory | Preserve the `contrapunk-audio` dev optimization; do not weaken detector thresholds or note ownership. |

<current_state>

Phase 10.2 Wave 3 is paused before any work on preset 13 **Axis Mirror**.

Thirteen research-backed presets are operational:

`2, 4, 7, 8, 12, 14, 23, 25, 27, 36, 43, 48, 1`

Preset 01 **Cloister Organum** shipped in `337d3c97` through the reusable `ExplicitIntervals` HarmonyMode. It includes exact NoteOn/NoteOff ownership, final-pitch routing, Tauri/WASM persistence and controls, plugin capability gating, and a bounded source-plus-one-voice Dorian P4/P5/P8 mapping.

The user-approved 12-preset checkpoint is cleared. No Axis Mirror report, synthesis, or implementation exists yet.

The product repository is clean on `main` at `9a5ace4e`, synchronized with `origin/main`. Since Phase 10.2 was parked, the production workspace, universal plug-ins/AUs, Guitar AU telemetry, approved-only arrangement menu, Holdsworth HAL versioning, dependency upgrades, app deployment, and Starlight user guide were completed. Direct VST3 hosting remains parked under Phase 21.C3.

</current_state>

<completed_work>

- Arrangement V2 schema, centralized snapshot/apply, persistence migration, capability gating, and lifecycle safety are complete.
- Wave 2's first 12 presets are operational with three cited reports, parent synthesis, deterministic lifecycle evidence, and honest public copy for each.
- Wave 3 began with Cloister Organum; its reusable explicit-interval capability is operational across supported surfaces.
- `7aca7ca` fixed the Tauri development guitar throughput regression without changing detector thresholds. The two-second sustain check improved from 6.09 s to 0.26 s; 226 audio tests passed; the user confirmed continuous hardware sustain.
- `2bf78070` ensures production menus show only approved built-ins plus custom presets.
- Release preparation through `9a5ace4e` is committed and clean; the public v1.3.0 tag does not exist.

</completed_work>

<remaining_work>

### Immediate Phase 10.2 slice

1. Discover executable subagents.
2. Launch exactly three fresh-context, read-only `openai-codex/gpt-5.6-sol` reports for **Axis Mirror**:
   - history, scoped corpus, and primary sources;
   - theory, inversion axis, voice leading, and temporal behavior;
   - performer input contract, interaction, and caricatures to avoid.
3. Validate that all three reports are independent, cited, nonempty, and satisfy `10.2-RESEARCH-TEMPLATE.md`.
4. Parent-synthesize them into `research/13-axis-mirror/synthesis.md`.
5. Only if synthesis approves a bounded capability, implement the smallest reusable inversion strategy with real controls on supported surfaces, capability gates elsewhere, persistence/apply/Save As round-trip, and exact NoteOn/NoteOff cleanup.
6. Continue Waves 3-7 after Axis Mirror.

### Separate v1.3.0 release lane

- Remove and ignore 376 tracked `.pi-subagents/` runtime artifacts without touching other `.planning` files.
- Refresh public README, contributing, installation, DAW-routing, and release documentation.
- Build and verify six release-candidate assets: macOS DMG, Windows app installer, macOS plug-in PKG/ZIP, and Windows plug-in installer/ZIP.
- User performs final Logic MIDI FX and Guitar Audio FX routing acceptance.
- Produce a go/no-go report, then stop for explicit approval before creating or pushing `v1.3.0`.
- Keep website release data on v1.2.0 until all six v1.3.0 assets exist.

</remaining_work>

<decisions_made>

- Every operational preset requires three independent cited reports and a parent synthesis before implementation.
- Use only `openai-codex/gpt-5.6-sol` for research and all other subagent work.
- Axis Mirror must reuse a shared HarmonyEngine strategy; no preset-specific inversion branch.
- Every new tunable capability gets a real Setup control on supporting surfaces and explicit gating elsewhere.
- Arrangement Apply preserves tonic, BPM, transport, devices, routing, sound, master, mute/solo, and plugins.
- Only `researchStatus === 'approved'` built-ins appear in production menus; custom presets remain visible.
- Exact NoteOn/NoteOff ownership and zero-orphan cleanup remain mandatory.
- v1.3.0 is **Holdsworth HAL**; no tag or release publication before the release-candidate report and human approval.

</decisions_made>

<blockers>

- Axis Mirror implementation is gated on three independent reports and parent synthesis; this is intentional, not an environmental blocker.
- Final Logic guitar audio → pitch detection → virtual CoreMIDI → instrument routing acceptance is manual. Do not operate Logic's UI.
- Creating/pushing `v1.3.0` is blocked on explicit human approval after the release-candidate report.
- Windows signing secrets are not currently listed; do not describe Windows artifacts as signed unless verification proves it.

</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable current position, release side lane, decisions, and blockers.
2. `.planning/phases/10.2-arrangement-presets/10.2-STATE.json` — authoritative per-preset evidence and commands.
3. `.planning/phases/10.2-arrangement-presets/10.2-RESEARCH-TEMPLATE.md` — mandatory report and synthesis contract.
4. `.planning/phases/10.2-arrangement-presets/10.2-PRESET-CATALOG.md` — Axis Mirror draft intent and capability placement.
5. `.planning/phases/10.2-arrangement-presets/10.2-CONTEXT.md` — phase boundaries and preserved environment state.
6. `.planning/phases/10.2-arrangement-presets/10.2-LOOP.md` — iteration, verification, reflection, and stop rules.
7. `CLAUDE.md` — repository surfaces, test commands, and footguns.

## Critical Anti-Patterns (do NOT repeat these)

- Do not implement an inversion algorithm before the three-report gate and parent synthesis.
- Do not encode Axis Mirror behavior in preset-specific Rust branches; add one reusable strategy only if research requires it.
- Do not ship backend-only tunables or silent no-ops on unsupported surfaces.
- Do not alter tonic, BPM, transport, devices, routing, sound, master, mute/solo, or plugins during Arrangement Apply.
- Do not stage `.pi-subagents/` with Phase 10.2 work. If release cleanup removes it, make that a separate coherent commit.
- Do not weaken guitar detector thresholds to hide throughput or lifecycle failures.
- Do not resume generic direct VST3 hosting; it is parked under Phase 21.C3.

## Infrastructure State

- Product repo: `/Users/vibhavbobade/go/src/github.com/contrapunk-audio/contrapunk`
- Branch/head: clean `main` at `9a5ace4e`, equal to `origin/main`.
- Website repo: `/Users/vibhavbobade/go/src/github.com/contrapunk-audio/website`, last known clean at `f9a743c`.
- Live app and docs: `https://app.contrapunk.com` and `https://contrapunk.com/docs/` return HTTP 200.
- Current public release remains v1.2.0; no v1.3.0 tag/release exists.
- Managed background processes: none running.
- Installed Logic components: `Contrapunk.component` MIDI FX and `Contrapunk Guitar.component` Audio FX.
- 376 `.pi-subagents/` runtime artifacts are tracked; cleanup belongs to the release lane, not the Axis Mirror research commit.
- Older SUMMARY files outside Phase 10.2 contain placeholder/TBD matches; none are current Phase 10.2 completion evidence.

<context>

The shortest safe next move is research only. Do not design the inversion axis from the preset title. Let the three reports establish the bounded historical/theoretical claim, distinguish static pitch reflection from tonal negative harmony or invertible counterpoint, define performer-owned rhythm/form, and say what the current engine can honestly approximate. The parent then chooses implementation, lock, or a narrower preset claim.

Keep Phase 10.2 and release work as separate commits. The release loop is gated immediately before tagging; Axis Mirror is gated immediately before implementation.

</context>

<next_action>

Start with: inspect configured subagents, then launch three independent fresh-context `openai-codex/gpt-5.6-sol` research reports for preset 13 **Axis Mirror**. Validate all three artifacts before writing parent synthesis. Do not implement inversion behavior first.

</next_action>

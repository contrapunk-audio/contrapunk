---
phase: 10.2-arrangement-presets
wave: 2
preset: 36-pixel-trio
status: awaiting-user-performance-checkpoint
last_updated: 2026-07-20T11:42:17Z
loop_id: 1
---

<current_state>

The first 12 research-backed baselines are operational:

`2, 4, 7, 8, 12, 14, 23, 25, 27, 36, 43, 48`

Preset 36 Pixel Trio is complete:

- research: `d31d84d docs(presets): research pixel trio`
- original honest capability lock: `32d90ef docs(presets): mark pixel trio capability-locked`
- reusable scheduler + operational preset: `b1b80eb feat(arrangements): add reusable pattern lanes`
- research pack: `.planning/phases/10.2-arrangement-presets/research/36-pixel-trio/`

The implementation adds two generic transport-aware PatternLane instances, `pattern_low` and `pattern_counter`, with declarative scale-degree events, stable role IDs, independent rests/registers, bounded tails, and exact lifecycle cleanup. Tauri and WASM are wired; plugin capability gating remains honest.

Verification: Companion 76 passed, Tauri 36 passed, WASM cargo check passed, catalog/persistence 15 passed, and UI check has zero errors with 29 pre-existing warnings.

</current_state>

<hard_stop>

The mandatory 12-preset user performance checkpoint is now active. Do not enter Wave 3 until the user tests the baselines and explicitly approves continuation.

Pixel Trio test contract:

- use the prototype with transport running;
- play a sparse monophonic middle-register hook;
- expect tonic/fifth low support and a complementary upper answer on independent beat positions;
- expect brief one-, two-, and three-role density rather than a constant three-note block;
- stop transport, switch presets, disable Companion, and press Panic to confirm no stuck notes.

</hard_stop>

<git_safety>

The tree still contains unrelated Golem, Elixir, AU wrapper, recon, docs, and `.pi-subagents/` work. Preserve it. All research/review subagents used `openai-codex/gpt-5.6-sol`.

</git_safety>

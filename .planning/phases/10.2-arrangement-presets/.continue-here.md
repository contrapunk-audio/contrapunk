---
phase: 10.2-arrangement-presets
wave: 2
preset: 25-bebop-chase
status: active
last_updated: 2026-07-20T09:01:19Z
loop_id: 1
---

<current_state>

Phase 10.2 is executing through saved Loop #1, **Complete Contrapunk’s research-backed arrangement preset catalog**.

Preset 25 Bebop Chase has three independent cited reports and a parent synthesis committed in:

- `86b3973 docs(presets): research bebop chase`
- `.planning/phases/10.2-arrangement-presets/research/25-bebop-chase/`

Seven baseline presets are operational: 2, 4, 7, 8, 12, 14, and 23. The remaining Wave 2 order is 25, 27, 36, 43, and 48.

</current_state>

<approved_scope>

The synthesis rejects the original “shortened answer” claim. Current Free Imitation has no phrase capture, phrase-end detection, motif selection, truncation, swing generation, chord targeting, form awareness, or role-aware response.

The approved bounded exercise is:

- PassThrough player voice;
- one Free Imitation response voice;
- 4-beat delay;
- +7 scale degrees (one scale octave);
- `timeRatio: 1.0` so every source attack and gate is retained;
- `velocityScale: 0.82`;
- 4-beat NearFuture tail so released source notes do not erase the scheduled response;
- performer plays four to six in-scale eighth notes within at most three beats, then leaves response space.

Public copy must call this a complete delayed octave answer, not a shortened bebop reply, trading fours, phrase recognition, or Parker/Gillespie simulation.

</approved_scope>

<next_action>

1. Inspect the existing approved catalog entries and exact serializer field casing.
2. Activate preset 25 with only the shared PassThrough and Free Imitation configuration above.
3. Add the smallest regression proving a four-note in-scale source produces exactly four delayed response NoteOns and four matched NoteOffs, retains pending answers after source release, and ends with no owned notes.
4. Update catalog validation expectations and public copy without broadening the synthesis claim.
5. Run:
   - focused Companion lifecycle test;
   - `cargo test -p contrapunk-companion --lib`;
   - catalog/persistence Node tests;
   - `npm --prefix ui run check`.
6. Commit implementation and checkpoint state separately.
7. Only then begin the three-report gate for preset 27.

</next_action>

<git_safety>

The tree contains unrelated Golem, Elixir, AU wrapper, recon, docs, and `.pi-subagents/` work. Never stage broadly and never stage `.pi-subagents/`. Use only explicit preset/catalog/test and Phase 10.2 paths.

All subagents and research must use `openai-codex/gpt-5.6-sol`; never fall back to OpenRouter or another model.

</git_safety>

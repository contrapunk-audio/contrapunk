---
phase: 06.1-humanization-ui-fix
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/app.rs]
autonomous: true

must_haves:
  truths:
    - "WASM build processes harmony notes through Humanizer engine before sending to Web MIDI"
    - "Delayed notes from jitter/swing are drained each frame and sent to Web MIDI outputs"
    - "Changing humanization sliders in WASM immediately affects note output"
    - "Metronome clicks send on MIDI channel 10 in WASM when enabled"
  artifacts:
    - path: "src/app.rs"
      provides: "WASM humanizer integration"
      contains: "wasm_humanizer"
  key_links:
    - from: "src/app.rs (handle_wasm_note_on)"
      to: "Humanizer::humanize_note_on"
      via: "direct call on wasm_humanizer field"
      pattern: "wasm_humanizer\\.humanize_note_on"
    - from: "src/app.rs (update)"
      to: "DelayQueue::pop_ready"
      via: "frame-based drain loop"
      pattern: "wasm_delay_queue\\.pop_ready"
    - from: "src/app.rs (update)"
      to: "wasm_humanizer.update_config"
      via: "per-frame config sync"
      pattern: "wasm_humanizer\\.update_config"
---

<objective>
Wire the existing Humanizer and DelayQueue into the WASM MIDI processing path so humanization effects (timing jitter, velocity variation, groove, metronome) actually work in the browser.

Purpose: Currently the WASM path completely bypasses humanization -- notes go directly from harmony engine to Web MIDI output. This is the core bug making humanization inaudible in-browser.
Output: Modified src/app.rs with WASM humanizer fields, note processing through humanizer, frame-based delay queue drain, config sync, and metronome integration.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.1-humanization-ui-fix/06.1-RESEARCH.md
@src/app.rs
@src/humanize/engine.rs
@src/humanize/scheduler.rs
@src/humanize/config.rs
@src/humanize/beat_clock.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add WASM humanizer fields and config sync</name>
  <files>src/app.rs</files>
  <action>
  Add two new fields to ContrapunkApp, gated with #[cfg(target_arch = "wasm32")]:
  - `wasm_humanizer: Humanizer` (from src/humanize/engine.rs)
  - `wasm_delay_queue: DelayQueue` (from src/humanize/scheduler.rs)

  Initialize them in the constructor (Default or new) with default Humanizer and DelayQueue.

  In the update() method, add a wasm32-gated block that:
  1. Syncs humanize_config to wasm_humanizer: `self.wasm_humanizer.update_config(self.humanize_config.clone())`
  2. Ticks the beat clock on wasm_humanizer if it has one (check engine.rs API)
  3. Drains the delay queue each frame:
     ```rust
     let now = web_sys::window()
         .and_then(|w| w.performance())
         .map(|p| p.now())
         .unwrap_or(0.0);
     while let Some(scheduled) = self.wasm_delay_queue.pop_ready(now) {
         // Send the scheduled note via web MIDI output
         // Use the same send pattern as handle_wasm_note_on/off
     }
     ```
  4. Check for metronome beat crossings and send MIDI channel 10 notes if metronome_enabled

  IMPORTANT: Use f64 milliseconds everywhere (no std::time::Instant -- not available in WASM). The existing Humanizer and DelayQueue already use f64 ms by design decision from Phase 6.
  </action>
  <verify>Run `cargo build --target wasm32-unknown-unknown --features gui` (or the project's WASM build command) and confirm it compiles without errors.</verify>
  <done>ContrapunkApp has wasm_humanizer and wasm_delay_queue fields. Config syncs each frame. Delay queue drains each frame. Metronome beat crossings checked each frame.</done>
</task>

<task type="auto">
  <name>Task 2: Route WASM harmony notes through humanizer</name>
  <files>src/app.rs</files>
  <action>
  Modify handle_wasm_note_on (approximately lines 420-472) to process harmony notes through the humanizer:

  For each note produced by the harmony engine:
  - Index 0 (melody): Pass through unchanged to Web MIDI output (existing behavior)
  - Index 1+ (harmony): Call `self.wasm_humanizer.humanize_note_on(note, channel, velocity, port_index)` to get a HumanizedNote with possibly modified velocity and a delay_ms value
    - If delay_ms == 0: Send immediately via Web MIDI
    - If delay_ms > 0: Push to `self.wasm_delay_queue` with timestamp = now_ms + delay_ms

  Modify handle_wasm_note_off similarly:
  - Index 0: Pass through unchanged
  - Index 1+: Call `self.wasm_humanizer.humanize_note_off(note, channel, port_index)` to get the matching jitter delay for the Note-Off (the existing design stores Note-On jitter in active_humanization HashMap so Note-Off inherits it)
    - If delay_ms == 0: Send immediately
    - If delay_ms > 0: Push to delay queue

  Get current time via: `web_sys::window().and_then(|w| w.performance()).map(|p| p.now()).unwrap_or(0.0)`

  Check the exact Humanizer API in engine.rs -- the method signatures may differ slightly from above. Match whatever the existing API provides. Do NOT modify the Humanizer struct itself.
  </action>
  <verify>Run `cargo build --target wasm32-unknown-unknown --features gui` and confirm compilation. Then run `trunk serve` (or project's dev server) and verify in browser: enable humanization, play notes, confirm harmony notes have velocity variation (visible in any MIDI monitor) and timing jitter (audible as slight delays).</verify>
  <done>WASM harmony notes pass through Humanizer. Velocity variation applies immediately. Timing jitter causes delayed sends via delay queue. Note-Off inherits Note-On jitter. Melody (index 0) unchanged.</done>
</task>

</tasks>

<verification>
1. `cargo build --target wasm32-unknown-unknown --features gui` compiles cleanly
2. `cargo build --features gui` (native) still compiles cleanly -- no regressions
3. `cargo test` passes all existing tests
4. In browser: enable humanization + jitter, play notes, hear timing variation on harmony notes
5. In browser: enable velocity variation, observe velocity changes on harmony notes
6. In browser: enable metronome, hear click on beat
</verification>

<success_criteria>
- WASM build compiles without errors
- Native build unaffected (no regressions)
- Humanization effects audible in browser
- Config changes from UI sliders take effect in WASM within one frame
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-humanization-ui-fix/06.1-01-SUMMARY.md`
</output>

---
phase: 06.1-humanization-ui-fix
plan: 02
type: execute
wave: 2
depends_on: ["06.1-01"]
files_modified: [src/app.rs]
autonomous: false

must_haves:
  truths:
    - "Humanization controls are visible without scrolling on typical browser viewport (768px+)"
    - "Each humanization sub-section (Beat Clock, Timing, Velocity, Duration, Swing) is collapsible"
    - "UI layout is consistent between native and WASM builds"
  artifacts:
    - path: "src/app.rs"
      provides: "Redesigned humanization UI with CollapsingHeaders"
      contains: "CollapsingHeader"
  key_links:
    - from: "src/app.rs (UI rendering)"
      to: "egui::CollapsingHeader"
      via: "collapsible sections for humanization controls"
      pattern: "CollapsingHeader::new"
---

<objective>
Redesign the humanization UI controls using egui CollapsingHeader sections so controls are visible and organized in both native and browser builds.

Purpose: Humanization controls are pushed below the visible area on browser viewports because preceding UI content (key, mode, octave, ports) takes too much vertical space. CollapsingHeaders reduce default height and improve discoverability.
Output: Redesigned humanization section in src/app.rs with collapsible sub-sections.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.1-humanization-ui-fix/06.1-RESEARCH.md
@src/app.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign humanization UI with CollapsingHeaders</name>
  <files>src/app.rs</files>
  <action>
  Replace the current flat humanization controls layout (approximately lines 669-720) with collapsible sections:

  1. Keep the top-level "Humanization" heading and enable checkbox
  2. When enabled, show these CollapsingHeader sections:
     - "Beat Clock" (default_open: true): BPM slider, metronome checkbox
     - "Timing Jitter" (default_open: false): jitter enable checkbox, min/max ms sliders
     - "Velocity" (default_open: false): velocity variation enable, range slider
     - "Duration" (default_open: false): duration variation enable, range slider
     - "Swing/Groove" (default_open: false): swing enable, amount slider

  3. Also wrap other SidePanel sections in CollapsingHeaders to further reduce vertical space:
     - "MIDI Configuration" (default_open: true): input/output port selectors
     - "Harmony" (default_open: true): key, mode, octave variation selectors
     - "Humanization" (default_open: true): the humanization controls above

  4. Ensure the ScrollArea has `auto_shrink(false)` so the scrollbar appears when content exceeds viewport.

  Use `egui::CollapsingHeader::new("Section Name").default_open(bool).show(ui, |ui| { ... })` pattern.

  Do NOT change any humanization config field names or types -- only restructure the UI layout.
  </action>
  <verify>Run `cargo build --features gui` for native and `cargo build --target wasm32-unknown-unknown --features gui` for WASM. Both must compile. Run `trunk serve` and verify in browser that humanization controls are visible and collapsible.</verify>
  <done>Humanization controls use CollapsingHeader sections. All sub-sections collapse/expand. Controls visible on 768px+ viewport without scrolling to bottom. Native and WASM layouts match.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete humanization UI fix: WASM humanizer integration (Plan 01) and redesigned collapsible UI controls (Plan 02 Task 1). Humanization effects should now be audible in the browser, and controls should be visible and well-organized.</what-built>
  <how-to-verify>
    1. Open the app in browser (trunk serve or contrapunk.fly.dev after deploy)
    2. Verify humanization controls are visible in the side panel without excessive scrolling
    3. Verify CollapsingHeader sections expand/collapse for Beat Clock, Timing, Velocity, Duration, Swing
    4. Enable humanization and play some notes -- harmony notes should have velocity variation
    5. Enable timing jitter -- harmony notes should have slight timing offsets
    6. Enable metronome -- should hear click on beat (if external synth connected to channel 10)
    7. Compare with native build -- behavior should be consistent
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. Both native and WASM builds compile without errors
2. All existing tests pass
3. Humanization controls visible in browser viewport
4. CollapsingHeaders work correctly (expand/collapse)
5. Humanization effects audible in both native and WASM
</verification>

<success_criteria>
- Controls visible on 768px+ browser viewport
- All sub-sections collapsible
- No regression in native build
- Human approves UI layout and functionality
</success_criteria>

<output>
After completion, create `.planning/phases/06.1-humanization-ui-fix/06.1-02-SUMMARY.md`
</output>

---
phase: 06.10-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/humanize/mod.rs
  - src/humanize/config.rs
  - src/humanize/engine.rs
  - src/humanize/beat_clock.rs
  - CONTRIBUTING.md
autonomous: true

must_haves:
  truths:
    - "humanize module has module-level documentation"
    - "CONTRIBUTING.md exists with build instructions"
    - "cargo doc includes humanize module documentation"
  artifacts:
    - path: "src/humanize/mod.rs"
      provides: "Module-level documentation explaining humanization system"
      contains: "//!"
    - path: "src/humanize/config.rs"
      provides: "Doc comments on HumanizeConfig and HumanizedNote"
      contains: "///"
    - path: "CONTRIBUTING.md"
      provides: "Developer onboarding guide with build, test, and architecture overview"
      contains: "## Development Setup"
  key_links:
    - from: "CONTRIBUTING.md"
      to: "src/"
      via: "architecture overview pointing to key modules"
      pattern: "harmony/"
---

<objective>
Add documentation to the humanize module and create CONTRIBUTING.md for developer onboarding.

Purpose: The humanize module currently has no documentation. CONTRIBUTING.md helps new contributors understand how to build, test, and navigate the codebase.

Output: Documented humanize module, new CONTRIBUTING.md file at project root.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06.10-docs/06.10-RESEARCH.md

Reference existing docs:
@src/harmony/mod.rs - Module docs pattern (after Plan 01 enhances it)
@README.md - Project overview for CONTRIBUTING context
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add humanize module documentation</name>
  <files>src/humanize/mod.rs, src/humanize/config.rs</files>
  <action>
1. **src/humanize/mod.rs**: Add module-level documentation explaining:
   - Purpose: Add human-like imperfections to generated harmony notes
   - Main types: BeatClock, Humanizer, HumanizeConfig, DelayQueue
   - How it integrates with the harmony engine (post-processing)

```rust
//! Humanization engine for adding human-like timing variation.
//!
//! This module provides timing jitter, velocity variation, groove/swing,
//! and beat clock tracking to make generated harmony notes feel more natural.
//!
//! # Main Types
//!
//! - [`BeatClock`] - Tracks musical time (BPM, beat position)
//! - [`Humanizer`] - Applies humanization effects to notes
//! - [`HumanizeConfig`] - Configuration for humanization parameters
//! - [`DelayQueue`] - Schedules delayed note events
//! - [`Metronome`] - Optional audible click track
//!
//! # Usage
//!
//! Humanization is applied after harmony generation, before notes are sent
//! to MIDI outputs. The melody note (index 0) bypasses humanization;
//! only harmony notes (index 1+) are affected.
```

2. **src/humanize/config.rs**: Add doc comments to:
   - `HumanizeConfig` struct and its fields (timing_jitter_ms, velocity_variation, groove_amount, etc.)
   - `HumanizedNote` struct explaining it wraps a note with timing offset

Pattern:
```rust
/// Configuration for humanization effects.
///
/// All parameters can be adjusted during playback. Typical ranges:
/// - `timing_jitter_ms`: 5-30ms for subtle variation
/// - `velocity_variation`: 0.1-0.2 (10-20% variation)
/// - `groove_amount`: 0.0-0.5 for swing feel
#[derive(Clone, Debug)]
pub struct HumanizeConfig { ... }
```
  </action>
  <verify>`cargo doc --all-features` completes; humanize module appears in docs index</verify>
  <done>humanize/mod.rs has comprehensive //! docs, config.rs has /// docs on structs</done>
</task>

<task type="auto">
  <name>Task 2: Document humanize engine and beat clock</name>
  <files>src/humanize/engine.rs, src/humanize/beat_clock.rs</files>
  <action>
1. **src/humanize/engine.rs**: Add documentation to:
   - Module-level docs explaining Humanizer's role
   - `Humanizer` struct doc comment
   - Key public methods

```rust
//! Humanizer engine that applies timing and velocity variations.

/// Applies humanization effects to harmony notes.
///
/// Takes notes from the harmony engine and adds:
/// - Timing jitter (random delay 0-N ms)
/// - Velocity variation (random +/- percentage)
/// - Groove/swing (off-beat timing shift)
///
/// The humanizer requires a [`BeatClock`] for groove calculations.
pub struct Humanizer { ... }
```

2. **src/humanize/beat_clock.rs**: Add documentation to:
   - Module-level docs
   - `BeatClock` struct explaining it tracks musical time
   - Public methods (start, stop, tick, beat_position, bpm)

```rust
//! Musical beat clock for tempo and time tracking.

/// Tracks musical time based on BPM and elapsed time.
///
/// Provides beat position for groove calculations and metronome sync.
/// Call [`tick`] on each update frame to advance the clock.
pub struct BeatClock { ... }
```
  </action>
  <verify>`cargo doc` shows humanize::Humanizer and humanize::BeatClock with docs</verify>
  <done>engine.rs and beat_clock.rs have module and struct documentation</done>
</task>

<task type="auto">
  <name>Task 3: Create CONTRIBUTING.md</name>
  <files>CONTRIBUTING.md</files>
  <action>
Create CONTRIBUTING.md at project root with:

1. **Development Setup**
   - Prerequisites (Rust stable, ALSA on Linux, trunk for WASM)
   - Build commands (native, release, WASM)
   - Running tests

2. **Architecture Overview**
   - Key modules: harmony/, humanize/, midi/, app.rs, ui.rs
   - Brief description of each module's responsibility
   - Data flow: MIDI input -> harmony engine -> humanizer -> MIDI output

3. **Making Changes**
   - Fork, branch, PR workflow
   - Run tests and clippy before submitting
   - Add doc comments to public items

4. **Code Style**
   - Follow rustfmt
   - Add doc comments with examples where practical

Use the template from research but customize for Contrapunk's actual structure.

NOTE: Do NOT include a "License" section since Contrapunk doesn't have a LICENSE file yet.
  </action>
  <verify>File exists: `ls CONTRIBUTING.md`; contains key sections: `grep -c "Development Setup" CONTRIBUTING.md`</verify>
  <done>CONTRIBUTING.md exists at project root with build instructions and architecture overview</done>
</task>

</tasks>

<verification>
1. `cargo doc --all-features` completes without warnings
2. humanize module visible in target/doc/contrapunk/humanize/index.html with docs
3. CONTRIBUTING.md exists and renders in GitHub
4. `cargo test --doc` passes (no new test failures)
</verification>

<success_criteria>
- humanize/mod.rs has //! module docs explaining the system
- humanize/config.rs has /// docs on HumanizeConfig and HumanizedNote
- humanize/engine.rs and beat_clock.rs have module and struct docs
- CONTRIBUTING.md exists with Development Setup, Architecture Overview, Making Changes sections
- All new docs render correctly in `cargo doc` output
</success_criteria>

<output>
After completion, create `.planning/phases/06.10-docs/06.10-02-SUMMARY.md`
</output>

---
phase: harmony-rework
task: research-complete
total_tasks: 7
status: research_complete_awaiting_implementation
last_updated: 2026-04-11T23:04:52.662Z
---

<current_state>
All 7 research reports complete and saved to `.planning/research/`. Design decisions locked in. Ready to start Phase A (honest renaming) in next session. This session was ENTIRELY research — no code changes yet. One critical design reframe late in the session: "Contrapunk is for improvisation, not progressions" — this dropped 5 planned modes (AxisProgression/DooWop/etc.) and reframed BachChorale to be reactive instead of sequence-imposing.
</current_state>

<completed_work>

- ✅ HN thread recon — 118 pts, 54 comments (was 76/31). One unreplied: Lycomedes1814 #47663649
- ✅ Full harmony mode reality-vs-claims audit (8 current modes)
- ✅ Traced the Lycomedes1814 critique to its mechanism (DiatonicThirds with voice_count=4, voice_position=0 → stacked diatonic 7th chords)
- ✅ Waitlist recon — 29 signups, identified Daniel Raffel + Tom Smith as high-value targets
- ✅ 7 parallel research agents completed, all reports in `.planning/research/`:
  1. harmony_01_functional_satb.md — Rameau/Riemann + Bach SATB rules + Rust types
  2. harmony_02_fux_species.md — Species 1-5 exhaustive rules
  3. harmony_03_bach_corpus.md — Actual Bach transition matrices (CSV ready)
  4. harmony_04_langoy_scales.md — 57 scales extracted, 26-29 new ones
  5. harmony_05_jazz_barry_harris.md — Real BH method (current impl = 5% of real)
  6. harmony_06_oss_libraries.md — Hybrid Markov+rules recommended
  7. harmony_07_next_note_formula.md — 11-term scoring function, O(1)/candidate
- ✅ Research index `.planning/research/README.md` and `SESSION_2026_04_12.md` summary
- ✅ Memory saved: `project_harmony_mode_audit.md`, `feedback_improvisation_not_progressions.md`
- ✅ MEMORY.md updated with both new entries
- ✅ Species counterpoint scope decision: 1-4 reactive in main engine, 5 deferred to Performance Mode
</completed_work>

<remaining_work>

**Phase A — Honest renaming (START HERE)**
- Rename `DiatonicThirds` → "Stacked Thirds (chained)" in UI
- Rename `BarryHarris` → "BH Parallel 2nds (requires 8-note BH scale)"
- Add UI tooltips explaining what each mode actually produces
- ~2-3 hours, no logic changes

**Phase B — Reactive functional harmony**
- Build `HarmonicContext` state (last_chord, key, beat_phase, borrowing_budget)
- `FunctionalHarmony` mode — picks chord that FITS played note (note = 1/3/5/7 of chord)
- `BachChorale` mode — same + strict SATB voice leading from `rules.rs`
- Offline Python script to extract Markov transition table from music21 Bach corpus
- Bundle as `assets/bach_markov.msgpack` via `include_bytes!`
- **CRITICAL: must be REACTIVE, never impose sequences**

**Phase C — Species counterpoint 1-4**
- Add 7 missing Fux rules to existing `CounterpointState::score_candidate` → strict Species 1
- Implement Species 2 (2:1 with passing tones)
- Implement Species 3 (4:1 with cambiata/neighbors)
- Implement Species 4 (suspensions — 9-8, 7-6, 4-3, 2-3 catalog)
- UI: selectable 1-4 inside "SpeciesCounterpoint" mode

**Phase D — Fix Barry Harris**
- 8-note scale guard (`BHMajor6thDim` / `BHMinor6thDim`)
- Beat-phase input through dispatcher
- Drop-2 4-voice output (breaks current 2-note return shape — ripples through engine)
- Sister chord substitutions

**Phase E — Langøy scale expansion**
- Reply to Lycomedes1814 on HN first, ask permission
- Add 26-29 new ScaleMode variants (pentatonic, symmetric, harmonic major, etc.)

**Phase F — Next-note suggestion overlay (killer feature)**
- Implement 11-term scoring function from research_07
- Fretboard/keyboard visualization with top-3 green / next-5 yellow
- Ergonomic "nearest playable position" for guitar
- Live-tunable weights via UI

**Phase G — Performance Mode + Species 5 (much later)**
- Phrase-buffer analysis infrastructure
- Species 5 florid counterpoint using phrase lookahead
- Basic-pitch integration for polyphonic chord/key detection
</remaining_work>

<decisions_made>

- **Contrapunk is for improvisation, NOT fixed progressions** (critical reframe from user late in session). Dropped AxisProgression, DooWop, PopMinor, MixolydianRock, FlatVIPop. Even BachChorale must be reactive.
- **Hybrid Markov+rules is the architecture** for BachChorale/FunctionalHarmony. Not neural (200ms+ latency), not CSP (unbounded). Gives 7.5/10 quality at <500µs latency.
- **Species 1-4 in main engine (reactive), Species 5 deferred to Performance Mode** (shared phrase-buffer infrastructure)
- **BarryHarris mode is fundamentally broken** — captures ~5% of real method. Needs 8-note scale, beat-phase, drop-2 4-voice output
- **Langøy scales need permission** before porting. Reply to Lycomedes1814 first
- **Next-note suggestion is separate overlay**, not a harmony mode
- **Phase A (rename) first** — 2 hours, high user impact, clears confusion before touching logic
- **Bach corpus transition table** training offline via Python music21, Rust loads via `rmp-serde` + `include_bytes!` (<500KB)
</decisions_made>

<blockers>
- **Lycomedes1814 permission** needed before porting langøy scales (not urgent — can do Phase A/B/C/D without it)
- **No active phase in GSD format** — this work was captured via research docs rather than structured GSD phases. All the detail is in `.planning/research/` and memory files
</blockers>

<context>
The session started with HN monitoring (checking for new comments and waitlist signups), led into the Lycomedes1814 comment which exposed a real music theory gap: no Contrapunk mode actually does functional harmony. This triggered a deep research push — 7 parallel agents investigating every angle (classical harmony, species counterpoint, Bach corpus, Jazz/Barry Harris, scale databases, OSS libraries, next-note prediction).

The research outputs are SUBSTANTIAL — they collectively specify everything needed to implement the rework. The biggest surprise was late in the session: the user corrected "Contrapunk is for improvisation, not progressions" which dropped 5 proposed modes and reframed everything. The remaining modes all must be REACTIVE — they pick harmonies that fit what the player actually plays, never impose a sequence.

Next session should START with Phase A (renaming) since it's quick, ships real user-facing improvement, and doesn't require any of the complex state machine work. Then Phase B (reactive FunctionalHarmony/BachChorale) is the real meat — that's where the hybrid Markov+rules architecture gets built. Phase C (species 1-4) can run in parallel since it only touches the existing CounterpointState.

All research is persisted in `.planning/research/`. All decisions/gotchas are in memory. Everything needed to resume cleanly is saved.

One thing I'd do fresh: don't reply to Lycomedes1814 until AFTER Phase A ships, so the reply can say "we renamed the mode, also working on proper FunctionalHarmony — would love your scale database." Shows progress.
</context>

<next_action>
Start with: **Phase A — rename misleading mode names**.

1. Read `src/harmony/config.rs` lines 275-337 (HarmonyMode enum)
2. Change `BarryHarris` display name in UI to "BH Parallel 2nds (8-note scale)"
3. Change `DiatonicThirds` display name to "Stacked Thirds (chained)"
4. Add tooltip/description fields for each mode explaining what they produce
5. Ship as a quick PR tagged `kind/docs`
6. Then open draft reply to Lycomedes1814 HN comment acknowledging the critique

Reference files:
- `.planning/research/README.md` — master index
- `.planning/research/SESSION_2026_04_12.md` — full session summary
- `memory/project_harmony_mode_audit.md` — phase plan
- `memory/feedback_improvisation_not_progressions.md` — critical design reframe
</next_action>

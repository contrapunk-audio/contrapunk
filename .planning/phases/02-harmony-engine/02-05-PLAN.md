---
phase: 02-harmony-engine
plan: 05
type: execute
wave: 3
depends_on: ["02-02"]
files_modified:
  - src/main.rs
autonomous: true

must_haves:
  truths:
    - "User can select a key from C through B before routing starts"
    - "User can select a harmony mode (1-7) before routing starts"
    - "Selected key and mode are displayed in configuration summary"
    - "HarmonyEngine is created with user's selections"
  artifacts:
    - path: "src/main.rs"
      provides: "CLI prompts for key and mode selection"
      contains: ["select_key", "select_mode", "HarmonyEngine::new"]
  key_links:
    - from: "src/main.rs"
      to: "src/harmony/config.rs"
      via: "Key::all() and HarmonyMode::all() for menu options"
      pattern: "Key::all\\(\\)|HarmonyMode::all\\(\\)"
    - from: "src/main.rs"
      to: "src/router.rs"
      via: "Passes HarmonyEngine to run_router()"
      pattern: "run_router.*engine"
---

<objective>
Add CLI prompts for key and mode selection before MIDI routing starts.

Purpose: Allows user to configure harmony settings (CONF-01, CONF-02). The runtime key/mode changes (CONF-03) will work automatically since HarmonyEngine supports set_key()/set_mode().
Output: Updated main.rs with key and mode selection prompts
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-harmony-engine/02-RESEARCH.md
@src/main.rs
@src/harmony/config.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add key selection function</name>
  <files>src/main.rs</files>
  <action>
Add a function to select musical key, similar to the existing port selection pattern.

```rust
use crate::harmony::{Key, HarmonyMode, HarmonyEngine};

/// Prompts user to select a musical key.
///
/// Displays all 12 keys and returns the selected Key.
fn select_key() -> Result<Key> {
    println!("Select musical key:");
    for (i, key) in Key::all().iter().enumerate() {
        println!("  {}: {}", i, key);
    }

    print!("\nEnter key number [0-11, default 0 (C)]: ");
    io::stdout().flush()?;

    let mut input = String::new();
    io::stdin().read_line(&mut input)?;
    let input = input.trim();

    if input.is_empty() {
        return Ok(Key::C);  // Default to C
    }

    let index: usize = input.parse()
        .map_err(|_| anyhow!("Invalid number: {}", input))?;

    Key::all()
        .get(index)
        .copied()
        .ok_or_else(|| anyhow!("Key index {} out of range (0-11)", index))
}
```

Add necessary imports at the top of main.rs:
```rust
use std::io::{self, Write};
use anyhow::anyhow;
```
  </action>
  <verify>Function compiles without errors</verify>
  <done>
- select_key() function displays 12 keys
- Returns selected Key or default (C)
- Handles invalid input with error message
  </done>
</task>

<task type="auto">
  <name>Task 2: Add mode selection and integrate into main</name>
  <files>src/main.rs</files>
  <action>
Add mode selection function and integrate both key and mode selection into main().

```rust
/// Prompts user to select a harmony mode.
///
/// Displays all 7 modes with descriptions and returns the selected mode.
fn select_mode() -> Result<HarmonyMode> {
    println!("Select harmony mode:");
    for mode in HarmonyMode::all() {
        println!("  {}: {}", mode.number(), mode.description());
    }

    print!("\nEnter mode number [1-7, default 1 (Pass-through)]: ");
    io::stdout().flush()?;

    let mut input = String::new();
    io::stdin().read_line(&mut input)?;
    let input = input.trim();

    if input.is_empty() {
        return Ok(HarmonyMode::PassThrough);  // Default to pass-through
    }

    let number: u8 = input.parse()
        .map_err(|_| anyhow!("Invalid number: {}", input))?;

    HarmonyMode::all()
        .iter()
        .find(|m| m.number() == number)
        .copied()
        .ok_or_else(|| anyhow!("Mode {} not found (valid: 1-7)", number))
}
```

Update main() to add key and mode selection after port selection:

```rust
mod harmony;
mod midi;
mod router;

use anyhow::{anyhow, Result};
use std::io::{self, Write};

use crate::harmony::{Key, HarmonyMode, HarmonyEngine};
use crate::midi::ports::{list_input_ports, list_output_ports, select_input_port, select_output_ports};

fn main() -> Result<()> {
    println!("Contrapunk MIDI Harmony Generator");
    println!("==================================\n");

    // --- MIDI Port Selection ---

    let input_ports = list_input_ports()?;
    if input_ports.is_empty() {
        println!("No MIDI input ports available.");
        println!("Connect a MIDI device and restart the application.");
        return Ok(());
    }

    let selected_input = select_input_port(&input_ports)?;
    println!(
        "\nSelected input: {} - {}\n",
        selected_input, input_ports[selected_input].1
    );

    let output_ports = list_output_ports()?;
    if output_ports.is_empty() {
        println!("No MIDI output ports available.");
        println!("Connect MIDI output devices and restart the application.");
        return Ok(());
    }

    let selected_outputs = select_output_ports(&output_ports, 2, 8)?;
    println!("\nSelected outputs:");
    for &idx in &selected_outputs {
        println!("  {} - {}", idx, output_ports[idx].1);
    }

    // --- Harmony Configuration ---

    println!("\n--- Harmony Configuration ---\n");

    let key = select_key()?;
    println!("\nSelected key: {}\n", key);

    let mode = select_mode()?;
    println!("\nSelected mode: {} - {}\n", mode.number(), mode.description());

    // --- Configuration Summary ---

    println!("\n========================================");
    println!("         Configuration Summary");
    println!("========================================");
    println!("Input:   {} ({})", input_ports[selected_input].1, selected_input);
    println!("Outputs: {} ports", selected_outputs.len());
    for (i, &idx) in selected_outputs.iter().enumerate() {
        let role = if i == 0 { "melody" } else { "harmony" };
        println!("  Voice {}: {} ({}) [{}]", i + 1, output_ports[idx].1, idx, role);
    }
    println!("Key:     {}", key);
    println!("Mode:    {} - {}", mode.number(), mode.description());
    println!("========================================\n");

    // --- Create Harmony Engine and Start Routing ---

    let mut engine = HarmonyEngine::new(key, mode);

    println!("Starting MIDI harmony routing...\n");

    if let Err(e) = router::run_router(selected_input, &selected_outputs, &mut engine) {
        eprintln!("Error during MIDI routing: {}", e);
        return Err(e);
    }

    println!("\nContrapunk exited cleanly.");
    Ok(())
}

/// Prompts user to select a musical key.
fn select_key() -> Result<Key> {
    println!("Select musical key:");
    for (i, key) in Key::all().iter().enumerate() {
        println!("  {}: {}", i, key);
    }

    print!("\nEnter key number [0-11, default 0 (C)]: ");
    io::stdout().flush()?;

    let mut input = String::new();
    io::stdin().read_line(&mut input)?;
    let input = input.trim();

    if input.is_empty() {
        return Ok(Key::C);
    }

    let index: usize = input.parse()
        .map_err(|_| anyhow!("Invalid number: {}", input))?;

    Key::all()
        .get(index)
        .copied()
        .ok_or_else(|| anyhow!("Key index {} out of range (0-11)", index))
}

/// Prompts user to select a harmony mode.
fn select_mode() -> Result<HarmonyMode> {
    println!("Select harmony mode:");
    for mode in HarmonyMode::all() {
        println!("  {}: {}", mode.number(), mode.description());
    }

    print!("\nEnter mode number [1-7, default 1 (Pass-through)]: ");
    io::stdout().flush()?;

    let mut input = String::new();
    io::stdin().read_line(&mut input)?;
    let input = input.trim();

    if input.is_empty() {
        return Ok(HarmonyMode::PassThrough);
    }

    let number: u8 = input.parse()
        .map_err(|_| anyhow!("Invalid number: {}", input))?;

    HarmonyMode::all()
        .iter()
        .find(|m| m.number() == number)
        .copied()
        .ok_or_else(|| anyhow!("Mode {} not found (valid: 1-7)", number))
}
```
  </action>
  <verify>`cargo check` compiles without errors, `cargo run` shows key/mode prompts</verify>
  <done>
- main.rs has select_key() and select_mode() functions
- main() prompts for key and mode after port selection
- Configuration summary shows all settings
- HarmonyEngine created with user's selections
- Engine passed to run_router()
  </done>
</task>

</tasks>

<verification>
- `cargo check` compiles without errors
- `cargo run` shows key selection prompt (12 options)
- `cargo run` shows mode selection prompt (7 options with descriptions)
- Configuration summary displays selected key and mode
</verification>

<success_criteria>
- User can select from 12 keys (C through B)
- User can select from 7 modes (with descriptions)
- Default key is C, default mode is 1 (Pass-through)
- Invalid input shows error message
- Configuration summary is clear and complete
</success_criteria>

<output>
After completion, create `.planning/phases/02-harmony-engine/02-05-SUMMARY.md`
</output>

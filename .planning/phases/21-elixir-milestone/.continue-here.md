---
phase: 21-elixir-milestone
task: manual-qa-standalone-and-integrated-synth
total_tasks: 24
status: paused
last_updated: 2026-05-18T17:49:50.850Z
---

<current_state>
Phase 21 Elixir is paused after a large implementation push through A6, A-Cut, B3, B4.1/B4.2, and B5.1/B5.2. The next resume should NOT start by adding more feature surface. Start by manually testing the two product paths the user called out: the standalone Elixir app and the integrated Tauri built-in synth path behind `elixir-synth`.
</current_state>

<completed_work>

- A6: Spectral oscillator + FX completion — Done (`d030825e`). Includes spectral morphs, phase distortion, unison, A6 FX family, filter models, hot-path filter coefficient preparation, standalone UI exposure, and A6 status docs.
- A-Cut: Tauri Elixir synth cutover — Done (`eaffc09e`, `f8375b35`, docs `a357e57c`). `src-tauri` now has `elixir-synth` feature and audio chain swaps `Synth` for `ElixirSynthBlock`; existing `SynthEvent` and `SynthParams` continue to work.
- B3: `elixir-plugin` skeleton — Done (`193040f2`). CLAP/VST3 nih-plug instrument with MIDI input and Elixir audio output.
- B4.1: Core plugin automation — Done (`8c9b2bb6`). Gain, amp ADSR, oscillator, unison, filter params exposed to DAW automation.
- B4.2: Plugin FX automation — Done (`0679c36a`). Drive/Delay/Reverb/FDN/Chorus/Flanger/Phaser/Compressor params exposed; FX allocated once and bypassed via mix=0.
- B5.1: Preset schema + Vital import — Done (`8f4bf7a1`). New `elixir-preset` crate parses `.vital` JSON and `.vitalbank` ZIPs, preserving raw settings and tracking `.vitaltable` paths for B7.
- B5.2: Standalone Vital import UI — Done (`6aa08cab`). `elixir-standalone` has PRESETS / VITAL IMPORT card that scans `~/Downloads`, imports Vital presets/banks, lists presets, and applies the supported subset.
- Skills created as requested: `contrapunk-audio-chain-cutover`, `contrapunk-nih-plug-skeleton`, `vital-preset-import`.
</completed_work>

<remaining_work>

- Manual QA: Run and test `elixir-standalone` audio/MIDI, A6 controls, FX controls, and Vital import/apply flow.
- Manual QA: Run and test Tauri integrated synth with `elixir-synth` feature; verify built-in synth path uses Elixir and existing UI synth controls still affect sound.
- B5.3: Wire plugin preset state serialization/loading using `elixir-preset`.
- B7: Wavetable editor + full `.vitaltable`/FFT-IFFT spectral parity.
- B8: Headless renderer.
- B9: Public `elixir-v0.1.0` release/signing.
- C0-C4: Multi-plugin hosting tracks.
</remaining_work>

<decisions_made>

- Defer `contrapunk-fx` extraction: Elixir FX remain in `crates/elixir-core/src/fx`; shared primitives live in `contrapunk-dsp`.
- A6 spectral morphs are scalar approximations until B7; docs explicitly avoid claiming full FFT/IFFT wavetable parity.
- Vital import preserves raw Vital settings and maps only a conservative subset now; full sonic parity waits for B7.
- Plugin FX bypass uses each effect's `mix=0.0`, not slot replacement, to preserve state and avoid process-time allocation.
- Reusable patterns should become skills; three project skills were added this session.
</decisions_made>

<blockers>

- Manual testing blocker: Standalone and integrated Tauri synth have not been manually audio-tested after the recent implementation push. User specifically wants to come back and test these.
- Technical blocker: Full Vital sonic parity is not implemented yet. Current import is subset mapping + raw settings preservation; `.vitaltable` handling is queued for B7.
- Technical blocker: Plugin preset serialization/loading is not wired yet.
</blockers>

<context>
The code is committed through B5.2. The repo should be clean except possible `.planning/STATE.md` timestamp churn. On resume, resist the urge to keep implementing immediately; first do product QA. The most important question is whether the standalone and integrated synth actually sound/control correctly after the rapid A6/A-Cut/B-track implementation.

Known Vital assets in `~/Downloads`: six loose `.vital` files plus `Vital Account.vitalbank`. The ignored test `cargo test -p elixir-preset --test downloads_vital -- --ignored` passed against them and counted 75 bank presets + 21 vitaltable paths.
</context>

<next_action>
Start with manual QA:

1. Run standalone:
   `cargo run -p elixir-standalone`
   - confirm audio output and MIDI input
   - test OSC/FILTER/FX controls
   - click `Scan ~/Downloads for Vital`
   - apply several imported presets, especially the six loose `.vital` files

2. Run integrated Tauri synth:
   `cd src-tauri && cargo tauri dev --features elixir-synth`
   - confirm app starts
   - confirm built-in synth audio path uses Elixir
   - confirm existing synth UI params still affect sound (enabled/gain/ADSR/filter/waveform mapping)
   - check for stuck notes/all-notes-off behavior

Only after that continue with B5.3 plugin preset state serialization or B7 wavetable parity.
</next_action>

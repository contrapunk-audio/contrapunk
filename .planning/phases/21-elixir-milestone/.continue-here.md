---
phase: 21-elixir-milestone
task: A6-core-scaffold
total_tasks: 24
status: paused
last_updated: 2026-05-18T16:30:46.494Z
---

<current_state>
Phase 21 Elixir is active. Work paused during Phase 21.A6 (Spectral + FX Completion) after building a tested scaffold for oscillator morph/phase/unison controls, A6 FX variants, FDN-16 reverb, filter model variants, and the first shared no_std DSP primitive crate (`crates/contrapunk-dsp`).

Current validation passes:
- `cargo test -p contrapunk-dsp` — passed
- `cargo test -p elixir-core --lib` — 82 passed
- `cargo check -p elixir-core --target wasm32-unknown-unknown` — passed
</current_state>

<completed_work>
- Resumed Phase 21 from the prior structured handoff and reconciled planning state for active Elixir work.
- Installed/checked in pi-gsd project prompts under `.pi/gsd` and configured 400k context window with 90% pause threshold.
- Updated `.claude/hooks/stop-autoloop.sh` to read `.planning/config.json.context_window` and `pause_at_context_usage_pct`.
- Implemented A6 oscillator scaffold in `crates/elixir-core/src/osc.rs`: `SpectralMorph`, `PhaseDistortionMode`, `UnisonStyle`, `OscParams`, and tests.
- Wired oscillator controls through `Engine -> Voice -> Oscillator` with engine-level setters and integration tests.
- Added A6 FX processors: `Chorus`, `Flanger`, `Phaser`, `Compressor` in `crates/elixir-core/src/fx/modulated.rs`.
- Added/updated FDN-16 reverb in `crates/elixir-core/src/fx/reverb.rs`, including sample-rate tracking and intended delay length tracking.
- Expanded `FX_SLOTS` to 8 and added engine tests proving A6 FX variants are reachable.
- Added A6 filter scaffold in `crates/elixir-core/src/filter.rs`: `FilterKind`, `FilterModel`, diode, dirty, formant, and phaser-filter models.
- Wired filter model controls through Engine and Voice; added filter and engine integration tests.
- Ran a reuse audit with subagents to avoid reinventing root app infrastructure.
- Created `crates/contrapunk-dsp` for shared no_std DSP primitives and migrated Elixir helpers to reuse it.
</completed_work>

<remaining_work>
- A6: Hoist filter coefficient calculations out of per-sample/per-voice hot paths. Current scaffold passes tests but computes `tanf()`/SVF coefficients inside several `tick()` paths.
- A6: Add stronger oscillator golden metric tests for all 12 morphs, 9 phase modes, and 11 unison styles.
- A6: Expose oscillator/filter/A6 FX controls in `crates/elixir-standalone/src/ui.rs`.
- A6: Decide whether to create `contrapunk-fx` now or wait until A6 APIs stabilize.
- A6: Document which current oscillator/spectral behavior is scaffold approximation vs final FFT/IFFT wavetable parity.
- A-Cut: Extend `src/chain/elixir_block.rs` rather than creating a new Contrapunk adapter.
- B3: Create `crates/elixir-plugin` using patterns from the existing `plugin/` crate, not by coupling to it.
- C0+: Reuse existing CLAP host code under `src/plugin_host/clap` for Track C.
</remaining_work>

<decisions_made>
- Proceed A6-first because the user asked to complete Elixir and A-Cut is locked to post-A6 parity.
- Keep Elixir FX self-contained through A6; revisit shared FX extraction after APIs stabilize/A-Cut.
- Do not make `elixir-core` depend on root `contrapunk`, `src/fx`, `src/chain`, Tauri, nih-plug, or `contrapunk-transport`.
- Reuse at the correct layer: root `AudioBlock`/Chain for A-Cut and Track C; nih-plug patterns for B3; `contrapunk-transport` in host layers; neutral no_std primitives through `contrapunk-dsp`.
- Avoid `contrapunk-audio::RingBuffer` for DSP delay lines because it uses `rotate_left` on overflow.
</decisions_made>

<blockers>
- Technical: A6 filter coefficient preparation is currently too expensive for final real-time use. Workaround: hoist coefficients/prepared params per block before declaring A6 real-time-ready.
- Technical: Spectral morphs are currently scalar sine-table approximations, not final FFT/IFFT wavetable parity. Workaround: keep docs honest and add golden metric tests before deeper wavetable implementation.
</blockers>

<human_actions_pending>
- Reserve `com.contrapunk.elixir.standalone` and `com.contrapunk.elixir.plugin` before B9 signing.
- Decide whether repo-root `context.md` from the reuse audit should be moved, committed, or deleted.
- Review commit splitting: code changes, planning/GSD changes, and generated artifacts should likely become separate commits.
</human_actions_pending>

<context>
The core architectural lesson from the pause session: do not import root app wrappers into `elixir-core`. The right reuse pattern is neutral no_std primitives (`contrapunk-dsp`) plus adapter-layer reuse (`AudioBlock`, ChainCommander, CLAP host, nih-plug patterns, transport snapshots). Current code is a good A6 scaffold with passing tests, but not final spectral wavetable parity and not yet CPU-optimized for filter coefficients.
</context>

<next_action>
Start with: hoist A6 filter coefficient calculations out of per-sample `FilterModel::tick()` paths. Then run `cargo test -p elixir-core --lib` and `cargo check -p elixir-core --target wasm32-unknown-unknown`.
</next_action>

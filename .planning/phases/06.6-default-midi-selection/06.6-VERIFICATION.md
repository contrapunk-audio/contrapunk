---
phase: 06.6-default-midi-selection
verified: 2026-02-01T21:27:53Z
status: passed
score: 5/5 must-haves verified
---

# Phase 06.6: Default MIDI Selection Verification Report

**Phase Goal:** Persist default MIDI input and output device selections so they are remembered across application restarts, on both native (file-based eframe::Storage) and WASM (localStorage)

**Verified:** 2026-02-01T21:27:53Z

**Status:** passed

**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Selected MIDI input and output devices persist across app restarts | ✓ VERIFIED | save() calls save_midi_defaults, new() calls load_midi_defaults. Both native and WASM paths present. |
| 2 | Device matching uses port names, not indices | ✓ VERIFIED | build_midi_defaults() resolves indices to names (lines 44-72). apply_midi_defaults() resolves names back to indices (lines 77-97). |
| 3 | Unavailable saved device falls back to None (shows Select...) | ✓ VERIFIED | apply_midi_defaults uses and_then pattern (lines 79-87, 90-96) which returns None if device name not found. |
| 4 | Works on native (eframe file storage) and WASM (localStorage) | ✓ VERIFIED | Native: apply_midi_defaults called immediately after refresh_devices (line 342). WASM: stored in midi_defaults_pending, applied after async device enumeration (line 922-923). Both compile successfully. |
| 5 | Virtual inputs (Note Generator, Computer Keyboard) persist via special string constants | ✓ VERIFIED | VIRTUAL_NOTE_GENERATOR and VIRTUAL_COMPUTER_KEYBOARD constants (lines 11-13). Round-trip in build (lines 46-50) and apply (lines 81-82). |

**Score:** 5/5 truths verified

### Required Artifacts

| Artifact | Expected | Status | Details |
|----------|----------|--------|---------|
| src/midi_defaults.rs | MidiDefaults struct, save/load/build/apply functions | ✓ VERIFIED | EXISTS (97 lines), SUBSTANTIVE (contains MidiDefaults struct with Serialize/Deserialize, 5 functions as planned, virtual input constants, no stub patterns), WIRED (imported and used 6 times in app.rs) |
| src/app.rs | Wired persistence in new() and save() | ✓ VERIFIED | EXISTS, SUBSTANTIVE (midi_defaults_pending field added line 250, save() integration line 839-840, load() integration line 334, native apply line 341-342, WASM deferred apply line 922-923), WIRED (all integration points present) |

### Key Link Verification

| From | To | Via | Status | Details |
|------|----|----|--------|---------|
| src/app.rs::save() | src/midi_defaults.rs | save() calls save_midi_defaults | ✓ WIRED | Line 839-840: builds defaults from state, saves to storage |
| src/app.rs::new() | src/midi_defaults.rs | new() calls load_midi_defaults | ✓ WIRED | Line 334: loads defaults from storage |
| src/app.rs::new() (native) | src/midi_defaults.rs | Immediate apply after refresh_devices | ✓ WIRED | Line 341-342: applies loaded defaults immediately after device enumeration |
| src/app.rs::update() (WASM) | src/midi_defaults.rs | Deferred apply after midi_initialized | ✓ WIRED | Line 922-923: applies pending defaults after async device enumeration using .take() |

### Compilation Verification

| Target | Status | Details |
|--------|--------|---------|
| Native (cargo check --features gui) | ✓ PASS | Compiled successfully with warnings (unrelated to midi_defaults) |
| WASM (cargo check --target wasm32-unknown-unknown --features gui) | ✓ PASS | Compiled successfully with warnings (unrelated to midi_defaults) |

### Anti-Patterns Found

No anti-patterns detected. Code is clean:

- No TODO/FIXME/placeholder comments
- No empty implementations
- No console.log-only functions
- Proper error handling (unwrap_or_default for missing storage)
- Name-based matching prevents index drift
- Virtual inputs use stable string constants
- WASM deferred application uses .take() to prevent double-application

### Must-Have Verification Details

**Truth 1: "Selected MIDI input and output devices persist across app restarts"**

Verified by:
- eframe::App::save() implementation (app.rs:837-841) builds and saves defaults
- ContrapunkApp::new() (app.rs:334) loads defaults from storage
- Both native and WASM paths apply the loaded defaults
- MidiDefaults struct has Serialize/Deserialize derives

**Truth 2: "Device matching uses port names, not indices"**

Verified by:
- build_midi_defaults() (midi_defaults.rs:44-72) converts selected indices to port names by looking up in available_inputs/available_outputs
- apply_midi_defaults() (midi_defaults.rs:77-97) converts saved names back to indices by searching available_inputs/available_outputs
- No raw index storage in MidiDefaults struct (only input_name: Option<String> and output_names: Vec<Option<String>>)

**Truth 3: "Unavailable saved device falls back to None (shows Select...)"**

Verified by:
- apply_midi_defaults() uses and_then pattern (line 79, 91) which returns None if the closure returns None
- find() returns None if device name not found in available list
- No unwrap() or expect() that would panic on missing device
- Comment explicitly states "Unavailable devices fall back to None" (line 76)

**Truth 4: "Works on native (eframe file storage) and WASM (localStorage)"**

Verified by:
- Native path: #[cfg(not(target_arch = "wasm32"))] applies immediately after refresh_devices (app.rs:340-343)
- WASM path: #[cfg(target_arch = "wasm32")] stores in midi_defaults_pending, applies after midi_initialized (app.rs:344-347, 922-924)
- Both cargo check commands pass
- eframe::Storage abstraction handles native vs WASM backend differences

**Truth 5: "Virtual inputs (Note Generator, Computer Keyboard) persist via special string constants"**

Verified by:
- Constants defined: VIRTUAL_NOTE_GENERATOR = "__virtual:note_generator", VIRTUAL_COMPUTER_KEYBOARD = "__virtual:computer_keyboard" (midi_defaults.rs:11-13)
- build_midi_defaults() converts sentinel indices (INPUT_NOTE_GENERATOR, INPUT_COMPUTER_KEYBOARD) to string constants (lines 46-50)
- apply_midi_defaults() converts string constants back to sentinel indices (lines 81-82)
- Virtual inputs don't have real port names, so string constants are necessary for stable persistence

---

_Verified: 2026-02-01T21:27:53Z_
_Verifier: Claude (gsd-verifier)_

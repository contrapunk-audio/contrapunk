---
phase: 06.9-repo-cleanup-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/deploy.yml
autonomous: true

must_haves:
  truths:
    - "A single workflow file handles build, test, and deploy"
    - "PRs trigger check, test, and wasm-check jobs but NOT deploy"
    - "Push to main triggers check, test, wasm-check, AND deploy (deploy only after tests pass)"
    - "Deploy job builds WASM with Trunk and deploys to Fly.io"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Unified CI/CD workflow with build, test, and deploy"
      contains: "cargo check"
    - path: ".github/workflows/deploy.yml"
      provides: "DELETED — no longer exists"
  key_links:
    - from: "deploy job"
      to: "test job"
      via: "needs: [check, test, wasm-check]"
      pattern: "needs:.*test"
    - from: "deploy job"
      to: "main branch"
      via: "if condition"
      pattern: "if:.*github.ref.*refs/heads/main"
---

<objective>
Consolidate CI and deploy into a single GitHub Actions workflow.

Purpose: Simplify CI/CD by merging two separate workflows (ci.yml and deploy.yml) into one unified pipeline. Deploy only runs after all checks pass, and only on main branch pushes.
Output: Single `.github/workflows/ci.yml` with 4 jobs (check, test, wasm-check, deploy). `deploy.yml` deleted.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.github/workflows/ci.yml
@.github/workflows/deploy.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge workflows into unified ci.yml</name>
  <files>.github/workflows/ci.yml</files>
  <action>
Replace `.github/workflows/ci.yml` with a unified workflow that has 4 jobs:

1. **check** — same as current ci.yml check job (checkout, rust-toolchain stable, rust-cache with shared-key native, cargo check)
2. **test** — same as current ci.yml test job (checkout, rust-toolchain stable, rust-cache with shared-key native, cargo test)
3. **wasm-check** — same as current ci.yml wasm-check job (checkout, rust-toolchain stable with wasm32-unknown-unknown target, rust-cache with shared-key wasm, cargo check --target wasm32-unknown-unknown)
4. **deploy** — taken from deploy.yml. Add `needs: [check, test, wasm-check]` so it only runs after all 3 jobs pass. Add `if: github.event_name == 'push' && github.ref == 'refs/heads/main'` so it only runs on main branch pushes (not PRs). Keep all deploy steps: checkout, rust-toolchain with wasm32 target, rust-cache wasm, cache Trunk binary, install Trunk if cache miss, trunk build --release, copy dist to deploy/, setup flyctl, run flyctl deploy with FLY_API_TOKEN secret.

Workflow name: "CI" (keep existing name).
Triggers: keep existing `on: push: branches: [main]` and `pull_request: branches: [main]`.
  </action>
  <verify>Review the file to confirm 4 jobs exist, deploy has needs and if condition, all steps preserved from both original files.</verify>
  <done>Single ci.yml contains check, test, wasm-check, and deploy jobs. Deploy depends on all 3 other jobs and only runs on main push.</done>
</task>

<task type="auto">
  <name>Task 2: Delete deploy.yml</name>
  <files>.github/workflows/deploy.yml</files>
  <action>
Delete `.github/workflows/deploy.yml` since its contents have been merged into ci.yml.

Use: `rm .github/workflows/deploy.yml`
  </action>
  <verify>`ls .github/workflows/` shows only ci.yml (no deploy.yml).</verify>
  <done>deploy.yml no longer exists. Only ci.yml remains in .github/workflows/.</done>
</task>

</tasks>

<verification>
1. `.github/workflows/ci.yml` exists with 4 jobs: check, test, wasm-check, deploy
2. `.github/workflows/deploy.yml` does NOT exist
3. Deploy job has `needs: [check, test, wasm-check]`
4. Deploy job has `if: github.event_name == 'push' && github.ref == 'refs/heads/main'`
5. All original CI steps preserved (cargo check, cargo test, wasm check)
6. All original deploy steps preserved (Trunk install, trunk build, flyctl deploy)
</verification>

<success_criteria>
A single GitHub Actions workflow builds, tests, and deploys. PRs get checked and tested but not deployed. Main branch pushes get the full pipeline including deployment to Fly.io, but only after all checks pass.
</success_criteria>

<output>
After completion, create `.planning/phases/06.9-repo-cleanup-docs/06.9-01-SUMMARY.md`
</output>

---
phase: 06.9-repo-cleanup-docs
plan: 03
type: execute
wave: 1
depends_on: []
files_modified: [README.md]
autonomous: true

must_haves:
  truths:
    - "README describes Contrapunk as a Rust MIDI harmony generator"
    - "README has working build instructions using cargo"
    - "README lists current features (harmony modes, scales, GUI, WASM)"
    - "No references to Python, pip, or requirements.txt in README"
  artifacts:
    - path: "README.md"
      provides: "Project documentation for Rust Contrapunk"
      contains: "cargo"
  key_links: []
---

<objective>
Rewrite README.md from the outdated Python-era description to accurately document the current Rust project.

Purpose: The README currently tells users to run `pip install` and `python main.py` for a project that is now 100% Rust. New visitors get completely wrong instructions.
Output: Accurate README.md covering features, installation, usage (native + WASM), and development.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.9-repo-cleanup-docs/06.9-RESEARCH.md
@Cargo.toml
@src/lib.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md for Rust project</name>
  <files>README.md</files>
  <action>
    Read the current README.md first to note anything worth preserving (the Ideas section has brainstorming -- not critical to keep, already captured in research).

    Then rewrite README.md with this structure:

    **# Contrapunk**
    Brief tagline: Real-time MIDI harmony generator built with Rust.

    **## Features**
    List current capabilities:
    - 9 harmony modes (Pass-through, Diatonic 3rds, Diatonic 5ths, Random Diatonic, Contrary Motion, Strict Counterpoint, Barry Harris)
    - 28+ scale modes organized by family (Church modes, Harmonic Minor, Melodic Minor, Exotic, Barry Harris 6th Diminished)
    - Modal interchange with visual feedback for chromatic note handling
    - Comprehensive chord detection (extended, slash, add chords, roman numeral analysis)
    - Steampunk-themed GUI with tabbed navigation (Play/Craft/Settings)
    - 11+ musical style presets with character personas
    - Humanization engine (timing jitter, velocity variation, groove, beat clock)
    - Mirror octaves for multi-octave harmony spread
    - Native desktop app (single binary) and WASM browser version
    - Server/client mode for remote MIDI processing
    - Persistent MIDI device selection

    **## Try It**
    Link to https://contrapunk.fly.dev/ for the browser version.

    **## Installation**

    ### From Source
    ```
    git clone https://github.com/waveywaves/contrapunk.git
    cd contrapunk
    cargo build --release
    ./target/release/contrapunk
    ```

    ### WASM (Browser)
    ```
    cargo install trunk
    trunk serve
    ```
    Then open http://localhost:8080

    **## Usage**
    Brief description: Connect MIDI input/output devices, select a harmony mode and scale, play notes to hear harmonies. Adjust humanization, presets, and settings via the tabbed GUI.

    **## Development**
    ```
    cargo build          # Debug build
    cargo test           # Run tests
    cargo clippy         # Lint
    trunk build          # WASM build
    ```

    **## Architecture**
    Brief mention of key modules: harmony engine, humanizer, beat clock, server mode, GUI (egui/eframe). Point to src/ for details.

    **## Deployment**
    Deployed to Fly.io at https://contrapunk.fly.dev/. CI/CD via GitHub Actions (.github/workflows/ci.yml).

    **## License**
    Check if a LICENSE file exists. If not, just put "TBD" or omit the section entirely. Do NOT add a license without one already existing.

    Keep it concise. No Python references. No ideas/brainstorming section (that content is captured in .planning/phases/06.9-repo-cleanup-docs/06.9-RESEARCH.md).
  </action>
  <verify>
    `grep -c "pip\|python main.py\|requirements.txt" README.md` returns 0.
    `grep -c "cargo" README.md` returns at least 3.
    `grep -c "harmony" README.md` returns at least 2.
    README.md is at least 50 lines long (meaningful content, not a stub).
  </verify>
  <done>README.md accurately describes the Rust Contrapunk project with features, build instructions, usage, and development info. Zero Python references.</done>
</task>

</tasks>

<verification>
- README.md mentions cargo build, not pip install
- README.md lists current features (harmony modes, scales, WASM, presets)
- README.md links to https://contrapunk.fly.dev/
- No Python-era content remains
</verification>

<success_criteria>
- README describes a Rust project with cargo-based workflow
- Features section reflects current app capabilities
- Build instructions work (cargo build, trunk serve)
- No references to Python, pip, main.py, or requirements.txt
</success_criteria>

<output>
After completion, create `.planning/phases/06.9-repo-cleanup-docs/06.9-03-SUMMARY.md`
</output>

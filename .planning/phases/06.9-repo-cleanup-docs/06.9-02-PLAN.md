---
phase: 06.9-repo-cleanup-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [main.py, contrapunk_ui.py, audio_to_midi.py, requirements.txt, .gitignore]
autonomous: true

must_haves:
  truths:
    - "No Python files exist in repository root"
    - "target/ directory is gitignored and untracked"
    - "dist/ directory is untracked (gitignored)"
    - ".gitignore contains Rust-specific entries"
  artifacts:
    - path: ".gitignore"
      provides: "Rust-focused gitignore with target/, dist/"
      contains: "target/"
  key_links: []
---

<objective>
Remove all legacy Python files from the repository and update .gitignore from Python-focused to Rust-focused.

Purpose: The repo migrated from Python to Rust but still contains dead Python files and a Python-era .gitignore missing critical Rust entries (target/).
Output: Clean repository root with no Python artifacts and a proper Rust .gitignore.
</objective>

<execution_context>
@/Users/vibhavbobade/.claude/get-shit-done/workflows/execute-plan.md
@/Users/vibhavbobade/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06.9-repo-cleanup-docs/06.9-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove legacy Python files and directories</name>
  <files>main.py, contrapunk_ui.py, audio_to_midi.py, requirements.txt, __pycache__/, templates/</files>
  <action>
    Use `git rm` to remove tracked Python files:
    - `git rm main.py`
    - `git rm contrapunk_ui.py`
    - `git rm audio_to_midi.py`
    - `git rm requirements.txt`
    - `git rm -r __pycache__/` (if tracked; if not tracked, just `rm -rf __pycache__/`)
    - `git rm -r templates/` (if tracked; if not tracked, just `rm -rf templates/`)

    Also untrack dist/ if it has tracked files: `git rm -r --cached dist/` (keeps files on disk but removes from git tracking). Check with `git ls-files dist/` first -- only run git rm if files are listed.

    Do NOT use git filter-branch or BFG. Simple git rm is correct -- files remain in history which is fine.
  </action>
  <verify>
    `ls main.py contrapunk_ui.py audio_to_midi.py requirements.txt 2>&1` should show "No such file" for all four.
    `git ls-files main.py contrapunk_ui.py audio_to_midi.py requirements.txt` should return empty.
    `git ls-files dist/` should return empty.
    `ls __pycache__ templates 2>&1` should show "No such file" for both.
  </verify>
  <done>All 4 Python files deleted from working tree and git index. __pycache__/ and templates/ removed. dist/ untracked.</done>
</task>

<task type="auto">
  <name>Task 2: Update .gitignore for Rust project</name>
  <files>.gitignore</files>
  <action>
    Rewrite .gitignore to be Rust-focused. Keep useful general entries, add Rust-specific ones, remove Python-only entries.

    New .gitignore content:

    ```
    # Rust build artifacts
    target/

    # WASM build output
    dist/

    # IDE
    .idea/
    .vscode/
    *.swp
    *.swo

    # OS
    .DS_Store

    # Environment
    .env

    # Logs
    *.log
    ```

    This covers: Rust build dir (target/), WASM output (dist/), common IDE/OS/env files. Python entries removed since no Python code remains.

    After writing, run `git rm -r --cached target/` if `git ls-files target/` shows any tracked files (target/ may have been committed before .gitignore had it).
  </action>
  <verify>
    `grep "target/" .gitignore` returns a match.
    `grep "dist/" .gitignore` returns a match.
    `grep "__pycache__" .gitignore` returns NO match (Python entries removed).
    `git ls-files target/` returns empty.
  </verify>
  <done>.gitignore contains Rust-specific entries. target/ and dist/ are gitignored and untracked. No Python-specific entries remain.</done>
</task>

</tasks>

<verification>
- `git status` shows deleted Python files and modified .gitignore staged
- No Python files in repo root: `ls *.py requirements.txt 2>&1` shows errors only
- `cargo build 2>&1 | head -1` still works (nothing broken by cleanup)
</verification>

<success_criteria>
- Zero Python files in repository root
- .gitignore has target/ and dist/ entries
- target/ and dist/ are not tracked by git
- Project still builds with cargo
</success_criteria>

<output>
After completion, create `.planning/phases/06.9-repo-cleanup-docs/06.9-02-SUMMARY.md`
</output>

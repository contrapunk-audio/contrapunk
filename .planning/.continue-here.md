---
context: feature-pr
branch: feat/bpm-clock
pr: 89
status: paused-ready-for-review
last_updated: 2026-04-28T22:07:49.990Z
---

<current_state>
PR #89 (`feat/bpm-clock`) has 10 commits implementing the BPM-aware Performance feature end-to-end. All originally-planned phases (1-5 + BeatGrid refactor + knob tooltip) are shipped. True Quantized mode is deferred (needs new infrastructure). User has paused work to queue a brutal code review next session.
</current_state>

<completed_work>

PR #89 commits on `feat/bpm-clock`:
- `11151cb` — Phase 1+2+3: clock → counterpoint beat-phase wiring, pattern store, programmer panel
- `85e2b26` — Backend `PatternConfig` scaffolding in `src-tauri/src/state.rs`
- `ee33b10` — Phase 4: Tauri commands + adapter + frontend dispatch + router cell-boundary detection
- `00d1fa1` — Step-sequencer NoteOff semantics + cyan knob accent (default)
- `bb255d3` — `set_pattern_enabled(true)` auto-starts the transport clock
- `0028702` — Live/Quantized/Gated input-mode dispatch + drain-on-disable
- `1da6f0b` — Knob hover tooltip (Serum-style "Label : Value" overlay)
- `05bcc50` — Phase 5: HistoryStrip beat-aligned header (bar/beat readout + pip row + pattern preview)
- `0bb6953` — Phase 6: BeatGrid component refactor (PatternPanel + HistoryStrip; TransportBar deliberately not migrated)
</completed_work>

<remaining_work>

**Next session — start here:**
- **Brutal code review of PR #89** via the `brutal-code-critic` agent. See `<next_action>`.

**Real follow-up work (not in PR #89):**
- **True Quantized mode** — currently falls through to Live. Needs router-thread MIDI buffer that delays user `handle_note_on` until next pattern-on cell. ~50-80 lines + careful diff-reharm coordination.
- **TransportBar pip migration to BeatGrid** — deliberately deferred during phase 6. Has distinctive `class:downbeat` + `pixel-btn` styling tied to its aesthetic.

**UAT gaps to validate next:**
- Pattern triggers audibly across all three input modes
- NoteOff fires correctly on transport stop mid-pattern
- NoteOff fires correctly when panel pip toggled off mid-pattern
- Knob tooltips appear and persist correctly during drag
</remaining_work>

<decisions_made>

- **Default Knob accent: cyan** (was amber) — matches Serum reference screenshot
- **Pattern master enable tied to panel-pip visibility** — `PatternPanel.svelte` onMount/onDestroy calls `pattern.setEnabled(true/false)`
- **`set_pattern_enabled(true)` auto-starts the transport** — UX papercut fix; user was confused why pattern didn't fire (transport was frozen)
- **Quantized falls through to Live** — true input-onset quantization needs MIDI buffer; deferred
- **TransportBar NOT migrated to BeatGrid** in phase 6 — visual regression cost > code-org gain
- **Cell-tick: NoteOff before NoteOn on retriggers** — explicit re-attack rather than NoteOn-on-already-held silently no-op
- **Live/Quantized share path; Gated separate "rising edge only" branch** — Gated = legato, Live = staccato
- **Drain-on-disable** — pattern disabled or transport stopped mid-cell-on → router NoteOffs harmony to prevent stuck-note feel
</decisions_made>

<blockers>
None.
</blockers>

## Required Reading (in order)
1. `.planning/PERFORMANCE-VIEW.md` — original design doc (8-knob view); pattern feature scoped against this
2. PR #89 description on GitHub — test plan + feature summary
3. `src-tauri/src/commands/engine.rs` lines ~410-510 — router pattern-tick logic
4. `src-tauri/src/state.rs` `PatternConfig` + `PatternInputMode` — backend state shape
5. `ui/src/lib/stores/pattern.svelte.ts` — frontend store + dispatch flow
6. `ui/src/lib/components/BeatGrid.svelte` — extracted shared widget

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention |
|---------|-------------|----------|------------|
| Pushing past context-limit warnings | This session crossed 80% repeatedly while user demanded more features. Each push past 85% increased risk of partial edits breaking the build. | advisory | Treat 85% as a hard pause point; commit and stop even if user asks to continue. |
| Tying user-facing entry to one subsystem when feature requires another | Initial pattern impl required user to find the separate transport play button. User saw "nothing happening" — root cause was `transport.is_running()=false`. Fixed by coupling `set_pattern_enabled→transport.play()`, but diagnosis took 10+ min of frustration. | advisory | When a feature depends on transport state, check that the user-facing entry point engages transport. |

## Infrastructure State

- **Branch**: `feat/bpm-clock` (10 commits ahead of `main` after PR #87 + #88 merged)
- **PR #89**: open, ready for review at https://github.com/contrapunk-audio/contrapunk/pull/89
- **PR #88**: merged (MIDI Learn + MPK Mini preset)
- **PR #87**: merged (Performance view scaffold + diff-reharm + continuous Spread)
- **Tauri dev**: was running on user's machine when paused; auto-rebuilds on Rust changes

## Pre-Execution Critique Required

- **Design artifact**: PR #89 itself (10 commits)
- **Critique focus**:
  1. Stuck-note edge cases — transport stop, panel toggle, mode change at cell boundary
  2. `harmony_notes` snapshot timing in router pattern-tick — race conditions with `process_midi_message` updates
  3. Diff-based reharm path × pattern-driven NoteOn/Off — could double-release a note, or leave one stuck?
  4. `last_pattern_cell_on` defaults to `false` — wrong first-boundary behavior when pattern enabled mid-song with a cell already on?
  5. BeatGrid 3-variant API (`pip`/`cell`/`mini`) — over-abstraction or right balance?
  6. BeatGrid `disabled = !onClick` — accessibility implications (focus, keyboard nav)?
  7. `set_pattern_enabled` auto-starting transport — clean coupling or surprising side effect?
- **Gate**: Do NOT merge PR #89 until brutal critique completes and any blocker findings are addressed.

<context>
User drove this work hard, pushing past context warnings to ship features. PR #89 = ~6 hours of design + implementation. Feature works end-to-end but hasn't been deeply audited. Brutal review is the natural next step before merge — user explicitly queued it ("in the next session we have to do a brutal code review critic").

Design vision: BPM-aware step sequencer for rhythmic chord triggers tied to the transport clock. Three input modes (Live/Quantized/Gated) for different idioms. Pattern panel pip is master toggle, auto-starts transport so feature "just works". Cyan knob accent + Serum-style tooltip. HistoryStrip beat-aligned header. BeatGrid extracts cell-strip primitive.

True Quantized is the only remaining feature gap.
</context>

<next_action>
Spawn the `brutal-code-critic` agent on PR #89's 10 commits. Brief it with:

> Brutal critique of PR #89 (`feat/bpm-clock`, 10 commits) — BPM-aware step-sequencer pattern feature for Contrapunk's harmony engine. Probe specifically for:
> 1. Stuck-note edge cases at transitions (transport stop / panel toggle / mode change at cell boundary)
> 2. Race conditions in `harmony_notes` snapshot vs. concurrent `process_midi_message` updates
> 3. Diff-based reharmonization (existing) × pattern-driven NoteOff/NoteOn (new) — could double-release or leave stuck?
> 4. `last_pattern_cell_on` initialization to `false` — wrong when pattern enabled mid-song with a cell-on already active
> 5. BeatGrid 3-variant API design (`pip`/`cell`/`mini`) — over-abstraction or right balance
> 6. `set_pattern_enabled(true)` auto-starting transport — clean coupling or surprising
>
> Focus files:
> - `src-tauri/src/commands/engine.rs` (router pattern-tick ~lines 410-510)
> - `src-tauri/src/commands/harmony.rs` (`set_pattern_enabled` + `set_pattern_config`)
> - `src-tauri/src/state.rs` (`PatternConfig`, `PatternInputMode`)
> - `ui/src/lib/stores/pattern.svelte.ts`
> - `ui/src/lib/components/PatternPanel.svelte`
> - `ui/src/lib/components/BeatGrid.svelte`
> - `ui/src/lib/components/HistoryStrip.svelte` (new beat-header section)
>
> Do NOT modify code. Diagnostic only. Output: prioritized punch-list of bugs / smells / over-abstractions.

After critique returns, address blocker findings before merging PR #89.
</next_action>

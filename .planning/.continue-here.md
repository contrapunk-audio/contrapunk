---
context: research
milestone: v1.2.x
active_roadmap: .planning/research/ROADMAP-v1.2.x.md
status: paused_mid_milestone
last_updated: 2026-05-11T14:18:13Z
---

# Where we are

**Milestone**: v1.2.x (locked roadmap at `.planning/research/ROADMAP-v1.2.x.md`).
**Phase**: 2 of 3 — Bugs + small features. **9 of 13 Phase 2 issues shipped + 2 slices of #81**.
**Phase 3**: not started (#9 plugin Path B spike).

Two-session arc:
- **Last session (May 6)** — v1.1.1 release, project-level Claude Code harness scaffolded, 9-agent research wave + brutal-code-critic review, v1.2.x roadmap locked with 5 user-answered decisions, clean-room candidates doc, Stop hook.
- **This session (May 11)** — Phase 1 complete, 9 Phase 2 issues + 4-commit #91 + 2 slices of #81 + multiple Stop-hook iterations.

# 5 locked decisions from last session

| Q | Decision | Why |
|---|---|---|
| 1 | Defer plugin signing — ship unsigned for v1.2.x | $99/yr Apple + ~$400/yr Windows cert spend + per-release notarization isn't worth it before plugin has user pull |
| 2 | #82 guitar pipeline rewrite → own milestone (v1.3.x+) | XL effort, separate concern |
| 3 | Jam dropped — #106 + #105 deferred | itch.io rejected jam posts; joining other jams instead. Website needs jam content hidden (private repo, can't touch from here) |
| 4 | **ZERO AGPL/GPL stance everywhere** | License contamination on MIT binary. #98 Ableton Link DROPPED (rusty_link is GPLv2+). Clean-room candidates become forward path |
| 5 | Companion architecture ready → wire #91 | Shipped in 4 commits this session |

# What shipped this session — `cfbd888 .. 9874028`

```
9874028 feat(harmony): Pearson correlation + profile rotation (#81 slice 2)
61a41a7 feat(harmony): add Krumhansl-Schmuckler profile constants (#81 slice 1)
98593c3 feat(commands): companion_set_enabled + companion_is_enabled (#91 C-minimal)
5045c36 feat(router): Companion::on_input + suppress_default (#91 B)
2060f07 harness(autoloop): remove stuck-detection — go until 90% or kill switch
bc8a71c feat(router): tick Companion + dispatch ops (#91 A)
c622e04 refactor(state): add Companion to AppState (#91 A-minus)
a73d635 harness(autoloop): track consecutive no-commit fires (later reverted)
3f93165 harness(autoloop): detect stuck-on-nothing iterations (later reverted)
99b28e0 feat(ui): AUTOPLAY scale-walker component (#42)
f895cc0 feat(ui): chord mini app route at /chord (#12)
f4901ce feat(plumb): wire bass-register suppression across WASM + Tauri (#100)
2c289fd feat(harmony): bass-register suppression for #100
cb7d5d9 docs: add DAW side-by-side setup guide (#99)
8064193 fix(routing): CC 123 panic — drain all tracked notes (#90)
2cc3812 fix(guitar): gate initial-bend emit on 10-cent threshold (#79)
cfbd888 fix(routing): warn when MIDI ports selected but no voice routes (#14)
```

**Test counts at pause (verified `cargo test`)**:
- `contrapunk-harmony --lib`: 265 (was 249 at session start; **+16**)
- `contrapunk-tauri --bin contrapunk-tauri`: 49 (was 31; **+18**)
- `contrapunk-wasm --lib`: 3 (unchanged)
- `contrapunk-audio --lib`: 205 (unchanged)

# What's remaining

## Phase 2 (4 issues)

| # | Title | Effort | Status / Blocker |
|---|---|---|---|
| #81 | Krumhansl auto-key detector | S left | **Slices 1+2 shipped** (profile constants + Pearson + rotation + 11 new tests). **Slice 3 = compose helpers into `KeyDetector::detect`, replace `score_tonic` with `pearson_correlation(histogram, rotate_profile(profile, tonic))` argmax**. Existing 5 detection tests must still pass. |
| #65 | Presets UI redesign (pill-row) | S | Needs UI dev-server session + visual UAT. CLI autoloop can't do this. |
| #66 | Canonical embed wave 3+4 (Piano wrapper swap + ChordReadout extract) | S | Regression risk on `components/Piano.svelte` features the embed version doesn't have. Visual UAT needed. |
| #8b | Per-voice phase offset slider | S | **BLOCKED on investigation**: humanizer module referenced in spec doesn't exist in current `src/` tree (`grep humanize` returns nothing in src/ or src-tauri/). Find where per-voice timing lives now in companion/ before designing. |

## Phase 3 (1 issue)

| # | Title | Effort | Notes |
|---|---|---|---|
| #9 | Plugin Path B spike + unsigned ship | M | Day 1 VIZIA spike: load 8-param native UI in Logic Pro. **If pass**: rebase `plugin/` onto `BillyDM/nih-plug` (current `robbert-vdh/nih-plug` upstream is DEAD as of March 2026), build unsigned `.vst3` + `.au` + `.clap`, 3-DAW UAT. **If fail**: Path C parameters-only UI. Signing deferred per Q1. |

## Deferred to v1.3.x+ (explicit)

`#82` guitar rewrite (own milestone), `#27` fixtures, `#28` Performance Mode (spike only), `#29` poly-pitch (research only), `#3` Canon (build as Lane after #91), `#97` Sampler (module not crate), `#103` BeatMachineLane (after LooperLane validates), `#102` ListenLane (spike only), `#104` DDSP (external), `#10` openDAW, `#11` SonoBus (docs only — GPLv3), `#15` Strudel+OSC (AGPL), `#31` TouchDesigner (td-rs no LICENSE), `#101` visualizer, `#105` + `#106` (jam dropped), `#8a` TempoEstimator, `#8c` pattern detection. **#98 Ableton Link DROPPED** — GPLv2+ contamination.

## Clean-room candidates (separate MIT repos, none started)

| Repo | Replaces | Effort | Priority |
|---|---|---|---|
| `contrapunk-link` | `rusty_link` (GPLv2+) | L | **First** — smallest, most reusable |
| `contrapunk-viz` | `hydra-synth` (AGPL-3.0) | L | Second — visual moat |
| `contrapunk-livecode` | `@strudel/web` (AGPL-3.0) | XL | Third — harmony-aware DSL is strategic moat |
| `contrapunk-net` | SonoBus (GPLv3 + JUCE) | XL+ | Last — only after 50+ user asks |

All four default to **Rust core + WASM bindings + thin JS host surface** (see `.planning/research/CLEAN-ROOM-CANDIDATES.md`).

# Decisions made this session

- **#81 sliced into 3** — profile constants → Pearson helpers → detector swap. Slices 1+2 done; slice 3 is what remains.
- **#91 sliced into 4** — AppState skeleton → router tick + dispatch helper → on_input + suppress_default → enable/disable commands. All 4 shipped.
- **Stop autoloop reverted** to simple "go until 90% or kill switch" after the user pointed out the stuck-detection was bailing at 69%. State file + 180s wall-clock heuristics removed.
- **Tract-onnx wins over ort/candle/wonnx** for future ONNX work — pure Rust, MIT/Apache, WASM-targetable (locked in `group-b-pitch-ml.md` + `CRITIQUE.md`).

# Blockers

| Blocker | Type | Workaround |
|---|---|---|
| #8b humanizer module not found | Investigation | Grep `companion/`, `chain/`, `synth/` for per-voice timing offsets |
| #65/#66 UI verification | Session shape | Run `npm run dev` in `ui/` + manual UAT in browser-mode session |
| Pre-existing dead-code warnings | External | Not introduced this session; defer to `/gsd-map-codebase` refresh |

# Required Reading (in order)

1. `.planning/research/ROADMAP-v1.2.x.md` — locked milestone plan
2. `.planning/research/CRITIQUE.md` — verdicts the roadmap is built from
3. `.planning/research/CLEAN-ROOM-CANDIDATES.md` — license-blocked items + Rust-first clean-room plan
4. `.planning/research/91-companion-router-wiring-prep.md` — #91 design doc (mostly implemented now)
5. `.planning/research/concerns-staleness-check.md` — CONCERNS.md staleness audit
6. `CLAUDE.md` — project-level harness rules (4-surface map, test commands, footguns)
7. `.claude/skills/code-entropy/SKILL.md` — entropy methodology used by SessionStart hook

# Critical Anti-Patterns (advisory)

| Pattern | Description | Severity | Prevention |
|---|---|---|---|
| Roadmap-marker-grep as pending-work signal | Autoloop hook's `grep ^## Phase` kept firing forever once concrete tasks were exhausted | advisory | Use `context >= 90%` or kill switch only (reverted in `2060f07`) |
| Trusting CONCERNS.md as ground truth | Two Phase 1 tasks referenced `src/audio_out/` which no longer exists — was reworked into `src/synth/` without CONCERNS.md being refreshed | advisory | Verify file/symbol references in CONCERNS.md against live tree before planning. See `.planning/research/concerns-staleness-check.md` |
| Premature plugin-ification | Group-c research proposed `crates/contrapunk-sampler/` as new workspace member; critic flipped to "module in `crates/contrapunk-audio/`" | advisory | Crate-vs-module driven by circular-dependency risk or consumer plurality, NOT line count |
| Misleading UI tooltip text | Species 2 tooltip says "Two harmony notes per melody note" but engine produces 1 harmony per call. Caused real support thread. | advisory | UI text should describe observable behavior, not music-theory definitions |

# Infrastructure State

- **Branch**: `main`, pushed to `origin/main` at `9874028` (and handoff commit on top once this lands)
- **Working tree**: clean before this pause-work commit
- **Kill switch**: `.claude/stop-loop.disabled` is **ACTIVE** — remove next session to re-enable autoloop
- **GitHub release**: `v1.1.1` published with `Contrapunk_1.1.1_universal.dmg` asset attached
- **Open GitHub issues**: 32 substantive remaining (37 total minus closed #2, #70 + 3 docs)
- **Companion orchestrator**: live in router thread but `enabled = false` + zero Lanes → fully no-op until a Lane registers and master switch flips

# Next action — pick one

1. **#81 slice 3 (S, ~30 min)** — Compose `pearson_correlation` + `rotate_profile` into new `KeyDetector::score_tonic_ks(tonic) -> f32`. Replace call site in `detect()`. Verify existing 5 detection tests pass. Add 2 comparison tests.

2. **#9 plugin Path B Day-1 spike (XS)** — Minimal VIZIA editor in `plugin/src/editor.rs` exposing existing 8 params. `cargo build -p contrapunk_plugin --release`, load `.vst3` in Logic Pro, verify GUI + param round-trip. **Kill switch on day 1**: if fail, drop Path B, fall back to Path C.

3. **UI session for #65 + #66** — Different session shape: `npm run dev` in `ui/`, browser open, visual iteration. Touches `ControlPanel.svelte` (Presets pill-row) + `components/Piano.svelte` (wrapper-swap to `embed/Piano.svelte`).

4. **#8b investigation (XS, ~15 min)** — Grep `companion/`, `chain/`, `synth/` for per-voice timing. Document at `.planning/research/8b-humanizer-location.md`. Decide if feature lands as a Lane, `SynthParams` field, or something else.

5. **Start `contrapunk-link` clean-room** — Separate MIT Rust crate. Read Ableton Link wire-protocol spec, implement from scratch. ~L effort, useful even outside Contrapunk.

To resume: `/gsd-resume-work` or open this file directly.

# Pre-Execution Critique Required

None for this pause — every shipped commit was verified green (`cargo test` + push). No design-only artifacts left in flight.

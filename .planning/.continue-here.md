---
context: default
phase: null
status: paused
last_updated: "2026-05-17T10:09:57Z"
head_commit: 9e6af50a
---

# Pause Context — I/O tab restructure + calibration hot-swap + live QA loop

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---|---|---|---|
| Store-without-consumer wire claims | Calibration profile was "wired" three times across three critic rounds — each fix moved the problem one layer deeper (stored but unread → applied at startup only → never hot-swapped on save). | blocking | Trace data flow end-to-end before declaring a feature "wired." Brutal-critic prompts must explicitly verify downstream READS, not just writes. Live QA catches what static analysis misses. |
| Coverage theater in new tests | `synthetic_pluck_…can_produce_events` accepted 0 events as "valid" in its own comment. `set_calibration_profile_seeds_noise_floor_ema` asserted `0.0 < x < 0.5` (99.99% pass band). `test_contrary_motion_preferred…` asserted `result.len() == 2` instead of the contrary motion itself. | advisory | Brutal-critic agents must flag any test where the assertion accepts the null/zero/empty case as success. Tighten bounds to ±epsilon against actual computed values. |
| Killing dev server while debugging | When I killed `cargo tauri dev` / `vite dev` to inspect, the user's webview dangled on ws://localhost:5173 and clicks stopped firing — user reported "Start button isn't working" but the real cause was dead HMR socket. | advisory | Don't kill dev servers during debugging. If you must restart, tell the user to close + reopen the Tauri webview. |
| Auto-persistence of opt-in-required sources | FTUX restored saved Guitar Audio sentinel on launch and auto-started routing → open mic at app start. | advisory | Audio-input sources that consume the microphone or require per-session config should never auto-restore on launch. |

<current_state>
v1.3.0 release deferred indefinitely (user direction). Working on `main` directly. Last 12+ commits implement the I/O tab restructure, MidiDevices deletion, calibration live-hot-swap pipeline, brutal-critic 1→2→3 fixes, and 3 rounds of live-QA bug fixes from the running Tauri build.

Most recent commit `9e6af50a` fixes: (a) Guitar-as-default-input regression (FTUX now resets Guitar Audio sentinel → Computer Keyboard on launch), (b) calibration apply spam (was firing 1-2x/sec because get_calibration_status re-applied on every poll — now gated on disk mtime change).

Apple Developer Program subscription is provisioning (~48h). Signing pipeline (6d5d14f1) ready to consume secrets when they arrive.
</current_state>

<completed_work>

Major surfaces shipped:
- I/O tab restructure (7 steps, 4 brutal-critic rounds): InputPanel, OutputPanel, VoiceGenerationChain, VoiceCard, MidiPermissionCard
- MidiDevices.svelte deleted (-416 LOC); Harmony tab now pure harmony controls
- Calibration pipeline: AppState slot, GuitarBridge publishes pipeline Arc, load/save/get_status commands, bounded retry hot-swap, mtime-gated re-apply
- Sustain + Decay gating in normalizer brush-rejection (round-2 + round-3 critic fixes)
- macOS Developer ID signing CI (entitlements.plist + macos-build.yml), gated on tag-release secrets
- ARIA tablist + roving tabindex + arrow-key nav on tab/subtab strips
- OS-agnostic getUserMedia error messages covering NotAllowed/NotFound/NotReadable/Security/Abort/Overconstrained/TypeError
- Stale planning docs banners (STRUCTURE/CONCERNS/ARCHITECTURE)
- 218 audio unit tests + 6 integration tests + 270 harmony tests, all passing
</completed_work>

<remaining_work>

- Round-4 brutal critic on post-9e6af50a state (user interrupted the round-4 spawn to report bugs; never re-run)
- User manual QA on a freshly restarted `cargo tauri dev`: confirm Start works, BPM responds, MIDI radio works, no spam in console
- Verify `current_note.string_idx` is actually populated in real audio frames (per-string normalizer hint depends on it — wired but not field-verified)
- Windows atomic-rename: current cfg(windows) fallback uses remove-then-rename; proper fix is MoveFileExW via windows-sys (documented as caveat, not blocking macOS)
- Nested mutex topology: round-3 critic flagged `Arc<Mutex<Option<Arc<Mutex<GuitarInput>>>>>` as design fragility. Consider Weak<Mutex<...>> for zombie-safety. Deferred — not a bug.
</remaining_work>

<decisions_made>

- v1.3.0 tag indefinitely deferred (user: "forget about 1.3 and any release completely")
- Computer Keyboard is the auto-default input even when user previously had Guitar Audio saved (avoids hot-mic surprise on launch)
- Normalizer brush/scrape rejection gated on `Idle | Attack` only — `Sustain` and `Decay` excluded (McLeod's adaptive_clarity filter makes confidence=0.0 there, would false-reject vibrato peaks and decay-tail re-plucks)
- Live calibration hot-swap via try_lock with 5-attempt 1ms-sleep retry (audio thread holds inner mutex ~2-2.5ms of 2.7ms callback)
- Calibration profile re-apply gated on disk mtime change (was firing on every UI status poll)
- MidiDevices.svelte deleted; "midi" panel id dropped from PanelId enum (restorePanels silently ignores unknown ids — no migration)
- selectSource('midi') from null state defaults to Computer Keyboard (visible picker + working input); from guitar w/o prior MIDI also defaults to keyboard UNLESS engine is mid-take (then clearInput to avoid hot-keyboard)
- Subtab strip moved INSIDE .io-area (was a grid sibling stealing the 1fr row, pushing content to the bottom)
- Apple Developer ID signing wired CI-side; secrets pending the 48h provisioning window
</decisions_made>

<blockers>
None functional. User's existing Tauri webview is dangling on a dead WebSocket because I killed the dev server while debugging — they need to close + reopen after restarting `cargo tauri dev`.
</blockers>

## Required Reading (in order)
1. `.planning/HANDOFF.json` — structured handoff (machine-readable, has full commit refs + decision log)
2. `CLAUDE.md` — project guide; especially the "four surfaces" note (any harmony fix must consider Tauri / WASM / nih-plug / CLI)
3. Conversation memory — round-2/round-3 brutal critic outputs are in this conversation, not on disk

## Infrastructure State
- `cargo tauri dev` / `vite dev`: NOT running (I killed mine; user's webview is dangling)
- Calibration file: `~/Library/Application Support/com.contrapunk.app/guitar_calibration_profile.json` (default v1, 0 samples)
- Branch: `main`, at `9e6af50a`, pushed to `origin/main`
- Working tree: clean
- Tests: 218 audio unit + 6 integration + 270 harmony + 42 src-tauri = 632+ passing, 1 ignored (pre-existing)

## Pre-Execution Critique Required
Not applicable — we're not pausing between design and execution. Pausing mid-iterative-QA-loop.

<context>
The pattern that worked best this session: brutal-critic agent → fix → push → user does live manual QA on the running app → reports specific symptoms → I diagnose + fix → critic again. Critics caught architectural issues (write-only fields, slot leaks, race windows); live QA caught UX surprises (empty layout, hot mic on launch, broken click handlers) that no static analysis surfaced.

The user is testing on real macOS hardware with a Tauri dev build. They have ZERO interest in shipping v1.3.0 right now — this work is forward progress on `main`, not anchored to a release. Don't propose release/tag actions unprompted.

The Apple Developer Program subscription is real but ~48h delayed. Don't try to set up signing secrets yet — the user will say "wait."
</context>

<next_action>
1. Wait for user to restart `cargo tauri dev` and confirm Start/BPM/MIDI all work on the post-9e6af50a build.
2. If clean: optionally spawn the round-4 brutal critic that was interrupted. The round-3 critic ID was `ac01fa6783c4b3a22` and is still continuable via SendMessage if you want to chain.
3. If user reports new bugs: same iterative loop — diagnose from logs, fix, commit, push.
4. Do NOT propose tagging v1.3.0 or any release-related action unprompted.
</next_action>
